---
phase: 35-homepage-feature-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/reader/HeroArticle.tsx
  - src/components/reader/MascotGreeting.tsx
  - src/components/reader/FragDenWurzelmannCard.tsx
autonomous: true
requirements: [HOME-01, HOME-02, HOME-04]

must_haves:
  truths:
    - "The Topmeldung hero displays a VOLLSTAENDIGEN ARTIKEL LESEN CTA span above the gradient"
    - "MascotGreeting renders as a flat tonal Wurzel sagt box with no speech bubble tail and no avatar image"
    - "Frag den Wurzelmann card renders with bg-ink dark background and dispatches openBezirkModal on click"
  artifacts:
    - path: "src/components/reader/HeroArticle.tsx"
      provides: "CTA button overlay on hero gradient"
      contains: "VOLLSTAENDIGEN ARTIKEL LESEN"
    - path: "src/components/reader/MascotGreeting.tsx"
      provides: "Tonal box greeting without speech bubble"
      contains: "Wurzel sagt"
    - path: "src/components/reader/FragDenWurzelmannCard.tsx"
      provides: "Dark green card with bezirk modal trigger"
      exports: ["FragDenWurzelmannCard"]
  key_links:
    - from: "src/components/reader/FragDenWurzelmannCard.tsx"
      to: "BezirkModal"
      via: "window.dispatchEvent(new Event('openBezirkModal'))"
      pattern: "openBezirkModal"
---

<objective>
Restyle three homepage components for the Archivist identity -- add CTA to Topmeldung hero, convert MascotGreeting from speech bubble to tonal box, and create the Frag den Wurzelmann dark card.

Purpose: HOME-01, HOME-02, and HOME-04 are independent UI changes that can run parallel with Plan 01's database work. These components will be wired into HomepageLayout in Plan 03.
Output: Three updated/new components ready for homepage integration.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-homepage-feature-components/35-RESEARCH.md

@src/components/reader/HeroArticle.tsx
@src/components/reader/MascotGreeting.tsx
@src/components/reader/HomepageLayout.tsx

<interfaces>
<!-- Key types the executor needs -->

From src/components/reader/HeroArticle.tsx:
```typescript
// HeroArticle is wrapped in a single <Link href={href}> — the entire card is clickable.
// CTA must be a <span> (not <Link> or <button>) to avoid nested interactive elements.
// The content zone is the <div className="relative z-10 flex flex-col justify-end ..."> at the bottom.
```

From src/components/reader/MascotGreeting.tsx:
```typescript
// Currently: "use client" with Image import, speech bubble with triangular tail, avatar image
// Time-of-day greetings: GREETINGS object with morning/afternoon/evening slots
// getTimeOfDay() returns "morning" | "afternoon" | "evening"
```

Established event pattern (from BezirkModal):
```typescript
// Fire: window.dispatchEvent(new Event('openBezirkModal'))
// Listen: useEffect with window.addEventListener('openBezirkModal', handler)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HeroArticle CTA button + MascotGreeting tonal restyle</name>
  <files>src/components/reader/HeroArticle.tsx, src/components/reader/MascotGreeting.tsx</files>
  <action>
    **HeroArticle.tsx (HOME-01):**
    Add a CTA span after the excerpt paragraph inside the content zone div (the `relative z-10 flex flex-col justify-end` div). The span is decorative (the outer Link already handles navigation):

    ```tsx
    <span className="mt-4 inline-block self-start px-4 py-2 font-label font-semibold uppercase text-xs tracking-wider text-parchment bg-ink/70 border border-parchment/30 rounded-xs">
      VOLLSTAENDIGEN ARTIKEL LESEN
    </span>
    ```

    Place this after the excerpt `{excerpt && ...}` block, still inside the content zone div. Use the German text exactly: "VOLLSTAENDIGEN ARTIKEL LESEN" (all caps, matching the requirement).

    **MascotGreeting.tsx (HOME-02):**
    Complete restyle. Keep the `"use client"` directive and the `getTimeOfDay()` / `GREETINGS` logic. Remove:
    - The `Image` import and avatar image
    - The speech bubble container with triangular tail (the `<span>` with border tricks)
    - The `flex-col items-start` wrapper structure

    Replace the entire return JSX with a flat tonal box:
    ```tsx
    return (
      <div className="px-[var(--spacing-gutter)]">
        <div className="bg-surface rounded-xs px-4 py-3">
          <p className="font-label uppercase text-xs font-semibold text-ink-muted mb-1 tracking-wider">
            Wurzel sagt ...
          </p>
          <p className="font-headline text-base font-semibold text-ink leading-snug">
            {greeting}!
          </p>
          <p className="font-label text-sm text-ink/60 mt-0.5">{quote}</p>
        </div>
      </div>
    );
    ```

    Remove the `import Image from "next/image"` since the avatar is gone.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>HeroArticle shows CTA text on gradient, MascotGreeting renders as flat tonal box with "Wurzel sagt ..." label, no speech bubble tail, no avatar image.</done>
</task>

<task type="auto">
  <name>Task 2: FragDenWurzelmannCard component</name>
  <files>src/components/reader/FragDenWurzelmannCard.tsx</files>
  <action>
    Create src/components/reader/FragDenWurzelmannCard.tsx as a client component that triggers the BezirkModal via the established openBezirkModal event pattern:

    ```tsx
    "use client"

    export function FragDenWurzelmannCard() {
      return (
        <div className="px-[var(--spacing-gutter)]">
          <button
            onClick={() => window.dispatchEvent(new Event('openBezirkModal'))}
            className="w-full text-left"
          >
            <p className="font-label uppercase text-xs font-semibold text-parchment/60 mb-1 tracking-wider">
              Dein Bezirk
            </p>
            <p className="font-headline text-xl font-semibold text-parchment leading-tight mb-2">
              Frag den Wurzelmann
            </p>
            <p className="font-label text-sm text-parchment/70">
              Waehle deinen Bezirk fuer hyperlokal gefilterte Nachrichten.
            </p>
          </button>
        </div>
      )
    }
    ```

    Note: The bg-ink wrapper for the dark background will be applied by HomepageLayout in Plan 03 (the card itself just handles content and click behavior). This keeps the component reusable and lets HomepageLayout control section backgrounds per HOME-06.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>FragDenWurzelmannCard component exists, renders dark-themed content, dispatches openBezirkModal event on click.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — no type errors
- HeroArticle.tsx contains "VOLLSTAENDIGEN ARTIKEL LESEN" span
- MascotGreeting.tsx has no Image import, no triangular tail span, no avatar
- MascotGreeting.tsx contains "Wurzel sagt" label text
- FragDenWurzelmannCard.tsx exports the component and dispatches openBezirkModal
</verification>

<success_criteria>
- HeroArticle shows CTA overlay text on the gradient
- MascotGreeting is a flat tonal "Wurzel sagt ..." box (no bubble, no avatar)
- FragDenWurzelmannCard dispatches the Bezirk modal event on click
- All three components compile without type errors
</success_criteria>

<output>
After completion, create `.planning/phases/35-homepage-feature-components/35-02-SUMMARY.md`
</output>
