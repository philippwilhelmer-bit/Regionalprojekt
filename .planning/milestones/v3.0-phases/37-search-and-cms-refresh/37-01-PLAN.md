---
phase: 37-search-and-cms-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/login/page.tsx
  - src/app/admin/login/login-form.tsx
  - src/app/(admin)/layout.tsx
  - src/app/(admin)/admin/articles/page.tsx
  - src/app/(admin)/admin/articles/ArticleRow.tsx
  - src/app/(admin)/admin/articles/ArticleFilters.tsx
  - src/app/(admin)/admin/articles/[id]/edit/page.tsx
  - src/app/(admin)/admin/articles/new/page.tsx
  - src/app/(admin)/admin/sources/page.tsx
  - src/app/(admin)/admin/sources/SourceCard.tsx
  - src/app/(admin)/admin/exceptions/ExceptionCard.tsx
  - src/app/(admin)/admin/ai-config/page.tsx
  - src/app/(admin)/admin/ai-config/GlobalAiConfigForm.tsx
  - src/app/(admin)/admin/ai-config/SourceOverrideForm.tsx
  - src/components/admin/LogoutButton.tsx
autonomous: true
requirements:
  - CMS-01
  - CMS-02

must_haves:
  truths:
    - "Every CMS admin page renders with parchment background and ink text -- no white/fallback backgrounds from undefined legacy tokens"
    - "All CTA buttons display as rounded-sm ink-gradient buttons -- no rounded-full pill shapes remain"
    - "All form inputs show ink-colored focus rings -- no blue/green primary-colored rings on focus"
    - "The article edit form contains a working theme tag select field styled with Archivist tokens"
  artifacts:
    - path: "src/app/admin/login/page.tsx"
      provides: "Archivist-styled login page"
      contains: "bg-parchment"
    - path: "src/app/(admin)/layout.tsx"
      provides: "Archivist-styled admin shell"
      contains: "bg-parchment"
    - path: "src/app/(admin)/admin/articles/[id]/edit/page.tsx"
      provides: "Article edit form with theme field"
      contains: "gruene_woche"
    - path: "src/components/admin/LogoutButton.tsx"
      provides: "Archivist-styled logout button"
      contains: "text-ink-dim"
  key_links:
    - from: "src/app/(admin)/layout.tsx"
      to: "src/components/admin/LogoutButton.tsx"
      via: "import and render"
      pattern: "LogoutButton"
    - from: "src/app/(admin)/admin/articles/[id]/edit/page.tsx"
      to: "updateArticle server action"
      via: "form submission with theme field"
      pattern: "name=\"theme\""
---

<objective>
Migrate all CMS admin pages from legacy MD3 tokens to Archivist Ink/Parchment/Slate tokens. After this plan, every admin surface — login, layout sidebar, articles list, article edit, article new, sources, exceptions, AI config — uses the v3.0 Archivist color system with no legacy token remnants.

Purpose: CMS-01 requires consistent Archivist treatment across all admin pages. CMS-02 requires verifying the existing theme tag field is correctly styled.
Output: 15 admin files updated with Archivist tokens, zero legacy `primary`/`secondary`/`text`/`background`/`accent` tokens remaining.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-search-and-cms-refresh/37-RESEARCH.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate login, admin layout, and article pages to Archivist tokens</name>
  <files>
    src/app/admin/login/page.tsx
    src/app/admin/login/login-form.tsx
    src/app/(admin)/layout.tsx
    src/app/(admin)/admin/articles/page.tsx
    src/app/(admin)/admin/articles/ArticleRow.tsx
    src/app/(admin)/admin/articles/ArticleFilters.tsx
    src/app/(admin)/admin/articles/[id]/edit/page.tsx
    src/app/(admin)/admin/articles/new/page.tsx
    src/components/admin/LogoutButton.tsx
  </files>
  <action>
Apply the Archivist token replacement systematically across each file. Process each file completely before moving to the next — do not leave partial replacements.

**Token replacement map (apply everywhere):**
- `bg-background` -> `bg-parchment`
- `text-text` -> `text-ink`
- `text-text/70` -> `text-ink-muted`
- `text-text/60` -> `text-ink-muted`
- `text-text/50` -> `text-ink-dim`
- `from-primary to-primary-container` -> `from-ink to-ink-soft`
- `text-white` (on dark button backgrounds only) -> `text-parchment`
- `rounded-full` (on buttons) -> `rounded-sm`
- `ring-primary` -> `ring-ink`
- `border-surface` (on inputs) -> `border-parchment-dim`
- `text-secondary` -> `text-slate`
- `text-accent` -> `text-red-600` (for error text)
- `bg-accent/10` -> `bg-red-50` (for error backgrounds)

**Status badge strategy — KEEP semantic colors:**
- PUBLISHED/REVIEW/REJECTED badges (green/yellow/red) -> KEEP as-is
- Approve/Reject action buttons (green-600/red-600) -> KEEP as-is
- "Loeschen" destructive button (red) -> KEEP as-is

**Status badge strategy — MIGRATE non-semantic colors:**
- Bezirk badge `bg-green-100 text-green-700` -> `bg-parchment-dim text-ink-muted`
- isPinned `bg-orange-100 text-orange-700` -> `bg-aged-wood-dim/20 text-aged-wood-soft` or `bg-surface text-slate`
- isFeatured `bg-purple-100 text-purple-700` -> `bg-ink/10 text-ink`

**CMS-02 verification (article edit page):**
- Confirm the `<select name="theme">` field exists with options "Kein Thema" and "Gruene der Woche" (or "Grune der Woche")
- Change option label to "Grune der Woche" if it reads differently, to match homepage section title
- Apply Archivist input styling to the theme select (same as other form inputs)

**LogoutButton:** Replace `text-text/50` -> `text-ink-dim`, `hover:text-text` -> `hover:text-ink`
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && grep -rn "from-primary\|to-primary-container\|text-text\b\|bg-background\|ring-primary\|text-secondary\|text-accent\|bg-accent" src/app/admin/login/ src/app/\(admin\)/layout.tsx src/app/\(admin\)/admin/articles/ src/components/admin/LogoutButton.tsx 2>/dev/null | grep -v node_modules | grep -v ".next" || echo "CLEAN — no legacy tokens found"</automated>
  </verify>
  <done>All 9 files use Archivist tokens exclusively. No legacy `primary`, `secondary`, `text`, `background`, or `accent` tokens remain. CTA buttons use `rounded-sm` not `rounded-full`. Theme tag field on article edit is present and Archivist-styled. Semantic status colors (green/red/yellow for PUBLISHED/REJECTED/REVIEW) are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate sources, exceptions, and AI config pages to Archivist tokens</name>
  <files>
    src/app/(admin)/admin/sources/page.tsx
    src/app/(admin)/admin/sources/SourceCard.tsx
    src/app/(admin)/admin/exceptions/ExceptionCard.tsx
    src/app/(admin)/admin/ai-config/page.tsx
    src/app/(admin)/admin/ai-config/GlobalAiConfigForm.tsx
    src/app/(admin)/admin/ai-config/SourceOverrideForm.tsx
  </files>
  <action>
Apply the same Archivist token replacement map from Task 1 across all 6 files. Process each file completely.

**sources/page.tsx:** Replace CTA button gradient + `rounded-full`.

**SourceCard.tsx:** Replace CTA button gradient + `rounded-full`. Migrate non-semantic badges:
- Source type badge `bg-blue-100 text-blue-700` -> `bg-surface text-slate`
- Source health badges (OK/DEGRADED/DOWN green/yellow/red) -> KEEP (semantic)

**ExceptionCard.tsx:** Replace `text-text`, `border-b border-surface` -> `border-b border-parchment-dim`.
- Source type badge `bg-yellow-100 text-yellow-800` -> `bg-surface text-slate` (not semantic — just identifies source type)
- Approve/Reject buttons (green-600/red-600) -> KEEP (semantic action affordance)

**ai-config/page.tsx:** Replace `text-text`, CTA button gradient + `rounded-full`.

**GlobalAiConfigForm.tsx:** Replace `text-text`, `ring-primary` -> `ring-ink`, CTA gradient + `rounded-full`.

**SourceOverrideForm.tsx:** Replace `text-text`, `text-secondary` -> `text-slate`, `ring-primary` -> `ring-ink`, CTA gradient + `rounded-full`. Migrate non-semantic badges:
- Category/keyword badges (purple/amber) -> `bg-surface text-slate`
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && grep -rn "from-primary\|to-primary-container\|text-text\b\|bg-background\|ring-primary\|text-secondary\|text-accent\|bg-accent" src/app/\(admin\)/admin/sources/ src/app/\(admin\)/admin/exceptions/ src/app/\(admin\)/admin/ai-config/ 2>/dev/null | grep -v node_modules | grep -v ".next" || echo "CLEAN — no legacy tokens found"</automated>
  </verify>
  <done>All 6 files use Archivist tokens exclusively. No legacy tokens remain. Source health badges retain semantic green/yellow/red. Approve/reject buttons retain semantic colors. Non-semantic badges (source type, category) use `bg-surface text-slate`.</done>
</task>

<task type="auto">
  <name>Task 3: Full admin token audit and build verification</name>
  <files>
    src/app/admin/login/page.tsx
    src/app/admin/login/login-form.tsx
    src/app/(admin)/layout.tsx
  </files>
  <action>
Run a comprehensive grep across ALL admin files to catch any remaining legacy tokens that were missed in Tasks 1-2. Check for:

1. `from-primary` or `to-primary-container` anywhere in admin
2. `text-text` (word boundary — not matching `text-texture` etc.) anywhere in admin
3. `bg-background` anywhere in admin
4. `ring-primary` anywhere in admin
5. `text-secondary` anywhere in admin
6. `text-accent` or `bg-accent` anywhere in admin
7. `rounded-full` on any button element in admin
8. `text-white` on any dark-background element in admin (should be `text-parchment`)

Fix any remaining occurrences found.

Then run `npx vitest run` to ensure the full test suite passes — token migration must not break any server action logic.

Finally, run `npx next build` (or the project build command) to confirm no build errors from removed/undefined CSS tokens.
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Zero legacy MD3 tokens (`primary`, `secondary`, `text`, `background`, `accent`) remain in any admin file. `rounded-full` removed from all admin buttons. Full test suite passes. Build succeeds without errors.</done>
</task>

</tasks>

<verification>
1. `grep -rn "from-primary\|to-primary-container\|text-text\b\|bg-background\|ring-primary\|text-secondary\|text-accent\|bg-accent\|rounded-full" src/app/admin/ src/app/\(admin\)/ src/components/admin/ 2>/dev/null` returns empty (no legacy tokens)
2. `npx vitest run` passes
3. `npx next build` succeeds
</verification>

<success_criteria>
- CMS-01: All admin pages (login, layout, articles, sources, exceptions, AI config) render with Archivist Ink/Parchment/Slate tokens. No legacy forest-green, warm-cream, or MD3 primary palette remnants visible.
- CMS-02: The article edit form contains a "Grune der Woche" theme select field, styled with Archivist tokens, with the existing server action wiring intact.
- All CTA buttons use `rounded-sm` (not `rounded-full` pill shapes)
- Semantic status colors (green/red/yellow for PUBLISHED/REVIEW/REJECTED, OK/DEGRADED/DOWN, Approve/Reject) are preserved
- Full test suite green, build clean
</success_criteria>

<output>
After completion, create `.planning/phases/37-search-and-cms-refresh/37-01-SUMMARY.md`
</output>
