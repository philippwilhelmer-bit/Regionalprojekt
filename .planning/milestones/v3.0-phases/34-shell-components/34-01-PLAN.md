---
phase: 34-shell-components
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/reader/WurzelNavBar.tsx
  - src/app/globals.css
autonomous: true
requirements: [SHEL-01, SHEL-02]

must_haves:
  truths:
    - "The bottom nav has a frosted-glass background — content scrolling behind it is visible through the translucent surface"
    - "The active nav tab has a top-border indicator instead of a terracotta pill"
    - "The active tab icon renders as filled; inactive icons render as outlined"
    - "Nav tabs show the correct Archivist icons (auto_stories, forest, face_5, book_2)"
  artifacts:
    - path: "src/components/reader/WurzelNavBar.tsx"
      provides: "Glassmorphic bottom nav with updated icons and active state"
      contains: "bg-glass-nav backdrop-blur"
    - path: "src/app/globals.css"
      provides: "icon-filled utility class for Material Symbols FILL axis"
      contains: "icon-filled"
  key_links:
    - from: "src/components/reader/WurzelNavBar.tsx"
      to: "src/app/globals.css"
      via: "bg-glass-nav token and icon-filled class"
      pattern: "bg-glass-nav|icon-filled"
---

<objective>
Replace the opaque parchment bottom nav with a glassmorphic nav using the existing `--color-glass-nav` token, update icons to the Archivist set (auto_stories, forest, face_5, book_2), and implement filled/outlined icon states using Material Symbols FILL axis.

Purpose: The bottom nav is the most-interacted reader chrome — the glassmorphic effect and updated icons establish the Archivist visual identity for every page.
Output: Updated WurzelNavBar.tsx with glass background, top-border active indicator, and filled icon variants.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-shell-components/34-RESEARCH.md
@.planning/phases/33-color-token-foundation/33-03-SUMMARY.md

<interfaces>
<!-- Tokens available from globals.css @theme block -->
--color-glass-nav: color-mix(in srgb, #FCF9EF 85%, transparent);
--shadow-nav: 0 -2px 8px color-mix(in srgb, #071806 8%, transparent);

<!-- Current WurzelNavBar structure (src/components/reader/WurzelNavBar.tsx) -->
- NAV_ITEMS array with {href, icon, label, enabled}
- Uses usePathname() for active detection
- Active state: bg-aged-wood pill, text-parchment icon
- Nav container: fixed bottom-0, bg-parchment, h-16
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Tailwind v4 backdrop-blur prefix and add icon-filled utility</name>
  <files>src/app/globals.css</files>
  <action>
First, verify that Tailwind v4 auto-generates -webkit-backdrop-filter for the backdrop-blur-md utility. Run:
```
npx tailwindcss --input src/app/globals.css --output /tmp/tw-out.css && grep -c "webkit-backdrop-filter" /tmp/tw-out.css
```

If the count is 0 (no webkit prefix), add this override in globals.css after the @theme block:
```css
@layer utilities {
  .backdrop-blur-md {
    -webkit-backdrop-filter: blur(12px);
    backdrop-filter: blur(12px);
  }
}
```

If the count is >= 1, skip the override — Tailwind handles it.

Then, add the icon-filled utility class in globals.css (after the @theme block, outside @layer):
```css
.icon-filled {
  font-variation-settings: 'FILL' 1;
}
```

This class enables the Material Symbols filled variant on active nav icons.
  </action>
  <verify>
    <automated>grep -n "icon-filled" src/app/globals.css && npx tailwindcss --input src/app/globals.css --output /tmp/tw-out.css 2>/dev/null && (grep -c "webkit-backdrop-filter" /tmp/tw-out.css || echo "prefix check done")</automated>
  </verify>
  <done>globals.css contains .icon-filled class. If Tailwind v4 does not auto-prefix backdrop-blur, a manual @layer utilities override is present.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild WurzelNavBar with glassmorphism, top-border active state, and Archivist icons</name>
  <files>src/components/reader/WurzelNavBar.tsx</files>
  <action>
Rewrite WurzelNavBar.tsx with these changes:

1. **Update NAV_ITEMS array** — replace current items with:
   - { href: "/", icon: "auto_stories", label: "Archiv", enabled: true }
   - { href: "#", icon: "forest", label: "Wald", enabled: false }
   - { href: "#", icon: "face_5", label: "Ratgeber", enabled: false }
   - { href: "/suche", icon: "book_2", label: "Bibliothek", enabled: true }

   Note: "Wald" and "Ratgeber" have no existing routes — keep enabled: false (renders with opacity-30 like current disabled items). "/" maps to Archive (homepage), "/suche" maps to Library (search).

2. **Nav container** — change from:
   `bg-parchment shadow-[var(--shadow-nav)] h-16 flex items-center justify-around px-2 rounded-none`
   To:
   `bg-glass-nav backdrop-blur-md shadow-[var(--shadow-nav)] h-16 flex items-center justify-around px-2`

   Remove `rounded-none` (unnecessary — default is no rounding). Remove `bg-parchment`. Add `bg-glass-nav backdrop-blur-md`.

3. **Active indicator** — remove the aged-wood pill (`<span className="... bg-aged-wood ...">` wrapper around the icon). Replace with a top-border on the Link itself:
   - ALL enabled tabs (active AND inactive) get `border-t-2` to prevent height jump
   - Active: `border-ink` (visible border)
   - Inactive: `border-transparent` (invisible, same space)

   Active Link class: `flex flex-col items-center gap-0.5 min-w-[56px] pt-1 border-t-2 border-ink`
   Inactive Link class: `flex flex-col items-center gap-0.5 min-w-[56px] pt-1 border-t-2 border-transparent`

4. **Filled icon state** — active icon gets `icon-filled text-ink`; inactive icon gets `text-slate` (no icon-filled class):
   ```tsx
   <span
     className={`material-symbols-rounded text-xl ${isActive ? "icon-filled text-ink" : "text-ink"}`}
     aria-hidden="true"
   >
     {item.icon}
   </span>
   ```

5. **Active label styling** — active: `text-ink font-semibold`; inactive: `text-slate`. Remove `text-aged-wood` references.

6. **Disabled items** — keep current opacity-30 pattern but update icon names and add `border-t-2 border-transparent` for layout consistency:
   ```tsx
   <div className="flex flex-col items-center gap-0.5 opacity-30 cursor-default border-t-2 border-transparent pt-1">
   ```

Keep the component as "use client" with usePathname. Do NOT add any transition/animation on the backdrop-filter — static blur only per requirements out-of-scope.
  </action>
  <verify>
    <automated>grep -n "bg-glass-nav\|backdrop-blur\|auto_stories\|forest\|face_5\|book_2\|icon-filled\|border-t-2\|border-ink\|border-transparent" src/components/reader/WurzelNavBar.tsx && grep -c "bg-aged-wood\|bg-parchment" src/components/reader/WurzelNavBar.tsx | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>WurzelNavBar renders with bg-glass-nav + backdrop-blur-md. Active tab has border-t-2 border-ink (no pill). Icons are auto_stories/forest/face_5/book_2. Active icon has icon-filled class. No bg-aged-wood or bg-parchment remains.</done>
</task>

</tasks>

<verification>
1. `grep -n "bg-glass-nav\|backdrop-blur" src/components/reader/WurzelNavBar.tsx` — confirms glassmorphism classes
2. `grep -n "bg-aged-wood" src/components/reader/WurzelNavBar.tsx` — should return nothing (pill removed)
3. `grep -n "auto_stories\|forest\|face_5\|book_2" src/components/reader/WurzelNavBar.tsx` — confirms all 4 Archivist icons
4. `grep -n "icon-filled" src/components/reader/WurzelNavBar.tsx src/app/globals.css` — confirms filled icon class usage and definition
5. `npx vitest run` — no test regressions
6. `npx next build` — build succeeds
</verification>

<success_criteria>
- WurzelNavBar uses bg-glass-nav + backdrop-blur-md (not bg-parchment)
- Active state uses border-t-2 border-ink (not bg-aged-wood pill)
- Nav icons are auto_stories, forest, face_5, book_2
- Active icon uses icon-filled class (FILL=1); inactive icons are outlined
- globals.css has .icon-filled utility class
- Build succeeds, no test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/34-shell-components/34-01-SUMMARY.md`
</output>
