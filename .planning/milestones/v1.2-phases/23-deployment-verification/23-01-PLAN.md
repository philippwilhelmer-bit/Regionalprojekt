---
phase: 23-deployment-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03]

must_haves:
  truths:
    - "regionalprojekt.vercel.app loads the homepage without error"
    - "/admin redirects to /admin/login when unauthenticated"
    - "Neon PostgreSQL is the active database with 13 Bezirke seeded"
    - "NEXT_PUBLIC_BASE_URL is set to https://regionalprojekt.vercel.app in Vercel"
  artifacts:
    - path: "Vercel Environment Variables"
      provides: "NEXT_PUBLIC_BASE_URL configuration"
      contains: "https://regionalprojekt.vercel.app"
  key_links:
    - from: "Vercel env vars"
      to: "Next.js build"
      via: "NEXT_PUBLIC_* inlined at build time"
      pattern: "NEXT_PUBLIC_BASE_URL"
---

<objective>
Set NEXT_PUBLIC_BASE_URL in Vercel, trigger a redeploy, and formally verify all four Phase 23 success criteria against the live deployment. This closes DEPLOY-01/02/03 requirements.

Purpose: Close the integration gap documented in Phase 21-02 (NEXT_PUBLIC_BASE_URL not yet set) and formally verify the Vercel+Neon deployment satisfies all deployment requirements.
Output: All 4 success criteria verified against live URL; DEPLOY-01/02/03 marked satisfied.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-deployment-verification/23-RESEARCH.md
@.planning/phases/21-railway-infrastructure/21-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Set NEXT_PUBLIC_BASE_URL in Vercel and trigger redeploy</name>
  <files>N/A — Vercel dashboard configuration only</files>
  <action>
The user must set NEXT_PUBLIC_BASE_URL in the Vercel dashboard and trigger a redeploy. This cannot be automated because it requires Vercel dashboard authentication.

Steps:
1. Open https://vercel.com/dashboard
2. Navigate to the `regionalprojekt` project
3. Go to Settings -> Environment Variables
4. Add new variable:
   - Name: `NEXT_PUBLIC_BASE_URL`
   - Value: `https://regionalprojekt.vercel.app` (NO trailing slash)
   - Scope: Production (and optionally Preview)
5. Go to Deployments tab
6. Find the latest production deployment -> click "..." menu -> "Redeploy"
7. Wait for deployment status to show green "Ready" badge

CRITICAL: NEXT_PUBLIC_* variables are inlined at build time. Setting the variable WITHOUT redeploying does nothing. The redeploy is mandatory.

WARNING: Do NOT add a trailing slash. `https://regionalprojekt.vercel.app/` will produce double-slashed URLs like `https://regionalprojekt.vercel.app//artikel/...`
  </action>
  <verify>
    <automated>MISSING -- requires Vercel dashboard access (human-only)</automated>
  </verify>
  <done>NEXT_PUBLIC_BASE_URL is set in Vercel environment variables AND a new deployment with "Ready" status is live on the production domain.</done>
  <resume-signal>Type "deployed" once the new deployment is live with Ready status</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all deployment success criteria</name>
  <files>N/A — verification only, no files modified</files>
  <action>
Verify all 4 Phase 23 success criteria against the live Vercel deployment. Run each check in order. All must pass for DEPLOY-01/02/03 to be marked satisfied.

Claude will run curl-based smoke tests where possible. The user verifies dashboard items and visual checks.
  </action>
  <what-built>Phase 21 deployed the app to Vercel+Neon. Phase 22 implemented test mode. Task 1 set the final missing env var and redeployed. Now we verify everything works.</what-built>
  <how-to-verify>
Run these checks in order. All must pass.

**Check 1 — Homepage loads (DEPLOY-01):**
Open https://regionalprojekt.vercel.app in a browser.
Expected: Homepage renders with articles and Bezirk selector showing 13 districts.
Or run: `curl -s -o /dev/null -w "%{http_code}" https://regionalprojekt.vercel.app` — expect `200`.

**Check 2 — /admin redirects to /admin/login (DEPLOY-01):**
Open an incognito/private browser window (no session cookie).
Navigate to https://regionalprojekt.vercel.app/admin
Expected: Redirects to https://regionalprojekt.vercel.app/admin/login
Or run: `curl -s -o /dev/null -w "%{url_effective}" -L https://regionalprojekt.vercel.app/admin` — expect URL ending in `/admin/login`.

**Check 3 — Neon PostgreSQL is active with current schema (DEPLOY-02):**
In Vercel dashboard: Settings -> Environment Variables -> confirm DATABASE_URL contains `neon.tech`.
On the live homepage: confirm 13 Bezirke appear in the region selector (proves DB is seeded and schema is current).

**Check 4 — NEXT_PUBLIC_BASE_URL is set (DEPLOY-03):**
In Vercel dashboard: Settings -> Environment Variables -> confirm NEXT_PUBLIC_BASE_URL = `https://regionalprojekt.vercel.app`.
Optional deeper check: View page source of the live homepage and search for `regionalprojekt.vercel.app` in OG meta tags to confirm the variable was inlined into the build.

**Check 5 — Test mode env var is active (DEPLOY-03):**
Verify TESTSEITE banner appears on the homepage (confirms NEXT_PUBLIC_IS_TEST_SITE=true is active).
Or run: `curl -s https://regionalprojekt.vercel.app/robots.txt` — expect `Disallow: /` (test mode blocks crawlers).
  </how-to-verify>
  <verify>
    <automated>MISSING -- requires live Vercel deployment and browser/dashboard access (human-only)</automated>
  </verify>
  <done>All 5 checks pass: homepage loads (200), /admin redirects to /admin/login, DATABASE_URL points to neon.tech with 13 Bezirke, NEXT_PUBLIC_BASE_URL is set, and test mode is active.</done>
  <resume-signal>Type "verified" with results of each check, or describe any failures</resume-signal>
</task>

</tasks>

<verification>
All Phase 23 success criteria are infrastructure/configuration tests verified against the live deployment:
1. regionalprojekt.vercel.app loads homepage without error
2. /admin redirects to /admin/login (unauthenticated)
3. Neon PostgreSQL is active with current Prisma schema
4. NEXT_PUBLIC_BASE_URL is set in Vercel environment variables

No code changes are made in this phase. The existing Vitest suite is unaffected.
</verification>

<success_criteria>
- NEXT_PUBLIC_BASE_URL set to https://regionalprojekt.vercel.app (no trailing slash) in Vercel
- New deployment with "Ready" status is live
- Homepage returns 200 with articles and 13 Bezirke
- /admin redirects to /admin/login in incognito
- DATABASE_URL points to neon.tech
- TESTSEITE banner visible (test mode active)
- DEPLOY-01, DEPLOY-02, DEPLOY-03 can be marked satisfied in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/23-deployment-verification/23-01-SUMMARY.md`
</output>
