---
phase: 24-admin-login-banner-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/login/page.tsx
  - src/app/admin/login/login-page.test.tsx
autonomous: true
requirements:
  - TEST-02
must_haves:
  truths:
    - "Visiting /admin/login shows the TESTSEITE banner when NEXT_PUBLIC_IS_TEST_SITE=true"
    - "Visiting /admin/login shows NO banner when NEXT_PUBLIC_IS_TEST_SITE is unset"
  artifacts:
    - path: "src/app/admin/login/page.tsx"
      provides: "Login page with TestSiteBanner import and render"
      contains: "TestSiteBanner"
    - path: "src/app/admin/login/login-page.test.tsx"
      provides: "Unit tests for banner presence/absence on login page"
      contains: "TESTSEITE"
  key_links:
    - from: "src/app/admin/login/page.tsx"
      to: "src/components/TestSiteBanner.tsx"
      via: "named import"
      pattern: "import.*TestSiteBanner.*from.*@/components/TestSiteBanner"
---

<objective>
Add the TESTSEITE banner to the /admin/login page, closing the last TEST-02 gap.

Purpose: The login page sits outside the (admin) route group layout and never receives the TestSiteBanner. This is the only admin page missing the banner.
Output: Updated login page with banner + unit test confirming behavior.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-admin-login-banner-fix/24-RESEARCH.md

@src/app/admin/login/page.tsx
@src/components/TestSiteBanner.tsx
@src/components/TestSiteBanner.test.tsx

<interfaces>
<!-- Existing component to import — no new interfaces needed -->

From src/components/TestSiteBanner.tsx:
```tsx
export function TestSiteBanner(): JSX.Element | null
// Returns yellow banner div with role="banner" when NEXT_PUBLIC_IS_TEST_SITE === 'true'
// Returns null otherwise
```

From src/app/admin/login/login-form.tsx:
```tsx
export function LoginForm(): JSX.Element
// Client Component — must be mocked in tests
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TestSiteBanner to login page with tests</name>
  <files>src/app/admin/login/login-page.test.tsx, src/app/admin/login/page.tsx</files>
  <behavior>
    - Test 1: When NEXT_PUBLIC_IS_TEST_SITE='true', LoginPage() returns a fragment containing an element with role="banner" and text "TESTSEITE"
    - Test 2: When NEXT_PUBLIC_IS_TEST_SITE is unset, LoginPage() returns content with NO element having role="banner"
    - Test 3: Login form content (h1 with site name, LoginForm component) still renders in both cases
  </behavior>
  <action>
RED phase:
1. Create `src/app/admin/login/login-page.test.tsx`
2. Mock `@/../bundesland.config` with `{ default: { siteName: 'Test Site' } }` (same pattern as `root-layout-adsense.test.ts`)
3. Mock `./login-form` with `vi.fn(() => ({ type: 'div', props: { 'data-testid': 'login-form' } }))` to avoid client component issues
4. Write tests that call `LoginPage()` directly (Server Component pattern) and check:
   - With env var set: result contains element with `role="banner"` containing "TESTSEITE"
   - Without env var: result does NOT contain banner element
   - In both cases: h1 text includes site name
5. Run tests — they MUST fail (page has no banner yet)

GREEN phase:
6. Edit `src/app/admin/login/page.tsx`:
   - Add import: `import { TestSiteBanner } from '@/components/TestSiteBanner'`
   - Wrap existing `<div>` in a React fragment `<>...</>`
   - Add `<TestSiteBanner />` as first child of the fragment (before the existing div)
   - Do NOT add `import React from 'react'` — fragments work without it in Next.js Server Components
7. Run tests — they MUST pass

IMPORTANT: Use `@/components/TestSiteBanner` import path (not relative). Use named export `{ TestSiteBanner }` (not default). Follow the exact pattern from `src/app/(admin)/layout.tsx`.
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && npx vitest run src/app/admin/login/login-page.test.tsx</automated>
  </verify>
  <done>
    - Login page test file exists with 2+ passing tests
    - LoginPage renders TestSiteBanner as first element when test mode active
    - LoginPage renders no banner when test mode inactive
    - All existing tests still pass (`npx vitest run`)
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run` — full test suite green (no regressions)
2. `grep -n "TestSiteBanner" src/app/admin/login/page.tsx` — import and usage present
3. `grep -n "role.*banner" src/app/admin/login/login-page.test.tsx` — test checks for banner element
</verification>

<success_criteria>
- /admin/login page renders TESTSEITE banner when NEXT_PUBLIC_IS_TEST_SITE=true
- /admin/login page renders NO banner when env var is unset
- Full test suite passes with no regressions
- TEST-02 requirement fully satisfied across all admin pages
</success_criteria>

<output>
After completion, create `.planning/phases/24-admin-login-banner-fix/24-01-SUMMARY.md`
</output>
