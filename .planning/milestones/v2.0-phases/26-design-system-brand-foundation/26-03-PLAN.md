---
phase: 26-design-system-brand-foundation
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - bundesland.config.ts
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/app/rss/[slug]/route.ts
  - src/app/(public)/artikel/[publicId]/[slug]/page.tsx
  - src/lib/reader/rss.ts
  - src/lib/reader/sitemap.test.ts
  - public/images/wurzelmann.png
autonomous: false
requirements: [BRAND-01, BRAND-02]

user_setup:
  - service: mascot-asset
    why: "Wurzelmann mascot PNG image needed at public/images/wurzelmann.png"
    env_vars: []
    dashboard_config:
      - task: "Place wurzelmann.png in public/images/ before execution"
        location: "Local filesystem"

must_haves:
  truths:
    - "No instance of 'Ennstal Aktuell' or 'ennstal-aktuell' appears anywhere in the codebase"
    - "The site name displays as 'Wurzelwelt' in the browser tab, RSS feeds, sitemap, and Impressum"
    - "The Wurzelmann mascot image is served from /images/wurzelmann.png"
  artifacts:
    - path: "bundesland.config.ts"
      provides: "Wurzelwelt brand identity (siteName, publisherName, email)"
      contains: "Wurzelwelt"
    - path: "public/images/wurzelmann.png"
      provides: "Wurzelmann mascot asset"
  key_links:
    - from: "src/app/layout.tsx"
      to: "bundesland.config.ts"
      via: "config.siteName import for metadata title"
      pattern: "config.siteName"
    - from: "src/app/rss/[slug]/route.ts"
      to: "bundesland.config.ts"
      via: "config import for RSS feed title"
      pattern: "config"
---

<objective>
Rename all "Ennstal Aktuell" references to "Wurzelwelt" across the entire codebase and add the Wurzelmann mascot asset.

Purpose: The brand identity must be consistent everywhere — config, SEO metadata, RSS feeds, structured data, Impressum, and fallback URLs. The mascot asset must be available for Phase 27+ components.
Output: Zero "Ennstal Aktuell" references remain. Wurzelmann PNG served at /images/wurzelmann.png.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-design-system-brand-foundation/26-CONTEXT.md

<interfaces>
<!-- Files containing "Ennstal Aktuell" or "ennstal-aktuell" references -->

From bundesland.config.ts:
```typescript
siteName: 'Steirer-News',
publisherName: 'Ennstal Aktuell Medien GmbH',
email: 'redaktion@ennstal-aktuell.at',
```

From src/app/sitemap.ts:
```typescript
const BASE_URL = process.env.NEXT_PUBLIC_BASE_URL ?? 'https://ennstal-aktuell.at'
```

From src/app/robots.ts:
```typescript
const BASE_URL = process.env.NEXT_PUBLIC_BASE_URL ?? 'https://ennstal-aktuell.at'
```

From src/app/rss/[slug]/route.ts:
```typescript
const BASE_URL = process.env.NEXT_PUBLIC_BASE_URL ?? 'https://ennstal-aktuell.at'
```

From src/app/(public)/artikel/[publicId]/[slug]/page.tsx:
```typescript
process.env.NEXT_PUBLIC_BASE_URL ?? "https://www.ennstal-aktuell.at";
```

From src/lib/reader/rss.ts:
```typescript
* @param baseUrl - Base URL of the site (e.g. 'https://ennstal-aktuell.at')
```

From src/lib/reader/sitemap.test.ts:
```typescript
const base = process.env.NEXT_PUBLIC_BASE_URL ?? 'https://ennstal-aktuell.at'
// (appears 4 times)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename all brand references from Ennstal Aktuell to Wurzelwelt</name>
  <files>
    bundesland.config.ts
    src/app/sitemap.ts
    src/app/robots.ts
    src/app/rss/[slug]/route.ts
    src/app/(public)/artikel/[publicId]/[slug]/page.tsx
    src/lib/reader/rss.ts
    src/lib/reader/sitemap.test.ts
  </files>
  <action>
**bundesland.config.ts:**
- `siteName: 'Steirer-News'` → `siteName: 'Wurzelwelt'`
- Add `tagline: 'Nachrichten aus der Steiermark'` (keep existing tagline field if present, update value)
- `publisherName: 'Ennstal Aktuell Medien GmbH'` → `publisherName: 'Wurzelwelt Medien GmbH'`
- `email: 'redaktion@ennstal-aktuell.at'` → `email: 'redaktion@wurzelwelt.at'`

**src/app/sitemap.ts:**
- `'https://ennstal-aktuell.at'` → `'https://wurzelwelt.at'`

**src/app/robots.ts:**
- `'https://ennstal-aktuell.at'` → `'https://wurzelwelt.at'`

**src/app/rss/[slug]/route.ts:**
- `'https://ennstal-aktuell.at'` → `'https://wurzelwelt.at'`

**src/app/(public)/artikel/[publicId]/[slug]/page.tsx:**
- `"https://www.ennstal-aktuell.at"` → `"https://www.wurzelwelt.at"`

**src/lib/reader/rss.ts:**
- JSDoc comment: `'https://ennstal-aktuell.at'` → `'https://wurzelwelt.at'`

**src/lib/reader/sitemap.test.ts:**
- All 4 occurrences: `'https://ennstal-aktuell.at'` → `'https://wurzelwelt.at'`

**Also update metadata in layout.tsx** (if not already done in Plan 01):
- Description should read "Wurzelwelt — Nachrichten aus der Steiermark"

After all changes, do a final grep for any remaining "ennstal" references in the entire src/ and root config files. Fix any that are found.
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && REMAINING=$(grep -ri "ennstal" src/ bundesland.config.ts --include="*.ts" --include="*.tsx" -c 2>/dev/null | awk -F: '{s+=$2}END{print s+0}') && echo "Remaining ennstal refs: $REMAINING" && if [ "$REMAINING" -eq 0 ]; then echo "PASS"; else grep -ri "ennstal" src/ bundesland.config.ts --include="*.ts" --include="*.tsx"; echo "FAIL"; fi</automated>
  </verify>
  <done>Zero occurrences of "ennstal" (case-insensitive) remain in any .ts or .tsx file. bundesland.config.ts shows "Wurzelwelt" as siteName and "Wurzelwelt Medien GmbH" as publisher.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Wurzelmann mascot asset placement</name>
  <files>public/images/wurzelmann.png</files>
  <action>
Verify the user has placed the Wurzelmann mascot PNG at public/images/wurzelmann.png. Claude cannot generate images — this file must be provided by the user. If the public/images/ directory does not exist, create it with `mkdir -p public/images`.
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && ls -la public/images/wurzelmann.png 2>/dev/null && echo "PASS" || echo "FILE NOT FOUND — user must place wurzelmann.png"</automated>
  </verify>
  <done>Wurzelmann PNG exists at public/images/wurzelmann.png and is accessible, OR user has acknowledged deferral to before Phase 27.</done>
</task>

</tasks>

<verification>
- `grep -ri "ennstal" src/ bundesland.config.ts` returns nothing
- `grep "Wurzelwelt" bundesland.config.ts` shows siteName and publisherName
- `grep "wurzelwelt.at" src/app/sitemap.ts src/app/robots.ts src/app/rss/*/route.ts` shows updated fallback URLs
- Sitemap test passes: `npx vitest run src/lib/reader/sitemap.test.ts`
</verification>

<success_criteria>
1. Zero instances of "Ennstal Aktuell" or "ennstal-aktuell" in the codebase
2. bundesland.config.ts contains "Wurzelwelt" as siteName and "Wurzelwelt Medien GmbH" as publisherName
3. All fallback BASE_URL values point to wurzelwelt.at
4. Sitemap tests pass with updated URLs
5. Wurzelmann PNG exists at public/images/wurzelmann.png (or user acknowledged deferral to pre-Phase-27)
</success_criteria>

<output>
After completion, create `.planning/phases/26-design-system-brand-foundation/26-03-SUMMARY.md`
</output>
