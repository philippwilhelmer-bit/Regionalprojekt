---
phase: 28-homepage-components
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/reader/MascotGreeting.tsx
  - src/components/reader/RegionalEditorialCard.tsx
  - src/components/reader/HeroArticle.tsx
autonomous: true
requirements: [COMP-02, COMP-03, COMP-04]

must_haves:
  truths:
    - "A speech-bubble card with Wurzelmann sagt and a time-aware greeting renders on the homepage"
    - "The Topmeldung hero uses Wurzelwelt spacing, rounded corners, and Newsreader headline"
    - "A new RegionalEditorialCard displays full-width aspect-video image with Newsreader headline and uppercase Jakarta Sans category label"
  artifacts:
    - path: "src/components/reader/MascotGreeting.tsx"
      provides: "Speech-bubble card with Wurzelmann avatar, time-aware greeting, hardcoded quote"
      exports: ["MascotGreeting"]
    - path: "src/components/reader/RegionalEditorialCard.tsx"
      provides: "Editorial card with full-width aspect-video image, serif headline, uppercase label"
      exports: ["RegionalEditorialCard"]
    - path: "src/components/reader/HeroArticle.tsx"
      provides: "Restyled Topmeldung with Wurzelwelt tokens"
  key_links:
    - from: "src/components/reader/MascotGreeting.tsx"
      to: "public/images/wurzelmann.png"
      via: "next/image import"
      pattern: "wurzelmann\\.png"
    - from: "src/components/reader/RegionalEditorialCard.tsx"
      to: "@/lib/content/articles"
      via: "ArticleWithBezirke type import"
      pattern: "ArticleWithBezirke"
---

<objective>
Create the three new/restyled homepage components for the Wurzelwelt editorial experience: MascotGreeting speech-bubble card, restyled Topmeldung hero, and RegionalEditorialCard.

Purpose: These components are the building blocks for the Wurzelwelt homepage layout — they must exist before the homepage can be rewired in Plan 02.
Output: Three component files ready to be imported by HomepageLayout.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-homepage-components/28-CONTEXT.md
@.planning/phases/27-app-chrome/27-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/content/articles.ts (type used by all card components):
```typescript
export type ArticleWithBezirke = Article & {
  bezirke: Array<{ bezirk: Bezirk }>;
};
```

From src/components/reader/ArticleCard.tsx (reuse this helper):
```typescript
export function formatRelativeTime(date: Date): string;
```

From src/lib/reader/slug.ts:
```typescript
export function slugify(text: string): string;
```

Design tokens from globals.css:
```css
--color-background: #FCF9EF;
--color-surface: #F6F4EA;
--color-surface-elevated: #FFFFFF;
--radius-sm: 0.75rem;
--spacing-gutter: 1.7rem;
--spacing-section: 4rem;
```

Existing HeroArticle.tsx structure (restyle in place — see src/components/reader/HeroArticle.tsx).
Wurzelmann mascot PNG at public/images/wurzelmann.png (transparent background).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MascotGreeting and RegionalEditorialCard components</name>
  <files>src/components/reader/MascotGreeting.tsx, src/components/reader/RegionalEditorialCard.tsx</files>
  <action>
**MascotGreeting.tsx** — Client component ("use client" for time-of-day logic):
- Speech-bubble card on `bg-surface` (#F6F4EA) background with rounded corners (rounded-sm = 0.75rem)
- "Wurzelmann sagt:" label in font-label (Jakarta Sans) uppercase, text-xs, font-semibold, text-primary
- Time-aware greeting: hour < 11 = "Guten Morgen", hour < 17 = "Guten Tag", else "Guten Abend"
- One hardcoded quote per slot: morning = "Frische Nachrichten aus deiner Region!", afternoon = "Was gibt's Neues in deinem Bezirk?", evening = "Der Tag in deiner Region auf einen Blick."
- Greeting + quote displayed inside the speech bubble (white bg-surface-elevated rounded-sm shadow-sm)
- Triangular speech-bubble tail: CSS triangle using border trick or clip-path, pointing down-left toward the Wurzelmann avatar
- Wurzelmann avatar: next/image, src="/images/wurzelmann.png", 56px wide, placed below and slightly left of the bubble, transparent background (no circular crop per user decision)
- Overall padding: px-[var(--spacing-gutter)]
- No props needed — self-contained component

**RegionalEditorialCard.tsx** — Server-compatible component:
- Props: `{ article: ArticleWithBezirke }`
- Full-width `aspect-video` image container with rounded-sm overflow-hidden
- Image uses `img-matte` class for consistent photo styling, object-cover
- Gradient fallback when no imageUrl (reuse primary-to-secondary gradient)
- Below image: Newsreader serif headline (font-headline text-lg font-semibold) with leading-snug
- Uppercase Jakarta Sans category label (font-label uppercase text-xs tracking-wider text-primary font-semibold) showing first bezirk name
- Relative timestamp using `formatRelativeTime` imported from ArticleCard.tsx
- Entire card is a Link to article detail page using publicId + slugify pattern
- Spacing: p-0 on image, p-3 on text body area

Do NOT add bezirk gradient color maps to RegionalEditorialCard — keep it clean with single primary text color for labels.
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && export PATH="$HOME/.nvm/versions/node/v25.8.0/bin:$PATH" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>MascotGreeting renders speech bubble with time-aware greeting + Wurzelmann avatar. RegionalEditorialCard renders full-width image card with Newsreader headline and uppercase label. Both compile without TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle HeroArticle as Wurzelwelt Topmeldung</name>
  <files>src/components/reader/HeroArticle.tsx</files>
  <action>
Restyle HeroArticle.tsx in place — same structure, Wurzelwelt visual treatment:

1. **Rounded corners**: Add `rounded-sm` (0.75rem) to the outer Link container, keep `overflow-hidden`
2. **Topmeldung badge**: Change from `rounded-sm bg-primary` pill to accent gradient pill — `bg-gradient-to-br from-primary to-primary-container rounded-full px-3 py-1` (CTA gradient pill pattern from Phase 26)
3. **Headline**: Ensure `font-headline` (Newsreader) is used — already correct, keep text-2xl md:text-3xl
4. **Bezirk badge**: Update to `rounded-full` to match Wurzelwelt rounded style, keep uppercase Jakarta Sans
5. **Spacing**: Update bottom padding to use `p-[var(--spacing-gutter)] pb-8` for organic Wurzelwelt spacing
6. **Gradient overlay**: Keep existing dark gradient (from-black/80 via-black/30 to-transparent) — this ensures text legibility per COMP-03 requirement
7. **Min-height**: Keep `min-h-[60vh]` — responsive hero height

Do NOT change the image handling, excerpt logic, or Link structure — only visual tokens.
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && export PATH="$HOME/.nvm/versions/node/v25.8.0/bin:$PATH" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>HeroArticle uses Wurzelwelt rounded corners, gradient pill badge, and organic spacing. Dark gradient overlay ensures headline legibility. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- MascotGreeting.tsx exports MascotGreeting component
- RegionalEditorialCard.tsx exports RegionalEditorialCard component
- HeroArticle.tsx uses rounded-sm, gradient pill badge, organic spacing
</verification>

<success_criteria>
All three components exist, compile, and are ready for import into HomepageLayout in Plan 02. MascotGreeting shows time-aware greeting in speech bubble with Wurzelmann avatar. RegionalEditorialCard shows full-width aspect-video image with serif headline. HeroArticle has Wurzelwelt visual treatment.
</success_criteria>

<output>
After completion, create `.planning/phases/28-homepage-components/28-01-SUMMARY.md`
</output>
