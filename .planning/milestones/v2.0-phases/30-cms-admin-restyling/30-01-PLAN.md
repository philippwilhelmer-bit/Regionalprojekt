---
phase: 30-cms-admin-restyling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/login/page.tsx
  - src/app/admin/login/login-form.tsx
  - src/app/(admin)/layout.tsx
  - src/components/admin/LogoutButton.tsx
  - src/components/admin/UnsplashPicker.tsx
  - src/components/admin/UnsplashPickerNew.tsx
autonomous: true
requirements: [CMS-01]

must_haves:
  truths:
    - "Admin login page displays Wurzelwelt palette (no gray-50/gray-100 backgrounds, no blue-600 buttons)"
    - "Admin sidebar uses Wurzelwelt primary colors with tonal hover states and rounded-sm nav links"
    - "All admin CTA buttons use gradient pill style (from-primary to-primary-container rounded-full)"
    - "Admin headings render in font-headline (Newsreader), body/labels in font-body (Plus Jakarta Sans)"
  artifacts:
    - path: "src/app/admin/login/page.tsx"
      provides: "Restyled login page with Wurzelwelt tokens"
      contains: "bg-background"
    - path: "src/app/(admin)/layout.tsx"
      provides: "Restyled admin sidebar with Wurzelwelt tokens"
      contains: "bg-surface"
    - path: "src/components/admin/LogoutButton.tsx"
      provides: "Restyled logout button with semantic tokens"
      contains: "text-text"
  key_links:
    - from: "src/app/admin/login/login-form.tsx"
      to: "globals.css"
      via: "Tailwind semantic tokens"
      pattern: "bg-gradient-to-br|from-primary|rounded-full"
    - from: "src/app/(admin)/layout.tsx"
      to: "globals.css"
      via: "Tailwind semantic tokens"
      pattern: "bg-surface|text-text|rounded-sm"
---

<objective>
Restyle the CMS admin shell (login page, sidebar layout, shared components) with Wurzelwelt brand tokens.

Purpose: The admin login and navigation frame are the first things an editor sees — they must reflect the Wurzelwelt identity, not legacy gray/blue defaults.
Output: Restyled login page, admin sidebar layout, logout button, and Unsplash picker components using Wurzelwelt semantic tokens.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css

<interfaces>
<!-- Wurzelwelt design tokens from globals.css -->
Color tokens: primary (#1B2D18), secondary (#4A5D23), accent (#9F411E), background (#FCF9EF), text (#071806), surface (#F6F4EA), surface-elevated (#FFFFFF), primary-container (#4A5D23)
Font tokens: font-headline (Newsreader), font-body (Plus Jakarta Sans), font-label (Plus Jakarta Sans)
Radius: radius-sm (0.75rem) via rounded-sm
CTA pattern: bg-gradient-to-br from-primary to-primary-container rounded-full text-white
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle login page and admin layout shell</name>
  <files>src/app/admin/login/page.tsx, src/app/admin/login/login-form.tsx, src/app/(admin)/layout.tsx, src/components/admin/LogoutButton.tsx</files>
  <action>
Apply Wurzelwelt token replacements across the admin shell files. The mapping is:

**Login page (page.tsx):**
- `bg-gray-50` -> `bg-background` (warm cream canvas)
- `bg-white` -> `bg-surface-elevated` (card background)
- `rounded-lg` -> `rounded-sm` (0.75rem radius)
- `text-gray-900` on h1 -> `text-text font-headline` (Newsreader for heading)
- Keep shadow-md as-is (elevation is fine)

**Login form (login-form.tsx):**
- Error box: `text-red-600 bg-red-50 border border-red-200 rounded` -> `text-accent bg-accent/10 rounded-sm` (no border, tonal background)
- Label: `text-gray-700` -> `text-text/70`
- Input: `border border-gray-300 rounded` -> `border border-surface rounded-sm` and `focus:ring-blue-500` -> `focus:ring-primary`
- Submit button: `bg-blue-600 text-white py-2 rounded hover:bg-blue-700` -> `bg-gradient-to-br from-primary to-primary-container text-white py-2 rounded-full hover:opacity-90 font-medium`

**Admin layout (layout.tsx):**
- Outer div: `bg-gray-100` -> `bg-background`
- Sidebar: `bg-white border-r border-gray-200` -> `bg-surface-elevated` (no border — tonal separation)
- Sidebar label "Regionencompass": change text to "Wurzelwelt" and keep `text-xs font-semibold uppercase tracking-wide`, change `text-gray-600` -> `text-secondary`
- Nav links: `rounded text-sm text-gray-700 hover:bg-gray-100 hover:text-gray-900` -> `rounded-sm text-sm text-text/70 hover:bg-surface hover:text-text`

**LogoutButton:**
- `text-gray-500 hover:bg-gray-100 hover:text-gray-700` -> `text-text/50 hover:bg-surface hover:text-text`
- `rounded` -> `rounded-sm`
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && grep -rn "bg-gray-\|bg-blue-\|border-gray-\|text-gray-\|focus:ring-blue" src/app/admin/login/ src/app/\(admin\)/layout.tsx src/components/admin/LogoutButton.tsx | grep -v node_modules | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>Login page uses bg-background canvas with gradient CTA button. Admin sidebar uses bg-surface-elevated with tonal nav links. Zero legacy gray/blue classes remain in these 4 files.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Unsplash picker components</name>
  <files>src/components/admin/UnsplashPicker.tsx, src/components/admin/UnsplashPickerNew.tsx</files>
  <action>
Apply Wurzelwelt token replacements to both Unsplash picker components. Same mapping:

**Both UnsplashPicker.tsx and UnsplashPickerNew.tsx:**
- Labels: `text-gray-700` -> `text-text/70`, `text-gray-600` -> `text-text/60`
- Inputs: `border border-gray-300 rounded` -> `border border-surface rounded-sm`, `focus:ring-blue-500` -> `focus:ring-primary`
- Search button (primary CTA): `bg-blue-600 text-white ... rounded hover:bg-blue-700` -> `bg-gradient-to-br from-primary to-primary-container text-white rounded-full hover:opacity-90`
- Auto button (secondary): `border border-gray-300 text-gray-700 rounded hover:bg-gray-50` -> `border border-surface text-text/70 rounded-sm hover:bg-surface`
- Remove button: keep `bg-red-600 text-white` (destructive red is intentional), but `rounded` -> `rounded-sm`
- Image borders: `border border-gray-200` -> `border border-surface`
- Hover border on image select: `hover:border-blue-500` -> `hover:border-primary`
- Credits text: `text-gray-400` -> `text-text/40`, `text-gray-500` -> `text-text/50`
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && grep -rn "bg-gray-\|bg-blue-\|border-gray-\|text-gray-\|focus:ring-blue\|hover:border-blue" src/components/admin/UnsplashPicker.tsx src/components/admin/UnsplashPickerNew.tsx | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>Both Unsplash pickers use Wurzelwelt tokens for all interactive elements. Zero legacy gray/blue classes remain.</done>
</task>

</tasks>

<verification>
Run grep across all files in this plan to confirm zero legacy gray/blue Tailwind classes remain:
```bash
grep -rn "bg-gray-\|bg-blue-\|border-gray-\|text-gray-\|focus:ring-blue" src/app/admin/login/ src/app/\(admin\)/layout.tsx src/components/admin/LogoutButton.tsx src/components/admin/UnsplashPicker.tsx src/components/admin/UnsplashPickerNew.tsx
```
Expected: no output (zero matches).
</verification>

<success_criteria>
- Login page displays warm cream background with gradient CTA button
- Admin sidebar has tonal surface-elevated background with no border separator
- Sidebar heading reads "Wurzelwelt" not "Regionencompass"
- All inputs have rounded-sm corners and focus:ring-primary
- Unsplash picker buttons use gradient pill or tonal secondary style
- Zero bg-gray-*, bg-blue-*, border-gray-*, text-gray-*, focus:ring-blue-* classes in any modified file
</success_criteria>

<output>
After completion, create `.planning/phases/30-cms-admin-restyling/30-01-SUMMARY.md`
</output>
