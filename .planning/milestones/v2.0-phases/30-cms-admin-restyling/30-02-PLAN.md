---
phase: 30-cms-admin-restyling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(admin)/admin/articles/page.tsx
  - src/app/(admin)/admin/articles/ArticleRow.tsx
  - src/app/(admin)/admin/articles/ArticleFilters.tsx
  - src/app/(admin)/admin/articles/new/page.tsx
  - src/app/(admin)/admin/articles/[id]/edit/page.tsx
  - src/app/(admin)/admin/exceptions/page.tsx
  - src/app/(admin)/admin/exceptions/ExceptionCard.tsx
  - src/app/(admin)/admin/sources/page.tsx
  - src/app/(admin)/admin/sources/SourceCard.tsx
  - src/app/(admin)/admin/sources/SourceFormFields.tsx
  - src/app/(admin)/admin/sources/new/page.tsx
  - src/app/(admin)/admin/ai-config/page.tsx
  - src/app/(admin)/admin/ai-config/GlobalAiConfigForm.tsx
  - src/app/(admin)/admin/ai-config/SourceOverrideForm.tsx
autonomous: true
requirements: [CMS-01]

must_haves:
  truths:
    - "Article list page displays Wurzelwelt palette with no legacy gray/blue colors"
    - "All admin form inputs have rounded-sm corners and focus:ring-primary"
    - "All primary action buttons (Neuer Artikel, Speichern, etc.) use gradient pill CTA style"
    - "Admin page headings use font-headline (Newsreader)"
    - "Card/panel borders replaced with tonal bg-surface backgrounds or border-surface tokens"
    - "Exception, Source, and AI Config pages all use Wurzelwelt tokens consistently"
  artifacts:
    - path: "src/app/(admin)/admin/articles/page.tsx"
      provides: "Restyled articles list with Wurzelwelt tokens"
      contains: "font-headline"
    - path: "src/app/(admin)/admin/exceptions/ExceptionCard.tsx"
      provides: "Restyled exception cards with tonal separation"
      contains: "bg-surface"
    - path: "src/app/(admin)/admin/ai-config/page.tsx"
      provides: "Restyled AI config with Wurzelwelt tokens"
      contains: "font-headline"
  key_links:
    - from: "src/app/(admin)/admin/articles/page.tsx"
      to: "globals.css"
      via: "Tailwind semantic tokens"
      pattern: "bg-gradient-to-br|from-primary|rounded-full"
    - from: "src/app/(admin)/admin/sources/SourceCard.tsx"
      to: "globals.css"
      via: "Tailwind semantic tokens"
      pattern: "bg-surface|border-surface|rounded-sm"
---

<objective>
Restyle all CMS admin content pages (Articles, Exceptions, Sources, AI Config) with Wurzelwelt brand tokens.

Purpose: Every admin page an editor interacts with must use the Wurzelwelt color palette, typography, and corner radius — eliminating all legacy gray/blue defaults.
Output: 14 restyled CMS page and component files using Wurzelwelt semantic tokens consistently.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css

<interfaces>
<!-- Wurzelwelt design tokens from globals.css -->
Color tokens: primary (#1B2D18), secondary (#4A5D23), accent (#9F411E), background (#FCF9EF), text (#071806), surface (#F6F4EA), surface-elevated (#FFFFFF), primary-container (#4A5D23)
Font tokens: font-headline (Newsreader), font-body (Plus Jakarta Sans), font-label (Plus Jakarta Sans)
Radius: radius-sm (0.75rem) via rounded-sm
CTA pattern: bg-gradient-to-br from-primary to-primary-container rounded-full text-white font-medium
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Articles pages (list, row, filters, new, edit)</name>
  <files>src/app/(admin)/admin/articles/page.tsx, src/app/(admin)/admin/articles/ArticleRow.tsx, src/app/(admin)/admin/articles/ArticleFilters.tsx, src/app/(admin)/admin/articles/new/page.tsx, src/app/(admin)/admin/articles/[id]/edit/page.tsx</files>
  <action>
Apply the Wurzelwelt token mapping consistently across all 5 Articles files. Use these replacements:

**Universal mapping (apply to ALL files):**
- Page headings (`text-2xl font-bold text-gray-900`): -> `text-2xl font-bold text-text font-headline`
- Section headings (`text-lg font-semibold text-gray-800`): -> `text-lg font-semibold text-text font-headline`
- Body text: `text-gray-900` -> `text-text`, `text-gray-800` -> `text-text`, `text-gray-700` -> `text-text/70`, `text-gray-600` -> `text-text/60`, `text-gray-500` -> `text-text/50`
- Card panels: `bg-white rounded-lg border border-gray-200` -> `bg-surface-elevated rounded-sm`
- Table header: `bg-gray-50 border-b border-gray-200` -> `bg-surface`
- Table header text: `text-xs font-semibold text-gray-700 uppercase` -> `text-xs font-semibold text-text/70 uppercase font-label`
- Inputs: `border border-gray-300 rounded` -> `border border-surface rounded-sm`, `focus:ring-blue-500` -> `focus:ring-primary`
- Primary CTA buttons (`bg-blue-600 text-white ... rounded hover:bg-blue-700`): -> `bg-gradient-to-br from-primary to-primary-container text-white rounded-full hover:opacity-90`
- Secondary/cancel buttons (`border border-gray-300 text-gray-700 rounded hover:bg-gray-50`): -> `border border-surface text-text/70 rounded-sm hover:bg-surface`
- Pagination buttons: same as secondary buttons
- Links: `text-blue-600 hover:underline` -> `text-secondary hover:underline`, `text-blue-700` -> `text-secondary`

**ArticleRow.tsx specific:**
- Row hover: `border-b border-gray-100 hover:bg-gray-50` -> `border-b border-surface hover:bg-surface`
- Status badges: keep semantic colors (green-100/green-800 for PUBLISHED, yellow for REVIEW, etc.) — these are status indicators, not brand colors
- Pin/Feature/Delete action buttons: keep their semantic colors (orange for pin, purple for feature, red for delete) but change `rounded` -> `rounded-sm` and `border-gray-300` -> `border-surface`, `bg-white` -> `bg-surface-elevated`
- Article link: `text-blue-700` -> `text-secondary`
- Checkbox: `text-blue-600 focus:ring-blue-500` -> `text-primary focus:ring-primary`

**ArticleFilters.tsx specific:**
- All selects and date inputs follow the universal input mapping above
- Reset button follows secondary button mapping

**New article page + Edit article page:**
- All form fields follow universal mapping
- Bezirk checkboxes: `text-blue-600 focus:ring-blue-500` -> `text-primary focus:ring-primary`
- Pin checkbox: keep `text-orange-500 focus:ring-orange-500` (semantic)
- Feature checkbox: keep `text-purple-500 focus:ring-purple-500` (semantic)
- Edit page preview box: `bg-gray-50 rounded-lg border border-gray-200` -> `bg-surface rounded-sm`
- Bezirk tags in preview: `bg-green-100 text-green-700` -> keep (semantic status)
- "Artikel ansehen" link: `text-blue-600 hover:text-blue-800` -> `text-secondary hover:text-secondary/80`
- Back links: `text-gray-700 hover:text-gray-900` -> `text-text/70 hover:text-text`
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && grep -rn "bg-gray-\|bg-blue-\|border-gray-\|text-gray-\|focus:ring-blue\|hover:bg-blue\|hover:text-blue\|hover:bg-gray" src/app/\(admin\)/admin/articles/ | grep -v "node_modules" | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>All 5 Articles files use Wurzelwelt tokens. Page headings use font-headline. CTA buttons use gradient pill. Inputs have rounded-sm and focus:ring-primary. Zero legacy gray/blue classes remain (except semantic status colors which use green/yellow/red/orange/purple intentionally).</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Exceptions, Sources, and AI Config pages</name>
  <files>src/app/(admin)/admin/exceptions/page.tsx, src/app/(admin)/admin/exceptions/ExceptionCard.tsx, src/app/(admin)/admin/sources/page.tsx, src/app/(admin)/admin/sources/SourceCard.tsx, src/app/(admin)/admin/sources/SourceFormFields.tsx, src/app/(admin)/admin/sources/new/page.tsx, src/app/(admin)/admin/ai-config/page.tsx, src/app/(admin)/admin/ai-config/GlobalAiConfigForm.tsx, src/app/(admin)/admin/ai-config/SourceOverrideForm.tsx</files>
  <action>
Apply the same Wurzelwelt token mapping to all 9 remaining CMS page files. Same universal mapping as Task 1:

**Universal (same as Task 1 — apply everywhere):**
- Headings: `text-gray-900` -> `text-text font-headline`, `text-gray-800` -> `text-text font-headline`
- Body/labels: `text-gray-700` -> `text-text/70`, `text-gray-600` -> `text-text/60`, `text-gray-500` -> `text-text/50`
- Cards: `bg-white rounded-lg border border-gray-200` -> `bg-surface-elevated rounded-sm`
- Inputs: `border border-gray-300 rounded` -> `border border-surface rounded-sm`, `focus:ring-blue-500` -> `focus:ring-primary`
- Primary buttons: `bg-blue-600 ... rounded hover:bg-blue-700` -> `bg-gradient-to-br from-primary to-primary-container text-white rounded-full hover:opacity-90`
- Secondary buttons/links: `border-gray-300` -> `border-surface`, `text-gray-700` -> `text-text/70`, `hover:bg-gray-50` -> `hover:bg-surface`

**ExceptionCard.tsx specific:**
- Card: `bg-white rounded-lg border border-gray-200` -> `bg-surface-elevated rounded-sm`
- Header divider: `border-b border-gray-100` -> `border-b border-surface`
- Approve button: `bg-green-600 ... rounded hover:bg-green-700` -> keep green (semantic approve action) but `rounded` -> `rounded-sm`
- Reject button: `bg-red-600 ... rounded hover:bg-red-700` -> keep red (semantic reject action) but `rounded` -> `rounded-sm`
- Side-by-side divider: `divide-x divide-gray-100` -> `divide-x divide-surface`
- Raw data panel: `bg-gray-50` -> `bg-surface`
- Source badge: keep `bg-yellow-100 text-yellow-800` (semantic)

**SourceCard.tsx specific:**
- Card: same as universal
- Header: `bg-gray-50 border-b border-gray-200` -> `bg-surface`
- Details border: `border-t border-gray-50` -> `border-t border-surface`
- Status badges: keep semantic colors (green/yellow/red for health, blue for type, purple for category, amber for keywords)
- Enable/disable toggle links: keep `text-red-600`/`text-green-600` (semantic)
- "Einstellungen bearbeiten" summary: `text-gray-700 hover:text-gray-700` -> `text-text/70 hover:text-text`

**SourceFormFields.tsx specific:**
- Labels and inputs follow universal mapping
- Help text: `text-gray-500` -> `text-text/50`

**Sources new page:**
- Same as universal mapping for all elements
- Back link: `text-gray-700 hover:text-gray-700` -> `text-text/70 hover:text-text`

**AI Config page + GlobalAiConfigForm + SourceOverrideForm:**
- All follow universal mapping
- Section cards: `bg-white rounded-lg border border-gray-200` -> `bg-surface-elevated rounded-sm`
- SourceOverrideForm header: `bg-gray-50 border-b border-gray-200` -> `bg-surface`
- Override active/inactive badges: keep `bg-orange-100 text-orange-700` and `bg-gray-100 text-gray-700` -> change inactive to `bg-surface text-text/70`
- Summary link: `text-blue-600 hover:text-blue-800` -> `text-secondary hover:text-secondary/80`
- Delete override link: keep `text-red-600 hover:text-red-800` (semantic destructive)
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && grep -rn "bg-gray-\|bg-blue-\|border-gray-\|text-gray-\|focus:ring-blue\|hover:bg-blue\|hover:text-blue\|hover:bg-gray" src/app/\(admin\)/admin/exceptions/ src/app/\(admin\)/admin/sources/ src/app/\(admin\)/admin/ai-config/ | grep -v "node_modules" | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>All 9 remaining CMS page files use Wurzelwelt tokens. Exceptions, Sources, and AI Config pages display the brand palette. Semantic status colors (green approve, red reject, health indicators) are preserved intentionally. Zero legacy gray/blue classes remain.</done>
</task>

</tasks>

<verification>
Run grep across ALL admin files to confirm zero legacy gray/blue Tailwind classes remain:
```bash
grep -rn "bg-gray-\|bg-blue-\|border-gray-\|text-gray-\|focus:ring-blue\|hover:bg-gray\|hover:bg-blue\|hover:text-blue" src/app/\(admin\)/admin/ | grep -v node_modules
```
Expected: no output (zero matches).

Also verify the app builds:
```bash
npx next build 2>&1 | tail -5
```
Expected: build succeeds with no errors.
</verification>

<success_criteria>
- Every admin content page heading uses font-headline (Newsreader)
- All primary CTA buttons display gradient pill style (from-primary to-primary-container rounded-full)
- All form inputs have rounded-sm corners and focus:ring-primary
- Cards use bg-surface-elevated with no border (tonal separation)
- Table headers use bg-surface background
- Semantic status colors (approve green, reject red, health indicators) preserved
- Zero bg-gray-*, bg-blue-*, border-gray-*, text-gray-* classes in any modified file
- Application builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/30-cms-admin-restyling/30-02-SUMMARY.md`
</output>
