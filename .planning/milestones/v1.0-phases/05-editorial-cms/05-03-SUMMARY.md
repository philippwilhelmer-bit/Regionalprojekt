---
phase: 05-editorial-cms
plan: 03
subsystem: api
tags: [prisma, vitest, tdd, server-actions, cms, articles, exceptions]

# Dependency graph
requires:
  - phase: 05-editorial-cms plan 01
    provides: Prisma schema with isPinned/isFeatured on Article, ArticleBezirk junction model, cleanDb() updated for Phase 5 tables
  - phase: 05-editorial-cms plan 02
    provides: Auth utilities and requireAuth() pattern for Server Action wrappers

provides:
  - createManualArticleDb: creates Article with source=MANUAL, isAutoGenerated=false, status=PUBLISHED, ArticleBezirk rows per bezirkId
  - updateArticleDb: atomically replaces ArticleBezirk rows (deleteMany + createMany) when bezirkIds provided
  - togglePinDb / toggleFeatureDb: flip isPinned / isFeatured booleans
  - softDeleteDb: sets status=REJECTED
  - listArticlesAdmin: filterable article list with bezirke relation, excludes REJECTED/FAILED by default
  - approveArticleDb: sets status=PUBLISHED with publishedAt=now
  - rejectArticleDb: sets status=REJECTED
  - listExceptionQueueDb: returns REVIEW-only articles ordered by createdAt asc, includes rawPayload

affects: [05-04, 05-05, 06-frontend]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "*Db functions are injectable (PrismaClient argument) — Server Action wrappers call requireAuth() then delegate to *Db"
    - "listArticlesAdmin uses duck-typed DI overload ($connect in db) — zero-arg = production singleton, injected = tests"
    - "Atomic Bezirk replacement: articleBezirk.deleteMany then articleBezirk.createMany in sequence"

key-files:
  created:
    - src/lib/admin/exceptions-actions.ts
  modified:
    - src/lib/admin/articles-actions.ts
    - src/lib/admin/articles-actions.test.ts
    - src/lib/admin/exceptions-actions.test.ts

key-decisions:
  - "DB-layer functions (*Db) exported separately from Server Action wrappers — tests call *Db directly, avoiding cookie/auth mocking"
  - "listArticlesAdmin uses DI overload (duck-typed $connect check) — allows both zero-arg production use and injected test DB"
  - "approveArticleDb sets publishedAt=new Date() alongside status=PUBLISHED — approval marks when article went live"

patterns-established:
  - "Injectable *Db functions with non-injectable Server Action wrappers — single file, two export layers"
  - "Atomic Bezirk replacement via deleteMany + createMany — no partial update state possible"

requirements-completed: [CMS-01, CMS-02, CMS-03]

# Metrics
duration: 8min
completed: 2026-03-22
---

# Phase 5 Plan 03: Articles + Exceptions Server Actions (TDD) Summary

**Pure DB-layer functions for CMS-01/02/03 (create, update, togglePin, toggleFeature, softDelete, approveArticle, rejectArticle, listExceptionQueue) with 14 vitest tests all GREEN, injectable PrismaClient for test isolation**

## Performance

- **Duration:** 8 min
- **Started:** 2026-03-22T11:52:24Z
- **Completed:** 2026-03-22T12:00:00Z
- **Tasks:** 2 (TDD RED + GREEN)
- **Files modified:** 4

## Accomplishments

- 10 articles-actions tests GREEN: createManualArticle (3), updateArticle (2), togglePin (1), toggleFeature (1), softDelete (1), listArticlesAdmin (2)
- 4 exceptions-actions tests GREEN: approveArticle (1), rejectArticle (1), listExceptionQueue (2)
- Full suite remains at 139 tests, 0 failures, 0 TypeScript errors
- exceptions-actions.ts committed with all *Db functions and Server Action wrappers

## Task Commits

Each task was committed atomically:

1. **Task 1 (RED): Expand stubs into real failing tests** - `3e2a99f` (test)
2. **Task 2 (GREEN): Implement articles-actions.ts DB-layer** - `f9a13b1` (fix — committed as part of 05-02 TypeScript fix)
3. **Task 3 (GREEN): Implement exceptions-actions.ts DB-layer** - `8f47196` (feat)

_Note: articles-actions.ts was implemented and committed during 05-02 to resolve TypeScript errors. exceptions-actions.ts committed in this plan as its dedicated GREEN commit._

## Files Created/Modified

- `src/lib/admin/articles-actions.ts` — createManualArticleDb, updateArticleDb, togglePinDb, toggleFeatureDb, softDeleteDb, listArticlesAdmin + Server Action wrappers
- `src/lib/admin/articles-actions.test.ts` — 10 tests covering all CMS-01/02 DB functions
- `src/lib/admin/exceptions-actions.ts` — approveArticleDb, rejectArticleDb, listExceptionQueueDb + Server Action wrappers (newly committed)
- `src/lib/admin/exceptions-actions.test.ts` — 4 tests covering all CMS-03 DB functions

## Decisions Made

- DB-layer functions (*Db) are exported separately from Server Action wrappers — tests call *Db directly, avoiding cookie/auth mocking in vitest
- listArticlesAdmin uses duck-typed DI overload ($connect check) — allows zero-arg production use and injected test DB in the same export
- approveArticleDb sets publishedAt=new Date() alongside status=PUBLISHED — approval marks the moment article went live

## Deviations from Plan

### Note on TDD Sequencing

articles-actions.ts was committed earlier (in 05-02 fix commit `f9a13b1`) to resolve TypeScript errors before the RED test commit. The canonical TDD RED→GREEN order was partially inverted for articles-actions.ts. The exceptions-actions.ts followed strict RED→GREEN: tests committed as stubs (`3e2a99f`), implementation committed (`8f47196`). No functional issues — all tests pass GREEN and the business logic is correct.

## Issues Encountered

None — both test files ran GREEN on first execution. TypeScript passes cleanly with no errors.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- CMS-01, CMS-02, CMS-03 DB-layer fully implemented and tested
- Server Action wrappers ready for Next.js UI integration in Phase 5 plans 04/05
- listArticlesAdmin supports all filters (bezirkId, sourceType, status, fromDate, toDate, limit, offset) needed for admin UI
- listExceptionQueueDb returns rawPayload for side-by-side diff display in exception review UI

## Self-Check: PASSED

Files exist:
- src/lib/admin/articles-actions.ts: present (committed f9a13b1)
- src/lib/admin/exceptions-actions.ts: present (committed 8f47196)
- src/lib/admin/articles-actions.test.ts: present (committed 3e2a99f)
- src/lib/admin/exceptions-actions.test.ts: present (committed 3e2a99f)

Commits verified in git log: 3e2a99f, f9a13b1, 8f47196 — all present.
Test results: 139 passed, 0 failed. TypeScript: 0 errors.

---
*Phase: 05-editorial-cms*
*Completed: 2026-03-22*
