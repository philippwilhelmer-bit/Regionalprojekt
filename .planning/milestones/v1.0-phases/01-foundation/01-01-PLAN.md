---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - vitest.config.ts
  - src/test/setup-db.ts
  - src/lib/content/bezirke.test.ts
  - prisma/seed.test.ts
autonomous: true
requirements:
  - CONF-01
  - CONF-02

must_haves:
  truths:
    - "Running `npx vitest run` completes without crashing (tests may be pending/skipped but the runner itself works)"
    - "Running `npx tsc --noEmit` exits with zero errors on the project skeleton"
    - "`npm run dev` starts Next.js 15 without errors (no content yet, just the app shell)"
    - "pgLite is importable in tests — the test setup utility connects to an in-process PostgreSQL instance"
  artifacts:
    - path: "package.json"
      provides: "Dependency manifest with Next.js 15, Prisma v6, Vitest, pgLite dev deps"
    - path: "vitest.config.ts"
      provides: "Vitest configuration pointing at src/test/setup-db.ts as setupFiles"
    - path: "src/test/setup-db.ts"
      provides: "Shared pgLite + Prisma adapter test utility (apply migrations, clean between tests)"
    - path: "src/lib/content/bezirke.test.ts"
      provides: "Stub test file for CONF-02 (failing/skipped — tests written, not yet passing)"
    - path: "prisma/seed.test.ts"
      provides: "Stub test file for CONF-01 config-driven seeding (failing/skipped)"
  key_links:
    - from: "vitest.config.ts"
      to: "src/test/setup-db.ts"
      via: "setupFiles array"
      pattern: "setupFiles.*setup-db"
    - from: "src/test/setup-db.ts"
      to: "@electric-sql/pglite"
      via: "import and PGlite constructor"
      pattern: "PGlite|pglite"
---

<objective>
Bootstrap the project — Next.js 15 app, Prisma v6, Vitest with pgLite in-process PostgreSQL, and stub test files for the two core requirements.

Purpose: Every subsequent plan depends on the project existing and the test runner working. Tests are written RED before implementation begins (Nyquist compliance).
Output: Runnable project skeleton with failing/pending tests for CONF-01 and CONF-02.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project with Prisma v6 and dev tooling</name>
  <files>package.json, tsconfig.json, next.config.ts, .env.example, .gitignore</files>
  <action>
Initialize a Next.js 15 App Router project (TypeScript, Tailwind CSS v3, no ESLint prompt customization, src/ directory, no Turbopack — stable webpack for now). Then install the production and dev dependencies:

Production deps:
- `prisma@^6.16.0` and `@prisma/client@^6.16.0`
- `zod@^3.x` (runtime config validation, used in Plan 02)

Dev deps:
- `tsx@^4.x` (seed runner)
- `typescript@^5.x` and `@types/node@^20.x`
- `vitest@^2.x` and `@vitest/coverage-v8`
- `@electric-sql/pglite` and `pglite-prisma-adapter` (in-process PostgreSQL for tests — no Docker needed)

Run `npx prisma init --datasource-provider postgresql` to create `prisma/schema.prisma` and add `DATABASE_URL` stub to `.env`. Add `.env` to `.gitignore` (keep `.env.example` with placeholder `DATABASE_URL=postgresql://...`).

Create `prisma.config.ts` at project root (Prisma v6 config, NOT the old `package.json` prisma.seed key):
```typescript
import { defineConfig } from 'prisma/config'
export default defineConfig({
  migrations: {
    path: 'prisma/migrations',
    seed: 'tsx prisma/seed.ts',
  },
})
```

In `tsconfig.json` ensure `"moduleResolution": "bundler"` (Next.js 15 default) and `"strict": true`.
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && node -e "require('./package.json')" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    `package.json` contains prisma, @prisma/client, vitest, @electric-sql/pglite, pglite-prisma-adapter as deps. `prisma/schema.prisma` exists with postgresql datasource. `prisma.config.ts` exists with migrations.seed set to `tsx prisma/seed.ts`. `npx tsc --noEmit` exits clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Set up Vitest with pgLite and write RED stub tests</name>
  <files>vitest.config.ts, src/test/setup-db.ts, src/lib/content/bezirke.test.ts, prisma/seed.test.ts</files>
  <behavior>
    - bezirke.test.ts — Test: `listBezirke()` returns exactly 13 entries after seed (CONF-02)
    - bezirke.test.ts — Test: every Bezirk has a non-empty `slug`, `name`, and `gemeindeSynonyms` array with at least 1 entry (CONF-02)
    - bezirke.test.ts — Test: `getBezirkBySlug('liezen')` returns the Liezen Bezirk with synonyms including 'Ennstal' (CONF-02)
    - seed.test.ts — Test: re-seeding with a config that has only 1 region produces exactly 1 Bezirk row (CONF-01 config-driven mechanic)
    - seed.test.ts — Test: seeding with the steiermark config produces 13 Bezirke (CONF-01 + CONF-02)
  </behavior>
  <action>
Create `vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config'
export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    setupFiles: ['src/test/setup-db.ts'],
  },
})
```

Create `src/test/setup-db.ts` as the shared pgLite test utility. It should:
- Import `PGlite` from `@electric-sql/pglite` and `PrismaPGlite` adapter from `pglite-prisma-adapter`
- Export an async `createTestDb()` function that: creates an in-process PGlite instance, applies Prisma migrations via `prisma migrate deploy` equivalent (or runs the migration SQL directly), returns a `PrismaClient` connected through the pgLite adapter
- Export a `cleanDb(prisma)` helper that truncates all tables between tests

Write `src/lib/content/bezirke.test.ts` with the 3 test cases listed in `<behavior>`. Import `listBezirke` and `getBezirkBySlug` from `../content/bezirke` (these don't exist yet — this is the RED state). Mark each test with `it.todo` or let them fail on import — either is acceptable RED state. The test file must be syntactically valid.

Write `prisma/seed.test.ts` with the 2 test cases listed in `<behavior>`. Import the seed `main()` function and the ability to swap config. Tests can be `.todo` stubs — they must be syntactically valid and named clearly.

At this point `npx vitest run` should exit with tests either failing (import errors) or as todos — NOT crashing the runner itself.
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>
    `vitest.config.ts` exists. `src/test/setup-db.ts` exports `createTestDb` and `cleanDb`. `src/lib/content/bezirke.test.ts` and `prisma/seed.test.ts` exist with named test cases covering all 5 behaviors above. `npx vitest run` completes (runner does not crash — tests may be red/todo).
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits with 0 errors
- `npx vitest run` completes without the runner crashing
- `ls prisma/schema.prisma prisma.config.ts src/test/setup-db.ts vitest.config.ts` all exist
- `cat package.json | grep -E "pglite|vitest|prisma"` confirms all required deps present
</verification>

<success_criteria>
Project skeleton exists and is ready for implementation in Plans 02 and 03. Test stubs are in place (RED state). No implementation code exists yet beyond the project scaffold.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
