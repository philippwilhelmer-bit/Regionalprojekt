---
phase: 10-config-branding-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/reader/Header.tsx
  - src/app/layout.tsx
  - src/lib/reader/rss.ts
  - src/app/admin/login/page.tsx
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - READ-06

must_haves:
  truths:
    - "Header.tsx renders config.siteName — the string 'Ennstal Aktuell' does not appear as a hardcoded literal in the file"
    - "app/layout.tsx metadata.title equals config.siteName — no hardcoded string"
    - "rss.ts feed title is built from config.siteName, not a hardcoded string"
    - "admin/login/page.tsx heading reads config.siteName + ' Admin' — no hardcoded string"
    - "Changing siteName in bundesland.config.ts propagates to all four locations without any other code change"
    - "npx vitest run passes — all existing tests green after refactor"
    - "npx tsc --noEmit passes — no TypeScript errors"
  artifacts:
    - path: "src/components/reader/Header.tsx"
      provides: "Site name rendered from config.siteName in header span"
      contains: "config.siteName"
    - path: "src/app/layout.tsx"
      provides: "metadata.title from config.siteName"
      contains: "config.siteName"
    - path: "src/lib/reader/rss.ts"
      provides: "RSS feed title from config.siteName"
      contains: "config.siteName"
    - path: "src/app/admin/login/page.tsx"
      provides: "Admin login heading from config.siteName"
      contains: "config.siteName"
  key_links:
    - from: "bundesland.config.ts"
      to: "src/components/reader/Header.tsx"
      via: "import config from '@/../bundesland.config'"
      pattern: "config\\.siteName"
    - from: "bundesland.config.ts"
      to: "src/app/layout.tsx"
      via: "import config from '@/../bundesland.config'"
      pattern: "config\\.siteName"
    - from: "bundesland.config.ts"
      to: "src/lib/reader/rss.ts"
      via: "import config from '@/../bundesland.config'"
      pattern: "config\\.siteName"
    - from: "bundesland.config.ts"
      to: "src/app/admin/login/page.tsx"
      via: "import config from '@/../bundesland.config'"
      pattern: "config\\.siteName"
---

<objective>
Replace four hardcoded "Ennstal Aktuell" string literals with config.siteName from bundesland.config.ts, making the Bundesland config the single source of truth for site branding.

Purpose: CONF-01 requires new Bundesland deployments to need only a config change. Four locations currently hardcode the site name, meaning a redeployment for another Bundesland would require code edits in addition to the config change.

Output: Four modified files. No new files, no schema changes, no new dependencies.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From bundesland.config.ts (project root) — current siteName value:
```typescript
import type { BundeslandConfig } from './src/types/bundesland'

const config: BundeslandConfig = {
  siteName: 'Ennstal Aktuell',
  // ...
}
export default config
```

From src/types/bundesland.ts — siteName is a required string field:
```typescript
export interface BundeslandConfig {
  siteName: string
  // ... other fields
}
```

Established import path (proven in Phase 9 — impressum/page.tsx and AdUnit.tsx):
```typescript
import config from '@/../bundesland.config'
```

Current state of four target files (before-state):

src/components/reader/Header.tsx line 47:
  <span className="font-bold text-zinc-900">Ennstal Aktuell</span>

src/app/layout.tsx line 6:
  title: "Ennstal Aktuell",

src/lib/reader/rss.ts line 19:
  title: `Ennstal Aktuell \u2013 ${slug}`,

src/app/admin/login/page.tsx line 7:
  <h1 className="text-2xl font-bold mb-6 text-gray-900">Ennstal Aktuell Admin</h1>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire config.siteName into Header.tsx and app/layout.tsx</name>
  <files>src/components/reader/Header.tsx, src/app/layout.tsx</files>
  <action>
Read node_modules/next/dist/docs/ for any relevant App Router conventions before editing if uncertain about metadata static export behavior.

In src/components/reader/Header.tsx:
- Add `import config from '@/../bundesland.config'` after the existing `import { useEffect, useState } from "react"` line (line 3)
- Replace line 47: `<span className="font-bold text-zinc-900">Ennstal Aktuell</span>` with `<span className="font-bold text-zinc-900">{config.siteName}</span>`
- No other changes to this file. The file uses "use client" directive — this is safe because bundesland.config.ts has no server-only imports (confirmed: it only imports BundeslandConfig type from src/types/bundesland.ts, a pure type file).

In src/app/layout.tsx:
- Add `import config from '@/../bundesland.config'` after the existing `import Script from "next/script"` line
- Replace line 6: `title: "Ennstal Aktuell",` with `title: config.siteName,`
- Keep `export const metadata` (static export) — do NOT switch to generateMetadata(). The config.siteName resolves at build time from the module-level import.
- No other changes to this file.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- Header.tsx has `import config from '@/../bundesland.config'` and renders `{config.siteName}` in the header span
- layout.tsx has `import config from '@/../bundesland.config'` and `title: config.siteName` in metadata
- The string literal "Ennstal Aktuell" does not appear in either file
- `npx tsc --noEmit` passes with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire config.siteName into rss.ts and admin/login/page.tsx, verify full suite</name>
  <files>src/lib/reader/rss.ts, src/app/admin/login/page.tsx</files>
  <action>
In src/lib/reader/rss.ts:
- Add `import config from '@/../bundesland.config'` after the existing `import { slugify } from './slug'` line (line 3)
- Replace line 19: `title: \`Ennstal Aktuell \u2013 ${slug}\`` with `title: \`${config.siteName} \u2013 ${slug}\``
- The description string (`Aktuelle Nachrichten für ${slug} aus der Steiermark`) does NOT contain the site name — leave it unchanged. This is intentional per user decision.
- No other changes.

In src/app/admin/login/page.tsx:
- Add `import config from '@/../bundesland.config'` after the existing `import { LoginForm } from './login-form'` line (line 1), placing it before the LoginForm import or after — either order is fine
- Replace line 7: `<h1 className="text-2xl font-bold mb-6 text-gray-900">Ennstal Aktuell Admin</h1>` with `<h1 className="text-2xl font-bold mb-6 text-gray-900">{config.siteName} Admin</h1>`
- Use JSX expression `{config.siteName} Admin` (expression followed by plain text node) — matches the JSX style used throughout the codebase (e.g. impressum/page.tsx). Do NOT use a template literal inside a JSX expression.
- No other changes.

After both edits, run the full test suite to confirm no regressions. The existing rss.test.ts does not assert the string "Ennstal Aktuell" in the title — it only checks that the slug appears in the XML. After this change, the feed title will be "Ennstal Aktuell – liezen" (real config value), which still passes those assertions.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>
- rss.ts has `import config from '@/../bundesland.config'` and uses `${config.siteName} \u2013 ${slug}` as feed title
- admin/login/page.tsx has `import config from '@/../bundesland.config'` and renders `{config.siteName} Admin` in the h1
- The string literal "Ennstal Aktuell" does not appear in either file
- `npx vitest run` passes — all tests green
- `npx tsc --noEmit` passes — no type errors
- grep for "Ennstal Aktuell" across the four modified files returns zero matches
  </done>
</task>

</tasks>

<verification>
Run after both tasks complete:

```bash
# No hardcoded site name remains in the four target files
grep -r "Ennstal Aktuell" src/components/reader/Header.tsx src/app/layout.tsx src/lib/reader/rss.ts src/app/admin/login/page.tsx

# All four files import config
grep "bundesland.config" src/components/reader/Header.tsx src/app/layout.tsx src/lib/reader/rss.ts src/app/admin/login/page.tsx

# Full test suite
npx vitest run

# TypeScript clean
npx tsc --noEmit
```

Expected: grep for "Ennstal Aktuell" returns zero results in those four files. All four files show the config import. Test suite green. tsc clean.
</verification>

<success_criteria>
1. `Header.tsx` renders `config.siteName` — grep for "Ennstal Aktuell" in Header.tsx returns zero matches
2. `app/layout.tsx` sets `metadata.title` from `config.siteName` — grep returns zero matches
3. `src/lib/reader/rss.ts` uses `config.siteName` in feed title — grep returns zero matches
4. `src/app/admin/login/page.tsx` uses `config.siteName + " Admin"` as heading — grep returns zero matches
5. Changing `siteName` in `bundesland.config.ts` propagates to all four locations — single config file is the source of truth
6. `npx vitest run` passes — no regressions
7. `npx tsc --noEmit` passes — no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-config-branding-wiring/10-01-SUMMARY.md` using the summary template at @/Users/philipp/.claude/get-shit-done/templates/summary.md
</output>
