---
phase: 19-article-detail-bottom-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(public)/artikel/[publicId]/[slug]/page.tsx
  - src/components/reader/TopMeldungenRow.tsx
  - src/components/reader/ShareButton.tsx
autonomous: true
requirements: [ART-01, ART-02]

must_haves:
  truths:
    - "Article headline renders in Newsreader serif (font-headline) on a warm cream background"
    - "Article body paragraphs render in Inter (font-body) with sage/dark text — no zinc colors remain"
    - "Full-bleed hero image displays edge-to-edge when imageUrl exists; nothing shows when absent"
    - "Share button appears below the headline, before article body"
    - "Related articles display as horizontal scroll cards matching the homepage Top-Meldungen pattern"
    - "Breadcrumb, timestamp, source attribution, and AI disclosure all use sage palette"
  artifacts:
    - path: "src/app/(public)/artikel/[publicId]/[slug]/page.tsx"
      provides: "Restructured article page with hero image, cream background, sage palette, repositioned share button"
      contains: "bg-cream"
    - path: "src/components/reader/TopMeldungenRow.tsx"
      provides: "Configurable heading prop for reuse in related articles"
      exports: ["TopMeldungenRow"]
    - path: "src/components/reader/ShareButton.tsx"
      provides: "Share button with sage/cream palette instead of zinc"
      exports: ["ShareButton"]
  key_links:
    - from: "src/app/(public)/artikel/[publicId]/[slug]/page.tsx"
      to: "TopMeldungenRow"
      via: "import and render with heading='Weitere Artikel'"
      pattern: "TopMeldungenRow.*articles.*heading"
    - from: "src/app/(public)/artikel/[publicId]/[slug]/page.tsx"
      to: "ShareButton"
      via: "positioned below headline, before article body"
      pattern: "ShareButton"
---

<objective>
Restyle the article detail page with editorial typography on a warm cream canvas, add a full-bleed hero image, reposition the share button, and convert related articles to horizontal scroll cards.

Purpose: Bring the article page into visual consistency with the Phase 16-18 design system (ART-01 editorial typography, ART-02 cream/sage palette).
Output: Restyled article page with hero image, cream background, sage palette, and horizontal-scroll related articles.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-article-detail-bottom-navigation/19-CONTEXT.md
@.planning/phases/19-article-detail-bottom-navigation/19-RESEARCH.md

@src/app/(public)/artikel/[publicId]/[slug]/page.tsx
@src/components/reader/TopMeldungenRow.tsx
@src/components/reader/ShareButton.tsx
@src/components/reader/HeroArticle.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/components/reader/TopMeldungenRow.tsx:
```typescript
interface TopMeldungenRowProps {
  articles: ArticleWithBezirke[];
}
export function TopMeldungenRow({ articles }: TopMeldungenRowProps)
```

From src/components/reader/ShareButton.tsx:
```typescript
interface ShareButtonProps {
  title: string;
  url: string;
}
export function ShareButton({ title, url }: ShareButtonProps)
```

From src/lib/content/articles (used in page.tsx):
```typescript
getArticleByPublicId(publicId: string): Promise<ArticleWithBezirke | null>
listArticlesReader({ bezirkIds, limit }): Promise<ArticleWithBezirke[]>
```

Design tokens available (globals.css @theme):
- `cream` (#fbfaee), `styrian-green` (#2D5A27), `sage` (#4a5d4e), `alpine-red` (#8b0000)
- `font-headline` (Newsreader), `font-body` (Inter), `font-label` (Work Sans)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add heading prop to TopMeldungenRow and restyle ShareButton</name>
  <files>src/components/reader/TopMeldungenRow.tsx, src/components/reader/ShareButton.tsx</files>
  <action>
**TopMeldungenRow.tsx** — Add an optional `heading` prop to the component interface:
- Change `TopMeldungenRowProps` to include `heading?: string` (default: `"Top-Meldungen"`)
- Replace the hardcoded `"Top-Meldungen"` text with `{heading}`
- Replace the `border-zinc-200` divider above the heading with `border-[#c4a882]/30` (warm-brown, matching the homepage section heading pattern for "Weitere Artikel" usage)
  - NOTE: Only change the divider if the heading is NOT "Top-Meldungen". Actually, simpler: accept an optional `dividerColor` prop defaulting to `"border-zinc-200"` — or just change the divider to warm-brown universally since the homepage also uses the editorial palette now. Use `border-sage/20` as a subtle warm divider that works for both contexts.
- Keep all existing functionality (BEZIRK_COLORS, scroll, fade) unchanged
- The component is already `"use client"` — no directive changes needed

**ShareButton.tsx** — Update palette from zinc to sage/cream:
- Replace `border-zinc-300` with `border-sage/30`
- Replace `text-zinc-700` with `text-sage`
- Replace `hover:bg-zinc-50` with `hover:bg-cream`
- Add a Material Symbols `share` icon: add `<span className="material-symbols-outlined text-base" aria-hidden="true">share</span>` before the text
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TopMeldungenRow accepts optional heading prop (defaults to "Top-Meldungen"). ShareButton uses sage/cream palette with share icon. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure article page with hero image, cream palette, and editorial layout</name>
  <files>src/app/(public)/artikel/[publicId]/[slug]/page.tsx</files>
  <action>
Restructure the entire article page JSX. The current page wraps everything in `max-w-2xl mx-auto px-4 py-8`. The new layout needs a full-bleed hero image that escapes the content column.

**New outer structure:**
```
<> (fragment)
  <script> (JSON-LD — unchanged)
  <div className="bg-cream min-h-screen">
    {/* Hero image — full bleed, outside content column */}
    {article.imageUrl && (
      // eslint-disable-next-line @next/next/no-img-element
      <img src={article.imageUrl} alt={article.title ?? ""} className="w-full object-cover max-h-[50vh]" loading="eager" />
    )}

    {/* Content column — constrained */}
    <div className="max-w-2xl mx-auto px-4 py-6">
      ... all text content ...
    </div>
  </div>
</>
```

**Inside the content column, apply these changes in order:**

1. **Breadcrumb**: Replace `text-zinc-500` with `text-sage`. Add `font-label` class to the nav element.

2. **Article header**:
   - h1: Replace `text-zinc-900` with `text-zinc-800` (keep very dark for readability on cream). Keep `font-headline text-2xl font-bold leading-tight mb-2`.
   - Timestamp: Replace `text-zinc-500` with `text-sage`. Keep `font-label text-sm`.

3. **Share button — MOVE HERE**: Place the ShareButton block immediately after the header `</header>`, before the article body. Wrap in `<div className="mb-6">`. Remove the old share button location (which was after article body, before ad unit).

4. **AI disclosure — MOVE BEFORE body**: If `article.isAutoGenerated`, render the disclosure block after share button, before article body. Restyle:
   - Replace `rounded bg-amber-50 border-amber-200 text-amber-800` with `rounded-sm bg-cream border border-sage/30 text-sage`

5. **Article body**: Replace `prose prose-zinc` with:
   ```
   prose max-w-none mb-6 prose-p:text-[#2a2a2a] prose-headings:font-headline prose-a:text-styrian-green prose-a:underline
   ```
   This removes the zinc color scheme and sets dark text for readability on cream, serif headings, and green links.

6. **Source attribution**: Replace `text-zinc-400` with `text-sage/70`. Keep `text-xs mb-4`.

7. **Ad unit**: Keep as-is (no palette changes needed for ad container).

8. **Related articles**: Replace the entire `<section>` block. Remove the vertical `flex-col gap-3` list of Link cards. Instead:
   - Keep the section wrapper with `aria-labelledby="related-heading"`
   - Remove the h2 and the manual card list
   - Render: `<TopMeldungenRow articles={relatedArticles} heading="Weitere Artikel" />`
   - Add import: `import { TopMeldungenRow } from "@/components/reader/TopMeldungenRow";`
   - Remove the `slugify` usage for related articles (TopMeldungenRow handles its own slugification)
   - Keep the `relatedArticles.length > 0` guard

**Imports to add:** `TopMeldungenRow` from `@/components/reader/TopMeldungenRow`
**Imports that may become unused:** Check if `slugify` is still used (yes — for canonical slug enforcement at top). Keep it.

IMPORTANT: The article page is a Server Component (no "use client"). TopMeldungenRow is "use client" — this is fine, Server Components can render Client Components.
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Article page has: full-bleed hero image when imageUrl present, warm cream background, all text in sage/dark palette (no zinc), share button below headline, AI disclosure restyled to sage, related articles as horizontal scroll cards via TopMeldungenRow with "Weitere Artikel" heading. No TypeScript errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run build` succeeds (RSC/client boundary validation)
- Visual: Article page at `/artikel/{publicId}/{slug}` shows cream background, serif headline, hero image (if present), sage-colored metadata, horizontal related articles
</verification>

<success_criteria>
- No zinc-* color classes remain in article page.tsx (all replaced with cream/sage/styrian-green)
- Hero image renders full-bleed above content column when imageUrl exists
- Share button appears between headline and article body
- Related articles render as horizontal scroll cards (not vertical list)
- TopMeldungenRow works with custom heading prop on both homepage and article page
- ShareButton uses sage palette with share icon
</success_criteria>

<output>
After completion, create `.planning/phases/19-article-detail-bottom-navigation/19-01-SUMMARY.md`
</output>
