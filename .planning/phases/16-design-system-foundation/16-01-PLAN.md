---
phase: 16-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-04

must_haves:
  truths:
    - "Newsreader, Inter, and Work Sans fonts load on reader pages via next/font/google (self-hosted, no external requests)"
    - "Tailwind tokens styrian-green, cream, alpine-red, sage are available for use in classes"
    - "Body background is warm cream (#fbfaee) instead of bg-zinc-50"
    - "Default rounded-sm maps to 2px border radius"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @theme tokens for colors, fonts, and border radius"
      contains: "@theme"
    - path: "src/app/layout.tsx"
      provides: "Font loading via next/font/google, CSS variable injection, cream background"
      contains: "Newsreader"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS variables set on html element consumed by @theme"
      pattern: "--font-"
---

<objective>
Define all design tokens — fonts, colors, border radius — in the Tailwind v4 theme and wire font loading through next/font/google in the root layout.

Purpose: Every subsequent phase (17-20) and plan 02 of this phase depend on these tokens being available as Tailwind classes. This is the foundation layer.
Output: Updated globals.css with @theme tokens, updated layout.tsx with font loading and cream background.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-design-system-foundation/16-CONTEXT.md

@src/app/globals.css
@src/app/layout.tsx

<interfaces>
<!-- Current globals.css — minimal, will be rewritten -->
<!-- src/app/globals.css -->
@import "tailwindcss";
body { font-family: Arial, Helvetica, sans-serif; }

<!-- Current layout.tsx — font loading and body classes will change -->
<!-- src/app/layout.tsx -->
export default function RootLayout({ children }) — body has className "min-h-full flex flex-col bg-zinc-50 text-zinc-900 antialiased font-sans"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define design tokens in globals.css and wire font loading in layout.tsx</name>
  <files>src/app/globals.css, src/app/layout.tsx</files>
  <action>
**IMPORTANT:** Before writing any Next.js code, read the relevant guide in `node_modules/next/dist/docs/` for `next/font` usage — APIs may differ from training data.

**globals.css** — Replace entire contents with:
1. `@import "tailwindcss";`
2. A `@theme` block defining:
   - Color tokens: `--color-styrian-green: #2D5A27`, `--color-cream: #fbfaee`, `--color-alpine-red: #8b0000`, `--color-sage: #4a5d4e`
   - Font family tokens: `--font-headline` (var(--font-newsreader), serif), `--font-body` (var(--font-inter), sans-serif), `--font-label` (var(--font-work-sans), sans-serif)
   - Border radius override: `--radius-sm: 2px` (so `rounded-sm` = 2px throughout the project)
3. A base `body` rule setting `font-family: var(--font-body)` so all reader text defaults to Inter.

**layout.tsx** — Update the root layout:
1. Import `Newsreader`, `Inter`, and `Work_Sans` from `next/font/google`
2. Configure each font:
   - Newsreader: weight ['400'], style ['normal', 'italic'], subsets ['latin'], display 'swap', variable '--font-newsreader'
   - Inter: weight ['400', '500', '600'], subsets ['latin'], display 'swap', variable '--font-inter'
   - Work_Sans: weight ['500', '600'], subsets ['latin'], display 'swap', variable '--font-label'
3. Add all three font CSS variable classes to the `<html>` element: `${newsreader.variable} ${inter.variable} ${workSans.variable}`
4. On `<body>`: replace `bg-zinc-50` with `bg-cream`, keep other utility classes (`min-h-full flex flex-col text-zinc-900 antialiased`), remove `font-sans` (body font now comes from globals.css)
5. Keep the existing AdSense Script tag and all other layout content unchanged.
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
  - globals.css has @theme with all 4 color tokens, 3 font tokens, and radius-sm override
  - layout.tsx loads 3 Google Fonts via next/font/google with CSS variables
  - Body background is bg-cream (#fbfaee)
  - Build succeeds without errors
  </done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- globals.css contains `@theme` block with `--color-styrian-green`, `--color-cream`, `--color-alpine-red`, `--color-sage`
- globals.css contains `--font-headline`, `--font-body`, `--font-label`
- globals.css contains `--radius-sm: 2px`
- layout.tsx imports Newsreader, Inter, Work_Sans from next/font/google
- layout.tsx body has `bg-cream` class
</verification>

<success_criteria>
Design tokens are defined and available as Tailwind classes. Fonts load on page visit. Body background is warm cream. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/16-design-system-foundation/16-01-SUMMARY.md`
</output>
