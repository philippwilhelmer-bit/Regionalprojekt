---
phase: 16-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - src/components/reader/ArticleCard.tsx
  - src/components/reader/BottomNav.tsx
  - src/components/reader/Header.tsx
  - src/components/reader/BezirkModal.tsx
  - src/components/reader/ShareButton.tsx
  - src/components/reader/CookieBanner.tsx
  - src/components/reader/ArticleFeed.tsx
  - src/app/(public)/artikel/[publicId]/[slug]/page.tsx
  - src/app/layout.tsx
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-03
  - DS-04

must_haves:
  truths:
    - "Headline elements use font-headline (Newsreader), body text uses font-body (Inter), labels use font-label (Work Sans)"
    - "Material Symbols Outlined icons render in BottomNav and Header replacing Unicode characters"
    - "All reader components use sharp 2px border radius — no rounded-xl, rounded-full (except spinner), or rounded-2xl remain"
    - "Bezirk color maps use design system palette (greens/sage/cream variations) instead of rainbow colors"
  artifacts:
    - path: "src/components/reader/ArticleCard.tsx"
      provides: "Remapped Bezirk colors using design palette, font-headline on titles, 2px radius"
      contains: "font-headline"
    - path: "src/components/reader/BottomNav.tsx"
      provides: "Material Symbol icon replacing Unicode glyph"
      contains: "material-symbols-outlined"
    - path: "src/components/reader/Header.tsx"
      provides: "Material Symbol icon replacing Unicode glyph, font-label on UI text"
      contains: "material-symbols-outlined"
    - path: "src/components/reader/BezirkModal.tsx"
      provides: "2px border radius replacing rounded-xl/2xl/lg"
      contains: "rounded-sm"
    - path: "src/app/layout.tsx"
      provides: "Material Symbols CDN link tag in head"
      contains: "Material+Symbols+Outlined"
  key_links:
    - from: "src/components/reader/BottomNav.tsx"
      to: "Material Symbols CDN"
      via: "icon font class"
      pattern: "material-symbols-outlined"
    - from: "src/components/reader/ArticleCard.tsx"
      to: "src/app/globals.css"
      via: "Tailwind token classes"
      pattern: "font-headline|bg-cream|text-styrian-green"
---

<objective>
Apply the design tokens from Plan 01 across all reader frontend components: swap fonts on headings/labels, load and use Material Symbols icons, remap Bezirk colors to the Styrian palette, and replace all oversized border-radius values with the sharp 2px default.

Purpose: Tokens defined in Plan 01 mean nothing until they are applied. This plan makes the visual transformation visible across the entire reader frontend.
Output: All reader components updated to use the new design system.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-design-system-foundation/16-CONTEXT.md
@.planning/phases/16-design-system-foundation/16-01-SUMMARY.md

@src/components/reader/ArticleCard.tsx
@src/components/reader/BottomNav.tsx
@src/components/reader/Header.tsx
@src/components/reader/BezirkModal.tsx
@src/components/reader/ShareButton.tsx
@src/components/reader/CookieBanner.tsx
@src/components/reader/ArticleFeed.tsx
@src/app/(public)/artikel/[publicId]/[slug]/page.tsx
@src/app/layout.tsx

<interfaces>
<!-- Key types the executor needs -->

<!-- From src/components/reader/ArticleCard.tsx -->
const BEZIRK_COLORS: Record<string, string> — gradient classes per Bezirk slug
const BEZIRK_BADGE_COLORS: Record<string, string> — badge text/bg classes per Bezirk slug
interface ArticleCardProps { article: ArticleWithBezirke; featured?: boolean; }

<!-- From src/components/reader/Header.tsx -->
export function Header({ bezirke }: { bezirke: BezirkItem[] }) — uses Unicode ▾ for dropdown arrow

<!-- From src/components/reader/BottomNav.tsx -->
export function BottomNav() — uses Unicode ◎ for nav icon

<!-- Rounded classes to replace in reader components (admin stays untouched): -->
ArticleCard.tsx: rounded-xl (line 81), rounded-full (line 139 — Bezirk badges)
BezirkModal.tsx: rounded-t-2xl, sm:rounded-2xl (line 101), rounded-lg (lines 108, 126), rounded-xl (lines 142, 148)
ShareButton.tsx: rounded-full (line 35)
CookieBanner.tsx: rounded (lines 60, 66) — already small, keep or change to rounded-sm
ArticleFeed.tsx: rounded-full (line 140) — spinner animation, KEEP as-is (spinners need circular shape)
Article page: rounded (line 128), rounded (line 165) — already small
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load Material Symbols CDN and apply font tokens + icons to Header, BottomNav, and article headlines</name>
  <files>src/app/layout.tsx, src/components/reader/Header.tsx, src/components/reader/BottomNav.tsx, src/components/reader/ArticleCard.tsx, src/app/(public)/artikel/[publicId]/[slug]/page.tsx</files>
  <action>
**IMPORTANT:** Before writing any Next.js code, read the relevant guide in `node_modules/next/dist/docs/` for metadata/head usage — APIs may differ from training data.

**layout.tsx** — Add Material Symbols Outlined CDN link:
- Add a `<link>` tag in the `<head>` (or via Next.js metadata approach) loading: `https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0`
- Per CONTEXT.md: external CDN request is accepted (GDPR acknowledged).

**Header.tsx** — Replace Unicode ▾ with Material Symbol:
- Replace `<span aria-hidden="true">▾</span>` with `<span className="material-symbols-outlined text-base" aria-hidden="true">arrow_drop_down</span>`
- Add `font-label` class to the site name span (Work Sans for UI labels)
- Keep all existing functionality unchanged.

**BottomNav.tsx** — Replace Unicode ◎ with Material Symbol:
- Replace `<span className="text-xl leading-none" aria-hidden="true">◎</span>` with `<span className="material-symbols-outlined text-xl" aria-hidden="true">newspaper</span>`
- Add `font-label` to the "Nachrichten" text span (Work Sans for labels).

**ArticleCard.tsx** — Apply font tokens to text:
- Add `font-headline` class to the `<h2>` element (Newsreader for headlines)
- Keep all other styling on the h2 unchanged.

**Article detail page** (`src/app/(public)/artikel/[publicId]/[slug]/page.tsx`):
- Find the article headline element (likely an `<h1>`) and add `font-headline` class
- Find body text containers and ensure they use `font-body` (should inherit from body rule, but verify)
- Add `font-label` to any label/metadata elements (category badges, date labels, etc.) where appropriate.
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
  - Material Symbols Outlined loads via CDN link in layout
  - Header shows arrow_drop_down material icon instead of ▾
  - BottomNav shows newspaper material icon instead of ◎
  - Article headlines use font-headline (Newsreader)
  - UI labels use font-label (Work Sans)
  - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Remap Bezirk colors to design palette and replace all oversized border-radius in reader components</name>
  <files>src/components/reader/ArticleCard.tsx, src/components/reader/BezirkModal.tsx, src/components/reader/ShareButton.tsx, src/components/reader/CookieBanner.tsx</files>
  <action>
**ArticleCard.tsx** — Remap BEZIRK_COLORS and BEZIRK_BADGE_COLORS:

Replace the rainbow color maps with Styrian palette variations. Use variations of styrian-green, sage, cream, and alpine-red. Example remapping (Claude's discretion on exact per-Bezirk shade assignments):
- BEZIRK_COLORS: Use gradients from combinations like `from-styrian-green to-sage`, `from-sage to-styrian-green`, `from-[#3a6b33] to-styrian-green`, `from-[#5a7d54] to-sage`, etc. Create visually distinguishable but palette-coherent gradients for all 13 Bezirke. Use arbitrary value syntax `from-[#hex]` for intermediate green/sage shades where needed.
- BEZIRK_BADGE_COLORS: Use `text-styrian-green bg-cream` as the base pattern with subtle variations. Some can use `text-sage`, `text-[#3a6b33]` etc. for variety while staying in palette.
- Default fallback: change `from-zinc-400 to-zinc-300` to a sage-based gradient, and `text-zinc-600 bg-zinc-100` to `text-sage bg-cream`.

Replace border-radius in ArticleCard:
- Line 81: `rounded-xl` -> `rounded-sm` (card container)
- Line 139: `rounded-full` -> `rounded-sm` (Bezirk badges — per CONTEXT.md "no pill shapes")

**BezirkModal.tsx** — Replace border-radius:
- Line 101: `rounded-t-2xl sm:rounded-2xl` -> `rounded-t-sm sm:rounded-sm`
- Line 108: `rounded-lg` -> `rounded-sm`
- Line 126: `rounded-lg` -> `rounded-sm`
- Line 142: `rounded-xl` -> `rounded-sm`
- Line 148: `rounded-xl` -> `rounded-sm`
- Also update the blue accent colors to use design tokens: `bg-blue-600` -> `bg-styrian-green`, `border-blue-600` -> `border-styrian-green`, `text-blue-700` -> `text-styrian-green`, `bg-blue-50` -> `bg-cream`, `hover:bg-blue-700` -> `hover:bg-[#244d20]` (darker green), `hover:border-blue-400` -> `hover:border-sage`.

**ShareButton.tsx** — Replace border-radius:
- `rounded-full` -> `rounded-sm`

**CookieBanner.tsx** — Replace border-radius:
- `rounded` on both buttons -> `rounded-sm` (already close to 2px but be explicit)

**DO NOT touch:**
- `ArticleFeed.tsx` spinner `rounded-full` — spinners must remain circular
- Any files in `src/app/(admin)/` — admin is out of scope
- `AdUnitClient.tsx` `rounded` — ad containers are fine as-is
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && npx next build 2>&1 | tail -20 && echo "--- Checking no rounded-xl/2xl in reader components ---" && grep -rn "rounded-xl\|rounded-2xl\|rounded-full\|rounded-lg" src/components/reader/ | grep -v "animate-spin" | grep -v node_modules || echo "PASS: No oversized radius found"</automated>
  </verify>
  <done>
  - BEZIRK_COLORS uses Styrian green/sage palette instead of rainbow
  - BEZIRK_BADGE_COLORS uses green/cream tones instead of per-color badges
  - All reader components use rounded-sm (2px) — no rounded-xl, rounded-full (except spinner), rounded-2xl, or rounded-lg remain
  - BezirkModal accent colors use styrian-green instead of blue
  - Build succeeds and grep confirms no oversized radius in reader components
  </done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- `grep -rn "rounded-xl\|rounded-2xl\|rounded-full\|rounded-lg" src/components/reader/ | grep -v "animate-spin"` returns nothing
- `grep -rn "font-headline" src/components/reader/ArticleCard.tsx` returns a match
- `grep -rn "material-symbols-outlined" src/components/reader/BottomNav.tsx src/components/reader/Header.tsx` returns matches for both files
- `grep -rn "from-blue\|from-sky\|from-emerald\|from-teal\|from-cyan\|from-violet\|from-purple\|from-indigo\|from-amber\|from-orange\|from-rose\|from-red\|from-lime" src/components/reader/ArticleCard.tsx` returns nothing (rainbow colors removed)
</verification>

<success_criteria>
The reader frontend visibly uses the new design system: Newsreader on headlines, Inter on body, Work Sans on labels, Material Symbol icons in nav, Styrian green palette on Bezirk elements, and sharp 2px corners everywhere. Admin pages are untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/16-design-system-foundation/16-02-SUMMARY.md`
</output>
