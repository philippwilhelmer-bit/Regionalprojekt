---
phase: 40-tile-pipeline-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/images/mapgen.ts
  - src/lib/images/mapgen.test.ts
autonomous: true
requirements:
  - MAP-03
  - MAP-05
  - MAP-07
  - MAP-08
  - INTG-02

must_haves:
  truths:
    - "sharp@0.33.5 is installed and can create a JPEG buffer from a blank canvas"
    - "@vercel/blob is installed as a dependency"
    - "mapgen.test.ts exists with test stubs covering tile fetch, layer selection, attribution, and graceful fallback"
    - "Attribution SVG uses pre-outlined path data, not <text> elements (no system font dependency)"
  artifacts:
    - path: "package.json"
      provides: "sharp@0.33.5 pinned + @img/sharp-linux-x64 optional + @vercel/blob"
      contains: "sharp.*0.33.5"
    - path: "src/lib/images/mapgen.ts"
      provides: "Module skeleton with types, constants, lat/lon math, layer selection, attribution SVG builder"
      exports: ["generateMapImage", "selectLayer", "latLonToTile"]
    - path: "src/lib/images/mapgen.test.ts"
      provides: "Test file covering MAP-03 through MAP-08, INTG-02"
      min_lines: 40
  key_links:
    - from: "src/lib/images/mapgen.ts"
      to: "sharp"
      via: "import sharp from 'sharp'"
      pattern: "import sharp"
    - from: "src/lib/images/mapgen.ts"
      to: "@vercel/blob"
      via: "import { put } from '@vercel/blob'"
      pattern: "from '@vercel/blob'"
---

<objective>
Install sharp@0.33.5 and @vercel/blob, create the mapgen.ts module skeleton with types, constants, pure functions (lat/lon math, layer selection, attribution SVG path builder), and a comprehensive test file. Confirm sharp can produce a JPEG buffer locally before any tile-fetching code is written.

Purpose: This is the blocking foundation -- sharp binary must work and test infrastructure must exist before Plan 02 builds the full pipeline.
Output: Working dependencies, module skeleton with tested pure functions, test file with stubs for async behaviors.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-tile-pipeline-infrastructure/40-CONTEXT.md
@.planning/phases/40-tile-pipeline-infrastructure/40-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/images/unsplash.ts:
```typescript
export interface UnsplashImage {
  url: string
  credit: string
}
```

Return shape for mapgen must match: `{ url: string; credit: string } | null`

From src/lib/ai/pipeline.ts:
- DI pattern: zero-arg production path; injected client for tests
- Error isolation: inner try/catch per article, failures logged but never thrown
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies and create mapgen.ts skeleton with pure functions and tests</name>
  <files>package.json, src/lib/images/mapgen.ts, src/lib/images/mapgen.test.ts</files>
  <behavior>
    - latLonToTile(47.07, 15.43, 13) returns { x: 4468, y: 2873 } (Graz test vector)
    - latLonToTile(48.21, 16.37, 13) returns correct values for Vienna
    - selectLayer("Neue Brücke über die Mur") returns "bmaporthofoto30cm" (aerial keyword "Brücke")
    - selectLayer("Wandern im Nationalpark Gesäuse") returns "bmapgelaende" (terrain keywords "Wandern", "Nationalpark")
    - selectLayer("Gemeinderatswahl in Graz") returns "bmapgrau" (default, no keywords match)
    - selectLayer("Hochwasser und Verkehr") returns "bmaporthofoto30cm" (aerial checked first per keyword order)
    - buildAttributionSvg(1200, 630) returns a Buffer containing valid SVG XML with path elements (not text elements)
    - The SVG buffer does NOT contain any `<text` elements (font-free validation)
    - sharp can create a 1200x630 JPEG buffer from a blank canvas (smoke test — confirms binary loads)
  </behavior>
  <action>
    1. Install dependencies:
       ```
       npm install sharp@0.33.5 @vercel/blob
       ```
    2. Add to package.json optionalDependencies: `"@img/sharp-linux-x64": "0.33.5"` (for Vercel linux-x64 deployment).

    3. Create `src/lib/images/mapgen.ts` with:
       - Type definitions: `LayerName = 'bmapgrau' | 'bmapgelaende' | 'bmaporthofoto30cm'`, `LAYER_CONFIG` record with ext/maxZoom per layer, `MapImage` type matching `{ url: string; credit: string }`.
       - Constants: `TILE_SIZE = 256`, `OUTPUT_WIDTH = 1200`, `OUTPUT_HEIGHT = 630`, `BASEMAP_SERVERS` array, `TERRAIN_KEYWORDS` array, `AERIAL_KEYWORDS` array (exact keywords from CONTEXT.md decisions).
       - `latLonToTile(lat, lon, zoom)` — Spherical Mercator formula from OSM wiki. Returns `{ x: number; y: number }`.
       - `selectLayer(headline)` — checks aerial keywords first, then terrain, defaults to bmapgrau. Uses `headline.includes(k)` for each keyword (case-sensitive per CONTEXT.md — German keywords are capitalized).
       - `tileUrl(layer, z, y, x)` — constructs basemap.at URL with z/y/x ordering (NOT z/x/y). Uses round-robin server selection.
       - `buildAttributionSvg(width, height)` — creates SVG with a semi-transparent dark rect at bottom-right (rgba 0,0,0,0.6) and "copyright basemap.at" rendered as SVG `<path>` elements (pre-outlined glyph data). Do NOT use `<text>` elements — Vercel lambdas have no system fonts. The path data for "copyright basemap.at" at ~13-14px in a sans-serif style should be hardcoded (~2KB of path data). Use a minimal set of glyphs: copyright symbol, space, b, a, s, e, m, p, period, t. White fill on the paths. Strip width ~180px, height ~26px, positioned bottom-right with 4px padding.
       - Stub `generateMapImage(lat, lon, headline, articleId)` that returns `Promise<MapImage | null>` — body is just `return null` for now (Plan 02 implements it).
       - Export: `generateMapImage`, `selectLayer`, `latLonToTile`, `buildAttributionSvg`, `tileUrl`, `LayerName`, `LAYER_CONFIG`.

    4. Create `src/lib/images/mapgen.test.ts` with:
       - Tests for `latLonToTile` with Graz vector (47.07, 15.43, zoom 13 => x:4468, y:2873).
       - Tests for `selectLayer` covering all three layer types + default.
       - Tests for `tileUrl` verifying z/y/x ordering in URL (NOT z/x/y) and correct server round-robin.
       - Tests for `buildAttributionSvg` verifying: returns Buffer, contains `<svg`, does NOT contain `<text`, contains `<path` or `<rect`.
       - Sharp smoke test: `sharp({ create: { width: 1200, height: 630, channels: 3, background: { r: 200, g: 200, b: 200 } } }).jpeg({ quality: 80 }).toBuffer()` produces a Buffer with length > 0.
       - Stub tests (marked with `.todo` or minimal assertion) for async behaviors tested in Plan 02: tile grid fetch, full generateMapImage success path, generateMapImage graceful failure path, Blob upload.

    NOTE: For the attribution SVG path data — if generating precise font outlines is too complex, use a reasonable approximation: simple geometric path data that renders readable "copyright basemap.at" text. The key constraint is NO `<text>` elements. An alternative approach: use a very small embedded bitmap/data URI inside the SVG, or use simple block-letter path outlines. The executor has discretion on the exact path shapes as long as the text is legible at thumbnail size and no font rendering is needed.
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && npx vitest run src/lib/images/mapgen.test.ts</automated>
  </verify>
  <done>
    - sharp@0.33.5 in dependencies, @img/sharp-linux-x64@0.33.5 in optionalDependencies
    - @vercel/blob in dependencies
    - mapgen.ts exports latLonToTile, selectLayer, tileUrl, buildAttributionSvg, generateMapImage
    - All pure function tests pass (lat/lon math, layer selection, tile URL format, attribution SVG)
    - Sharp smoke test passes (can create JPEG buffer)
    - Test stubs exist for async behaviors (Plan 02 fills them in)
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/images/mapgen.test.ts` — all tests pass
- `npm test` — full suite still green (no regressions)
- `grep "sharp.*0.33.5" package.json` — sharp pinned correctly
- `grep "@vercel/blob" package.json` — blob SDK installed
</verification>

<success_criteria>
- sharp@0.33.5 loads and produces a valid JPEG buffer locally
- Pure functions (latLonToTile, selectLayer, tileUrl, buildAttributionSvg) tested and correct
- Attribution SVG uses path elements, not text elements
- Test file exists with stubs for Plan 02's async behaviors
- Full test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/40-tile-pipeline-infrastructure/40-01-SUMMARY.md`
</output>
