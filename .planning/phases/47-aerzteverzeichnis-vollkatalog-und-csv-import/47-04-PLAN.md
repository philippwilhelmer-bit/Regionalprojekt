---
phase: 47-aerzteverzeichnis-vollkatalog-und-csv-import
plan: 04
type: execute
wave: 5
depends_on:
  - 47-03
files_modified:
  - src/app/(admin)/admin/aerzte/import/page.tsx
  - src/app/(admin)/admin/aerzte/import/ImportPreview.tsx
  - src/app/(admin)/admin/aerzte/page.tsx
  - src/app/(admin)/admin/aerzte/DoctorFilters.tsx
  - src/app/(admin)/admin/aerzte/DoctorRow.tsx
  - src/app/(admin)/admin/aerzte/DoctorForm.tsx
autonomous: false
requirements:
  - DIR-27
  - DIR-30
  - DIR-31
tags: [admin, ui, import, datalist, geocode-button, fachrichtung-select]
user_setup: []

must_haves:
  truths:
    - "/admin/aerzte/import page exists, gated by (admin)/layout, with file upload form posting to parseAndPreviewCsvForm"
    - "Token-driven re-render: /admin/aerzte/import?token=X reads from PREVIEW_CACHE, renders ImportPreview component"
    - "ImportPreview component shows summary line + collapsible conflicts table + Commit button (form action commitCsvImportForm)"
    - "/admin/aerzte page header shows progress counter 'X von Y Ärzte geocoded (Z ausstehend)'"
    - "/admin/aerzte page has 'Geocode next 200 (~4 min)' button (form action geocodeBatchForm) and 'CSV importieren' link"
    - "DoctorFilters drops kategorie chips; adds Fachrichtung searchable input + <datalist>"
    - "DoctorRow drops kategorie chip; shows fachrichtung label via FACHRICHTUNG_LABELS"
    - "DoctorForm drops kategorie field; replaces free-text fachrichtung with <select> over FACHRICHTUNG_OPTIONS; adds arztNr input; renames website → profilUrl"
  artifacts:
    - path: "src/app/(admin)/admin/aerzte/import/page.tsx"
      provides: "Admin RSC page with upload form + token-driven preview render"
      contains: "force-dynamic"
    - path: "src/app/(admin)/admin/aerzte/import/ImportPreview.tsx"
      provides: "Summary + conflicts table + commit form"
      contains: "commitCsvImportForm"
    - path: "src/app/(admin)/admin/aerzte/page.tsx"
      contains: "Geocode next 200"
      provides: "Geocoder button + counter + CSV importieren link"
  key_links:
    - from: "src/app/(admin)/admin/aerzte/import/page.tsx"
      to: "src/lib/admin/doctors-import-actions.ts"
      via: "import { parseAndPreviewCsvForm, commitCsvImportForm }"
      pattern: "parseAndPreviewCsvForm"
    - from: "src/app/(admin)/admin/aerzte/page.tsx"
      to: "src/lib/admin/doctors-import-actions.ts"
      via: "import { geocodeBatchForm }"
      pattern: "geocodeBatchForm"
    - from: "src/app/(admin)/admin/aerzte/DoctorForm.tsx"
      to: "src/lib/admin/import/fachrichtung-mapping.ts"
      via: "import { FACHRICHTUNG_OPTIONS }"
      pattern: "FACHRICHTUNG_OPTIONS"
---

## Objective

<objective>
Ship the admin UI for Phase 47: the new `/admin/aerzte/import` route (file upload + preview + commit) and the updates to the existing `/admin/aerzte` list page + DoctorForm + DoctorRow + DoctorFilters to match the new schema (drop kategorie, add Fachrichtung select, add arztNr input, rename website → profilUrl, add geocoder button + progress counter).

Purpose: This is the operator-facing surface for the CSV import + batch geocode workflow. Plan 47-03 built the headless actions; this plan wires them to forms an editor can click. Phase 46's admin UI patterns (`--dir-*` design tokens, `<details>` two-step confirms, Server-Component-first, no requireAuth() in pages) carry forward unchanged.

Output: 6 files modified/created; admin layout still gates the route group; all forms post to Plan 47-03's Form wrappers.

**Plan-level autonomous=false:** The new import page + the geocoder button button warrant a human checkpoint after build (last task) so the operator can smoke-test the upload → preview → commit flow before downstream Wave 6 work consumes it.

**Cross-plan TSC consistency:** This plan (47-04) and its sibling 47-05 run in the same wave (Wave 5) and may both have in-flight edits when individual `tsc --noEmit` runs are invoked. Full-suite TSC consistency across both plans is enforced by Plan 47-06's full-suite tsc gate (Wave 6). Task 2's verify below uses a project-scoped `npx tsc --noEmit | tail -20` so the operator can read any errors and decide whether they are 47-05-in-flight noise or genuine 47-04 regressions; per-file filename filters were removed because grep-on-filename is unreliable for type errors (errors reference imported types, not just the modified file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/47-aerzteverzeichnis-vollkatalog-und-csv-import/47-CONTEXT.md
@.planning/phases/47-aerzteverzeichnis-vollkatalog-und-csv-import/47-RESEARCH.md
@.planning/phases/47-aerzteverzeichnis-vollkatalog-und-csv-import/47-PATTERNS.md
@.planning/phases/46-aerzteverzeichnis/DESIGN.md
@src/app/(admin)/admin/aerzte/page.tsx
@src/app/(admin)/admin/aerzte/DoctorFilters.tsx
@src/app/(admin)/admin/aerzte/DoctorRow.tsx
@src/app/(admin)/admin/aerzte/DoctorForm.tsx
@src/app/(admin)/admin/aerzte/new/page.tsx
@src/app/(admin)/layout.tsx
@src/lib/admin/import/fachrichtung-mapping.ts
@src/lib/admin/import/preview-cache.ts
@src/lib/admin/doctors-import-actions.ts
@src/lib/content/doctors.ts
</context>

## Tasks

<tasks>

<task type="auto">
  <name>Task 1: Build /admin/aerzte/import page + ImportPreview component</name>
  <read_first>
    - src/app/(admin)/admin/aerzte/new/page.tsx (analog — thin RSC + form action, "← Zurück" link, h1 + dynamic = 'force-dynamic')
    - src/app/(admin)/admin/aerzte/DoctorRow.tsx (analog — `<details>` two-step confirm pattern + --dir-* token usage)
    - .planning/phases/47-aerzteverzeichnis-vollkatalog-und-csv-import/47-PATTERNS.md (§ import/page.tsx and § import/ImportPreview.tsx — full pattern descriptions)
    - .planning/phases/47-aerzteverzeichnis-vollkatalog-und-csv-import/47-CONTEXT.md (D-13, D-14, D-15 — import UI flow)
    - .planning/phases/47-aerzteverzeichnis-vollkatalog-und-csv-import/47-RESEARCH.md (§ Open Questions — German error tone for "Vorschau abgelaufen")
    - src/lib/admin/doctors-import-actions.ts (parseAndPreviewCsvForm + commitCsvImportForm signatures; also export `getPreview` re-exported from preview-cache OR import directly)
  </read_first>
  <action>
    Create `src/app/(admin)/admin/aerzte/import/page.tsx` as a Server Component:
    - First line `export const dynamic = 'force-dynamic'` (AGENTS.md).
    - Default async function `ImportPage({ searchParams })`. Read `const token = searchParams?.token` (Next.js 15: `searchParams` may be a Promise — use `const sp = await searchParams; const token = sp?.token` if so).
    - Header section: "← Zurück" link to `/admin/aerzte`, h1 `"CSV importieren"` with classes from new/page.tsx.
    - Upload form: `<form action={parseAndPreviewCsvForm} encType="multipart/form-data"><input type="file" name="file" accept=".csv" required /><button type="submit">Vorschau erzeugen</button></form>`. Use `--dir-*` tokens for button styling matching Phase 46 admin buttons.
    - If `token` is set: import `getPreview` from `@/lib/admin/import/preview-cache`; `const cached = getPreview(token)`. If `cached === null`: render German error `"Vorschau abgelaufen — bitte erneut hochladen"` inside a `bg-dir-error-container` chip. If cached: render `<ImportPreview token={token} summary={...} conflicts={cached.conflicts} />`.
    - Compute summary from cached.rows: `newRows = rows.filter(r => !r.isUpdate).length`, `updateRows = rows.length - newRows`, `totalRows = rows.length`, `conflictCount = cached.conflicts.length`.

    Create `src/app/(admin)/admin/aerzte/import/ImportPreview.tsx` as a Server Component (NO `'use client'`):
    - Props: `{ token: string; summary: { totalRows, newRows, updateRows, conflictCount }; conflicts: RowConflict[] }`.
    - Summary line: `"{totalRows} Zeilen: {newRows} neu, {updateRows} Updates, {conflictCount} Konflikte"`. Wrap in `bg-dir-tertiary-container` chip for success-y feel (matches DoctorRow's `Verifiziert` chip pattern).
    - Conflicts table: only render if `conflicts.length > 0`. Wrap in `<details>` (JS-less expand pattern from DoctorRow:82-100) with `<summary>"{conflictCount} Konflikte anzeigen"</summary>`. Inside: `<table>` with columns "Zeile" (csvLineNumber), "ArztNr", "Schweregrad" (error/warning chip), "Grund" (reason). Use `bg-dir-error-container` for error rows, `bg-dir-surface-variant` for warning rows.
    - Commit form: `<form action={commitCsvImportForm}><input type="hidden" name="token" value={token} /><details><summary class="...destructive button styling...">Import bestätigen ({newRows + updateRows} Zeilen)</summary><button type="submit">Wirklich importieren</button></details></form>` — two-step confirm via `<details>` per DoctorRow analog.
  </action>
  <verify>
    <automated>[ -f "src/app/(admin)/admin/aerzte/import/page.tsx" ] && [ -f "src/app/(admin)/admin/aerzte/import/ImportPreview.tsx" ] && grep -q "parseAndPreviewCsvForm" "src/app/(admin)/admin/aerzte/import/page.tsx" && grep -q "commitCsvImportForm" "src/app/(admin)/admin/aerzte/import/ImportPreview.tsx" && grep -q "force-dynamic" "src/app/(admin)/admin/aerzte/import/page.tsx" && grep -q "&lt;datalist\|input type=\"file\"" "src/app/(admin)/admin/aerzte/import/page.tsx"</automated>
  </verify>
  <done>
    Both files exist; page exports default async ImportPage; export const dynamic = 'force-dynamic' at top; form action references parseAndPreviewCsvForm; ImportPreview imports commitCsvImportForm; conflicts table renders inside `<details>`; `<form encType="multipart/form-data">` present for file upload.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update /admin/aerzte page — drop kategorie filter, add geocoder counter + button + CSV importieren link</name>
  <read_first>
    - src/app/(admin)/admin/aerzte/page.tsx (full file — current header, parseKategorie helper, listDoctors call, link bar)
    - src/lib/content/doctors.ts (just-updated DAL — listDoctors no longer accepts kategorie)
    - src/lib/admin/doctors-import-actions.ts (geocodeBatchForm signature)
    - .planning/phases/47-aerzteverzeichnis-vollkatalog-und-csv-import/47-PATTERNS.md (§ admin/aerzte/page.tsx — exact deviations)
    - .planning/phases/47-aerzteverzeichnis-vollkatalog-und-csv-import/47-CONTEXT.md (D-22 — counter format)
    - prisma/schema.prisma (post-Plan-47-01 — Doctor has no kategorie)
  </read_first>
  <action>
    Edit `src/app/(admin)/admin/aerzte/page.tsx`:
    - DELETE the `parseKategorie` helper (current lines ~25-30).
    - DELETE the `const kategorie = parseKategorie(searchParams?.kategorie)` extraction (~line 44) and the `kategorie` arg to `listDoctors({ ... })` (~line 51). The DAL signature change from Plan 47-01 already removed it.
    - Add a counter section in the header (after h1, before action bar). Compute via:
      ```typescript
      import { prisma } from '@/lib/prisma'
      const [total, geocoded] = await Promise.all([
        prisma.doctor.count(),
        prisma.doctor.count({ where: { NOT: { lat: null } } }),
      ])
      const pending = total - geocoded
      ```
      Render: `<p class="text-sm text-dir-on-surface-variant">{geocoded} von {total} Ärzte geocoded ({pending} ausstehend)</p>` (D-22 wording).
    - Add a "Geocode next 200" button: `<form action={geocodeBatchForm}><button type="submit" disabled={pending === 0}>Geocode next 200 (~4 min)</button></form>` — use `--dir-*` button styling. If `pending === 0`, disable + show muted text.
    - Add a "CSV importieren" link next to the existing "Neuer Arzt" link: `<Link href="/admin/aerzte/import">CSV importieren</Link>` with same Tailwind classes as the "Neuer Arzt" link.
    - If `searchParams?.imported` is present (commit-redirect target), show a success banner: `bg-dir-tertiary-container` chip "{imported} Ärzte importiert".
    - If `searchParams?.geocoded` is present, show similar banner: "{geocoded} Ärzte geocoded".
    - Keep `export const dynamic = 'force-dynamic'` and `requireAuth-via-layout` (no per-page requireAuth — Phase 46 D-40).
  </action>
  <verify>
    <automated>! grep -q "kategorie" "src/app/(admin)/admin/aerzte/page.tsx" && grep -q "geocodeBatchForm" "src/app/(admin)/admin/aerzte/page.tsx" && grep -q "CSV importieren" "src/app/(admin)/admin/aerzte/page.tsx" && grep -q "von .* Ärzte geocoded" "src/app/(admin)/admin/aerzte/page.tsx" && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    `/admin/aerzte` page no longer references kategorie; renders progress counter using live DB count; has `Geocode next 200` button posting to geocodeBatchForm; has "CSV importieren" link; `tsc --noEmit` output inspected — any errors traceable to 47-04 fixed here, any 47-05 mid-edit noise deferred to Plan 47-06's full-suite gate (Wave 6). Per-file tsc filename filters removed because tsc errors reference imported types, not just modified files; whole-project output inspection by the operator is the gate.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update DoctorFilters + DoctorRow + DoctorForm — drop kategorie, add Fachrichtung select, add arztNr, rename website → profilUrl</name>
  <read_first>
    - src/app/(admin)/admin/aerzte/DoctorFilters.tsx (current kategorie chip row + free-text fachrichtung input)
    - src/app/(admin)/admin/aerzte/DoctorRow.tsx (current kategorieLabel import + fachrichtung guard)
    - src/app/(admin)/admin/aerzte/DoctorForm.tsx (current kategorie + free-text fachrichtung + website fields)
    - src/lib/admin/import/fachrichtung-mapping.ts (FACHRICHTUNG_OPTIONS, FACHRICHTUNG_LABELS signatures)
    - .planning/phases/47-aerzteverzeichnis-vollkatalog-und-csv-import/47-PATTERNS.md (§ DoctorFilters, § DoctorRow, § DoctorForm — exact deviations + new field shapes)
    - .planning/phases/47-aerzteverzeichnis-vollkatalog-und-csv-import/47-CONTEXT.md (D-02, D-03, D-04 — schema)
  </read_first>
  <action>
    **DoctorFilters.tsx:**
    - REMOVE `KATEGORIE_OPTIONS` constant and the chip-row block rendering it.
    - REMOVE `kategorie?: DoctorKategorie` from `active` prop type.
    - ADD a Fachrichtung row using `<select>` (admin uses select not datalist — admin doesn't need search-as-you-type for the focused admin workflow; public uses datalist per D-25). Shape: `<select name="fachrichtung" defaultValue={active.fachrichtung ?? ''}><option value="">Alle Fachrichtungen</option>{FACHRICHTUNG_OPTIONS.map(o => <option value={o.id}>{o.label}</option>)}</select>`. Submit on change OR keep the existing submit pattern (whatever DoctorFilters does for bezirk filter — match it).
    - Import `FACHRICHTUNG_OPTIONS` from `@/lib/admin/import/fachrichtung-mapping`.

    **DoctorRow.tsx:**
    - REMOVE `import { kategorieLabel } from '@/lib/reader/doctor-metadata'` (function deleted in Plan 47-05).
    - REPLACE `{kategorieLabel(doctor.kategorie)}{fachrichtungLine}` with `{FACHRICHTUNG_LABELS[doctor.fachrichtung]}`.
    - DELETE `fachrichtungLine` ternary logic.
    - Optionally show arztNr as small monospaced chip: `<span class="font-mono text-xs text-dir-on-surface-variant">{doctor.arztNr}</span>`.
    - Replace any `doctor.website` reference with `doctor.profilUrl` (if DoctorRow renders it — likely it doesn't, but verify).
    - Import `FACHRICHTUNG_LABELS` from `@/lib/admin/import/fachrichtung-mapping`.

    **DoctorForm.tsx:**
    - DELETE the "Kategorie" select field block.
    - REPLACE the free-text "Fachrichtung" `<input type="text">` with `<select name="fachrichtung" required defaultValue={doctor?.fachrichtung ?? ''}><option value="" disabled>Fachrichtung wählen…</option>{FACHRICHTUNG_OPTIONS.map(o => <option value={o.id}>{o.label}</option>)}</select>`.
    - ADD an "ArztNr" `<input type="text" name="arztNr" required defaultValue={doctor?.arztNr ?? ''}>` field above Name (natural key; required for manual creates).
    - RENAME the "Website" field: label "Profil-URL (Ärztekammer)", id/name `profilUrl`, defaultValue `doctor?.profilUrl ?? ''`, placeholder `"https://www.aekstmk.or.at/aerztesuche-46?arztnr=..."`.
    - Keep Server Component (no `'use client'`); keep `formAction` prop pattern.
    - Import `FACHRICHTUNG_OPTIONS` from `@/lib/admin/import/fachrichtung-mapping`.
  </action>
  <verify>
    <automated>! grep -q "kategorie" "src/app/(admin)/admin/aerzte/DoctorFilters.tsx" "src/app/(admin)/admin/aerzte/DoctorRow.tsx" "src/app/(admin)/admin/aerzte/DoctorForm.tsx" && ! grep -q "kategorieLabel" "src/app/(admin)/admin/aerzte/DoctorRow.tsx" && grep -q "FACHRICHTUNG_OPTIONS" "src/app/(admin)/admin/aerzte/DoctorForm.tsx" && grep -q "FACHRICHTUNG_OPTIONS" "src/app/(admin)/admin/aerzte/DoctorFilters.tsx" && grep -q "FACHRICHTUNG_LABELS" "src/app/(admin)/admin/aerzte/DoctorRow.tsx" && grep -q 'name="arztNr"' "src/app/(admin)/admin/aerzte/DoctorForm.tsx" && grep -q 'name="profilUrl"' "src/app/(admin)/admin/aerzte/DoctorForm.tsx" && ! grep -q "website" "src/app/(admin)/admin/aerzte/DoctorForm.tsx"</automated>
  </verify>
  <done>
    Zero `kategorie` references in any of the 3 admin component files; FACHRICHTUNG_OPTIONS used in Filters + Form; FACHRICHTUNG_LABELS used in Row; arztNr input present in Form; profilUrl input replaces website in Form; no `website` references remain.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Smoke-test admin import + geocoder UI</name>
  <action>Pause for operator smoke-test of the admin import + geocoder flow. See `<how-to-verify>` below for the 9-step manual checklist.</action>
  <verify><human-check>Operator approval per `<how-to-verify>` steps 1-9.</human-check></verify>
  <done>Operator responds `approved` per `<resume-signal>`.</done>
    <what-built>
    The full admin UI for Phase 47 is in place:
    - `/admin/aerzte/import` accepts CSV uploads, shows preview, commits to DB
    - `/admin/aerzte` shows geocoder progress counter + "Geocode next 200" button + "CSV importieren" link
    - Doctor list / row / form use the new Fachrichtung enum + arztNr + profilUrl shapes
  </what-built>
  <how-to-verify>
    1. `npm run dev` — start local Next.js dev server.
    2. Visit `/admin/aerzte` (log in if needed via `/admin/login`). Confirm header shows `"0 von 0 Ärzte geocoded (0 ausstehend)"` (clean DB after Plan 47-01 TRUNCATE) and the `Geocode next 200` button is disabled (pending === 0).
    3. Click `CSV importieren` → land on `/admin/aerzte/import`. Confirm h1 "CSV importieren" and a file upload form.
    4. Upload `test/fixtures/aerzte-sample.csv`. Confirm URL becomes `/admin/aerzte/import?token=<uuid>` and preview shows "10 Zeilen: 10 neu, 0 Updates, 0 Konflikte" in a green chip.
    5. Click `Import bestätigen (10 Zeilen)` → click `Wirklich importieren`. Confirm redirect to `/admin/aerzte?imported=10` with success banner.
    6. Back on `/admin/aerzte`, confirm: counter now "0 von 10 Ärzte geocoded (10 ausstehend)"; `Geocode next 200` button now enabled.
    7. Click the geocoder button. Wait for completion (≤ 4 min for 10 doctors). Confirm counter updates to "10 von 10 Ärzte geocoded (0 ausstehend)" and success banner "10 Ärzte geocoded".
    8. Click a doctor in the list → open edit form. Confirm: arztNr text input is filled; Fachrichtung is a `<select>` not a free-text input; Profil-URL field is labeled "Profil-URL (Ärztekammer)".
    9. Test idempotency: re-upload the same CSV. Confirm preview shows "10 Zeilen: 0 neu, 10 Updates, 0 Konflikte". Commit. Counter stays at 10/10 (no geo-clearing because addresses unchanged per D-17 + Pitfall 7).
  </how-to-verify>
  <acceptance_criteria>
    All 9 verification steps pass. Operator confirms the German UI tone matches Phase 46 admin pages. If steps 1-9 pass: respond `approved`. If anything is broken: respond with the failing step + symptoms.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the failing step(s).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `browser → /admin/* RSC` | (admin)/layout gate via verifySessionCookie — Phase 46 D-40 |
| `browser → form action (Server Action)` | Server Action calls requireAuth() FIRST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-04-AUTH | Spoofing | /admin/aerzte/import + child Server Actions | mitigate | (admin)/layout.tsx route-group gate + per-action requireAuth() (defence-in-depth). Per Phase 46 D-40 confirmed pattern. |
| T-47-04-CSRF | Tampering | File upload + commit forms | mitigate | Next.js Server Actions enforce same-origin POST + signed action ID — built-in CSRF defence. Public site cannot trigger admin actions. |
| T-47-04-XSS | Tampering | German error messages in preview UI | mitigate | React auto-escapes; no `dangerouslySetInnerHTML`. Conflict reasons are server-generated strings (no user-injected HTML). |
| T-47-04-FILE-SIZE | DoS | File upload | accept | Vercel default 4.5 MB body limit; 3,577-row CSV ≈ 600 KB. Documented in RESEARCH § Security Domain. |
</threat_model>

<verification>
After all four tasks complete:
1. `npx tsc --noEmit` — clean (full project; cross-plan TSC consistency between 47-04 and 47-05 is the responsibility of Plan 47-06's gate, but Task 2's project-scoped output should be sane at this point)
2. `npm run build` — succeeds (catches dynamic-import + type errors)
3. Operator smoke-test (Task 4) approved
4. `grep -r "kategorie" "src/app/(admin)/admin/aerzte/" | grep -v "Plan\|CSV"` returns 0 matches
</verification>

<success_criteria>
- /admin/aerzte/import page exists with file upload + token-driven preview render + commit form
- /admin/aerzte page has geocoder counter + Geocode button + CSV importieren link
- DoctorFilters, DoctorRow, DoctorForm all use FACHRICHTUNG_OPTIONS / FACHRICHTUNG_LABELS; no kategorie; arztNr input in Form; profilUrl replaces website
- Operator smoke-test (Task 4) approved end-to-end (upload → preview → commit → geocode → verify)
- tsc + build clean
</success_criteria>

<output>
Create `.planning/phases/47-aerzteverzeichnis-vollkatalog-und-csv-import/47-04-SUMMARY.md` when done. Cover:
- Confirmation of operator smoke-test approval + any UX tweaks made during testing
- Time taken by the geocoder for 10 rows (validates real Nominatim cadence ≈ 11s minimum)
- Any unexpected Next.js searchParams handling drift (Next 15+ changed it to Promise)
- Note for Plan 47-05: public UI changes can now safely consume the same FACHRICHTUNG_LABELS / FACHRICHTUNG_OPTIONS without conflict
</output>
</output>
