---
phase: 47-aerzteverzeichnis-vollkatalog-und-csv-import
plan: 06
type: execute
wave: 6
depends_on:
  - 47-04
  - 47-05
files_modified:
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - DIR-14
  - DIR-15
  - DIR-16
  - DIR-17
  - DIR-18
  - DIR-19
  - DIR-20
  - DIR-21
  - DIR-22
  - DIR-23
  - DIR-24
  - DIR-25
  - DIR-26
  - DIR-27
  - DIR-28
  - DIR-29
  - DIR-30
  - DIR-31
tags: [integration, smoke, verification, state-update]
user_setup: []

must_haves:
  truths:
    - "Full vitest suite (npm test) is green"
    - "npx tsc --noEmit is clean across the entire project"
    - "npm run build succeeds (Vercel-equivalent build)"
    - "/admin/aerzte/import end-to-end manual smoke passes (upload → preview → commit → geocode → re-import idempotency)"
    - "All 18 DIR-14..DIR-31 requirements verified via their VALIDATION.md test commands"
    - "STATE.md updated with Phase 47 completion entry"
  artifacts:
    - path: ".planning/STATE.md"
      contains: "Phase 47"
      provides: "Project-state record of Phase 47 closure"
  key_links: []
---

## Objective

<objective>
Final integration check + state record for Phase 47. Runs the full test suite, full typecheck, full build, and an end-to-end manual smoke on the import + geocoder flow. Then updates `.planning/STATE.md` to record phase closure.

Purpose: One last gate before declaring Phase 47 done. Catches integration regressions between the 6 prior plans (e.g., one plan's edit breaks another's test, or an unmodeled tsc error in an unrelated file due to the schema change).

Output: Confirmed green test suite, clean tsc, successful build, signed-off smoke checklist, STATE.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-aerzteverzeichnis-vollkatalog-und-csv-import/47-CONTEXT.md
@.planning/phases/47-aerzteverzeichnis-vollkatalog-und-csv-import/47-VALIDATION.md
</context>

## Tasks

<tasks>

<task type="auto">
  <name>Task 1: Full-suite verification — vitest + tsc + build</name>
  <read_first>
    - .planning/phases/47-aerzteverzeichnis-vollkatalog-und-csv-import/47-VALIDATION.md (Per-Task Verification Map — confirm all automated checks pass when run individually + as full suite)
    - package.json (verify the "test" and "build" scripts)
  </read_first>
  <action>
    Run sequentially and capture output:

    1. `npm test` — full vitest suite. Must exit 0. If any test fails, halt and investigate (it's likely an integration regression from one of the 5 prior plans).
    2. `npx tsc --noEmit` — must exit 0 with no errors. If errors surface in files not modified by Phase 47 (e.g. pre-existing TSC failures noted in STATE.md "deferred-items.md"), confirm they were already broken before Phase 47 started (`git stash && npx tsc --noEmit` against the pre-phase commit) — if so, they are out of scope per AGENTS.md (no scope creep).
    3. `npm run build` — Next.js build must succeed. This is the closest local proxy to a Vercel deploy and catches dynamic-import errors, env-var issues, and Edge-runtime warnings that vitest misses.

    For each command, capture exit code + tail of output (last 20 lines) for the SUMMARY.

    If `npm run build` fails with `prisma migrate deploy` errors: the local migration applied via `migrate dev` in Plan 47-01, but Vercel build runs `migrate deploy`. Confirm `prisma/migrations/<ts>_phase47_csv_schema/` is committed AND the migration name sorts after `20260515_phase46_doctors`. Document any deploy-script discoveries (the v3.2 STATE.md pending todo about adding `prisma migrate deploy` to the build script may surface here — if it does, NOTE it but do NOT fix in this phase; that's a separate cross-cutting concern).
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5 && npx tsc --noEmit && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    All three commands exit 0; full test count printed; tsc reports no errors; Next.js build produces a `.next/` directory without errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end smoke checklist — admin + public flows</name>
  <action>Pause for operator to run the 14-step end-to-end smoke checklist covering admin + public flows. See `<how-to-verify>` below.</action>
  <verify><human-check>Operator approval per `<how-to-verify>` steps 1-14.</human-check></verify>
  <done>Operator responds `approved` per `<resume-signal>`.</done>
    <what-built>
    All 6 prior plans (47-00 through 47-05) are landed and the full automated suite passes. This checkpoint runs the operator-facing smoke that automated tests can't cover: native browser <datalist>, native <details>/<summary> two-step confirms, end-to-end Server Action chains, JSON-LD rendering in actual HTML output.
  </what-built>
  <how-to-verify>
    Start `npm run dev`. Run these steps in a single browser session (Chrome or Firefox):

    **Admin flow:**
    1. Visit `/admin/aerzte` (log in if needed). Confirm counter "0 von 0 Ärzte geocoded (0 ausstehend)"; `Geocode next 200` button disabled; `CSV importieren` and `Neuer Arzt` links visible.
    2. Click `CSV importieren` → `/admin/aerzte/import`. Confirm h1 "CSV importieren" + file upload form.
    3. Upload `test/fixtures/aerzte-sample.csv`. Confirm preview: "10 Zeilen: 10 neu, 0 Updates, 0 Konflikte" in green chip. No conflicts table shown.
    4. Click `Import bestätigen` (first click reveals `Wirklich importieren`), then click `Wirklich importieren`. Redirect to `/admin/aerzte?imported=10`; green banner "10 Ärzte importiert"; counter "0 von 10 Ärzte geocoded (10 ausstehend)"; button enabled.
    5. Click `Geocode next 200 (~4 min)`. Wait. Confirm completion (≤ 11 × 1.1s + map fetch ≈ 30s for 10 rows). Counter updates to "10 von 10 Ärzte geocoded (0 ausstehend)"; green banner "10 Ärzte geocoded"; button disabled again.
    6. Click any doctor in the list → edit form. Confirm: `arztNr` field filled (e.g. `A0001`); `Fachrichtung` is a `<select>` not a free-text input; `Profil-URL (Ärztekammer)` field present; no Kategorie field.

    **Public flow:**
    7. Visit `/aerzte` (log out first, or use incognito). Confirm filter UI: Bezirk chips (13), Fachrichtung input field with "Fachrichtung wählen…" placeholder. NO kategorie chips.
    8. Click into the Fachrichtung input. Type "Aug". Confirm browser-native autocomplete shows "Augenheilkunde und Optometrie".
    9. Select "Augenheilkunde und Optometrie". Tab out to trigger onBlur. URL should update to `/aerzte?fachrichtung=AUGENHEILKUNDE_UND_OPTOMETRIE`. List filters accordingly.
    10. Clear the input. URL drops the param. List shows all 10.
    11. Click a doctor → detail page. Confirm: address shown; map pin visible (geocoder ran); editorial section shows "Angaben ohne Gewähr" disclaimer; if profilUrl present, a "Profil auf aekstmk.or.at" link with target=_blank.
    12. View source of detail page; search for `"@type":"Physician"`. Confirm present. Confirm `"medicalSpecialty"` is the German label (e.g. "Allgemeinmedizin"), NOT the enum identifier. Confirm `"sameAs":["https://www.aekstmk.or.at/..."]` if profilUrl set. NO `"Dentist"`.

    **Idempotency:**
    13. Back to admin. Re-upload the same CSV. Preview should show "10 Zeilen: 0 neu, 10 Updates, 0 Konflikte". Commit. Counter stays "10 von 10 Ärzte geocoded" (no geo-clearing because addresses unchanged — D-17 + Pitfall 7).
    14. Manually edit one fixture row's address externally (e.g. `sed -i '' 's/Hauptplatz 1/Hauptplatz 99/' test/fixtures/aerzte-sample.csv`). Re-upload + commit. Counter should drop to "9 von 10 Ärzte geocoded (1 ausstehend)" — the address-change geo-clearing fired for exactly 1 row. (Revert the fixture edit after testing: `git checkout -- test/fixtures/aerzte-sample.csv`.)
  </how-to-verify>
  <acceptance_criteria>
    All 14 steps pass. If any step fails: report failing step + symptoms + suspected plan that introduced the regression. If all pass: respond `approved`.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the failing step(s).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md with Phase 47 closure entry</name>
  <read_first>
    - .planning/STATE.md (current state file — match the existing entry format for Phase 46)
    - .planning/REQUIREMENTS.md (just-updated — confirms all DIR-14..DIR-31 are pending or complete)
  </read_first>
  <action>
    Edit `.planning/STATE.md`:

    1. **Frontmatter:** update `current_plan: 6 (Phase 47 complete)`; `status: complete`; `stopped_at: Phase 47 shipped`; `last_updated: <today's ISO>`; `last_activity: <today>`; bump `progress.completed_phases` by 1 (now 8 of 9); bump `progress.completed_plans` by 7 (now 28 of 31 — Phase 47 = 7 plans); `percent: 90` (28/31 ≈ 90%).
    2. **Current Position section:** update to "Phase 47 — Aerzteverzeichnis Vollkatalog und CSV-Import — COMPLETE". Note: all 18 DIR-14..DIR-31 requirements complete; 7/7 plans shipped; end-to-end smoke approved.
    3. **Progress bar:** update to "Phase 47 complete (7/7 plans shipped)".
    4. **Performance Metrics:** add row to the in-flight table for each Phase 47 plan: `| Phase 47 P00 | <duration if known> | 4 tasks | 4 files |`, etc. (Or skip if durations aren't tracked — match Phase 46's table style.)
    5. **Accumulated Context > Decisions section:** add new entries summarizing key Phase 47 decisions:
       - `[Phase 47-csv-import]: papaparse selected as CSV parser (multi-line quoted values D-07); single new runtime dep, DECISIONS.md entry recorded per AGENTS.md anti-bloat rule`
       - `[Phase 47-csv-import]: Fachrichtung enum minted as closed 51-value Prisma enum; identifiers derived via uppercase + Ä→AE / Ö→OE / Ü→UE / ß→SS / strip .,() / space-dash→_ transform; collision-free over the 51 source labels`
       - `[Phase 47-csv-import]: Doctor table TRUNCATEd in migration (D-12 wipe-and-reload); Phase 46 shipped with test/seed data only, no production-critical rows`
       - `[Phase 47-csv-import]: In-memory PREVIEW_CACHE Map keyed by crypto.randomUUID() — single-editor workflow tolerates Vercel cold-start cache loss; user re-uploads on expiry (D-15)`
       - `[Phase 47-csv-import]: Admin-triggered batch geocoder (200 doctors / click) replaces background cron — Vercel Hobby 1-cron/day already consumed by ingest. 18 clicks for 3,577 doctors; one-time seeding burden per D-22`
       - `[Phase 47-csv-import]: JSON-LD always @type='Physician' (Dentist branch deleted — no dentists in source CSV); medicalSpecialty populated from FACHRICHTUNG_LABELS unconditionally (fachrichtung now NOT NULL)`
       - `[Phase 47-csv-import]: Public Fachrichtung filter uses HTML5 <datalist> — native browser autocomplete over 51 options, no JS framework added (D-25)`
       - `[Phase 47-csv-import]: profilUrl open-redirect risk deferred to future phase — admin-controlled source (trusted CSV); rel="noopener noreferrer" + target=_blank mitigates window-opener attacks`
    6. **Session Continuity > Resume with:** update suggestions to point to Phase 44 replan (parked) or Phase 45 (REVIEW Heuristic & Quality Loop) or v3.4 milestone bootstrap, since v3.3 Phase 47 is now done.
    7. **Update REQUIREMENTS.md traceability table:** flip DIR-14..DIR-31 status from `Pending` to `Complete`.
  </action>
  <verify>
    <automated>grep -c "Phase 47" .planning/STATE.md | awk '{ if ($1 >= 3) exit 0; else exit 1 }' && grep -c "Phase 47" .planning/REQUIREMENTS.md | awk '{ if ($1 >= 18) exit 0; else exit 1 }'</automated>
  </verify>
  <done>
    STATE.md frontmatter reflects Phase 47 completion; Current Position updated; Decisions section appended with 8 Phase 47 entries; REQUIREMENTS.md traceability shows DIR-14..DIR-31 as Complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new — this plan only runs verification commands and updates STATE.md) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-06-STATE | Tampering | STATE.md drift | accept | Git-tracked; reviewed in commit before push. |
| T-47-06-SMOKE-FALSE | Spoofing | Operator marks smoke approved without testing | accept | Trust the operator; checkpoint cannot be auto-approved (workflow.auto_advance ignored for human-verify). Approval is logged in chat transcript. |
</threat_model>

<verification>
After all three tasks:
1. `npm test` — green
2. `npx tsc --noEmit` — clean
3. `npm run build` — succeeds
4. Smoke checklist (Task 2) approved
5. STATE.md updated; REQUIREMENTS.md DIR-14..DIR-31 marked Complete
</verification>

<success_criteria>
- Full test suite green; tsc clean; build succeeds
- 14-step end-to-end smoke checklist approved by operator
- STATE.md frontmatter shows Phase 47 complete; Decisions section appended; REQUIREMENTS.md updated
- Phase 47 ready for git commit + closure
</success_criteria>

<output>
Create `.planning/phases/47-aerzteverzeichnis-vollkatalog-und-csv-import/47-06-SUMMARY.md` when done. Cover:
- Final test count (vitest output's "Tests passed: X")
- tsc + build exit codes
- Operator smoke-checklist approval timestamp
- Any deviations noted during smoke (small UX glitches, accessibility observations, etc.)
- STATE.md diff summary (what frontmatter changed, what decisions were appended)
- Suggested next-session resume points (Phase 44 replan / Phase 45 / v3.4 bootstrap)
</output>
