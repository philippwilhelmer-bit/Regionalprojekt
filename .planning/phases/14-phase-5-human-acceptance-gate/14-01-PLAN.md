---
phase: 14-phase-5-human-acceptance-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-editorial-cms/05-VERIFICATION.md
  - .planning/phases/14-phase-5-human-acceptance-gate/14-SIGN-OFF.md
  - .planning/phases/14-phase-5-human-acceptance-gate/14-01-SUMMARY.md
autonomous: false
requirements: [CMS-01, CMS-02, CMS-03, CMS-04, AICONF-01, AICONF-02, AICONF-03]

must_haves:
  truths:
    - "Auth gate blocks unauthenticated access to /admin/* and login flow works end-to-end"
    - "Auth gap is confirmed closed — direct POST without session cookie is rejected"
    - "Manual article creation produces MANUAL/PUBLISHED article visible in admin list"
    - "Article list filters (Bezirk, source, status, date) work responsively with 200+ articles"
    - "Exception queue approve sets PUBLISHED, reject sets REJECTED, side-by-side layout is readable"
    - "Source CRUD works — add creates healthStatus=OK, edit persists, disable sets enabled=false"
    - "Global AI config tone change persists in DB and affects pipeline output"
    - "Per-source AI override creates/deletes AiSourceConfig row correctly"
  artifacts:
    - path: ".planning/phases/05-editorial-cms/05-VERIFICATION.md"
      provides: "Updated verification report with status: verified"
      contains: "status: verified"
    - path: ".planning/phases/14-phase-5-human-acceptance-gate/14-SIGN-OFF.md"
      provides: "Full test results for all 8 tests with pass/fail and fix log"
      contains: "SIGN-OFF"
  key_links:
    - from: "14-SIGN-OFF.md"
      to: "05-VERIFICATION.md"
      via: "Sign-off updates verification status"
      pattern: "status: verified"
---

<objective>
Formally verify the 7 deferred CMS human verification flows from Phase 5 plus the auth gap exploit test against a running application, producing sign-off artifacts.

Purpose: Phase 5 Editorial CMS has 23/23 automated truths verified but 7 flows require human confirmation in a live environment. This phase closes that gap so the CMS can be declared fully verified.

Output: Updated 05-VERIFICATION.md (status: verified) + 14-SIGN-OFF.md (detailed test results)
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-phase-5-human-acceptance-gate/14-CONTEXT.md
@.planning/phases/14-phase-5-human-acceptance-gate/14-RESEARCH.md
@.planning/phases/05-editorial-cms/05-VERIFICATION.md

<interfaces>
<!-- Key code locations for the walkthrough -->

Auth check: src/lib/admin/auth-node.ts — requireAuth() reads session cookie, redirects to login if invalid
Middleware: middleware.ts — guards /admin/:path*, skips /admin/login
Articles actions: src/lib/admin/articles-actions.ts — createManualArticleForm, toggle/softDelete wrappers
Exceptions actions: src/lib/admin/exceptions-actions.ts — approveArticleForm, rejectArticleForm
Sources actions: src/lib/admin/sources-actions.ts — createSourceForm, updateSourceForm
AI config actions: src/lib/admin/ai-config-actions.ts — upsertAiConfigAction, upsertAiSourceConfigAction, deleteAiSourceConfigAction
AI config DAL: src/lib/admin/ai-config-dal.ts — getAiConfig(), getResolvedAiConfig(), upsertAiSourceConfig(), deleteAiSourceConfig()
Pipeline entry: src/scripts/ai-run.ts — bun run src/scripts/ai-run.ts
Seed utility: src/test/setup-db.ts — seedBulkArticles(), cleanDb() FK-safe deletion order
Step 2 prompt: src/lib/ai/steps/step2-write.ts — buildSystemPrompt(config, bezirkContext) reads AiConfig.tone
Bundesland config: bundesland.config.ts — 13 Bezirke for Steiermark
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Guided walkthrough of 8 CMS acceptance tests</name>
  <files>n/a — human verification task, no files modified</files>
  <action>
Claude runs a guided walkthrough of 8 acceptance tests with the user. Claude handles setup (DB connectivity, seed data, pre-checks) and the user performs browser actions and confirms visual results. Each test follows: pre-check -> seed if needed -> user action -> Claude DB verification -> record PASS/FAIL.

**Setup (Claude does before any tests):**

1. Ask user for `{APP_URL}` (production/staging URL) and `{DB_CONNECTION_STRING}` (or confirm DATABASE_URL is set)
2. Check DB connectivity using available tool (psql, Prisma CLI, or Bun script — Claude picks at runtime)
3. Check env vars: ADMIN_PASSWORD, ADMIN_SESSION_SECRET are configured (ask user to confirm)
4. Check ANTHROPIC_API_KEY availability (needed for Test 7; if absent, Test 7 adapts to partial verification)
5. Seed test data if prerequisites are missing:
   - 200+ articles across all 13 Bezirke with mixed statuses (PUBLISHED, REVIEW, REJECTED, FAILED, ERROR) for Test 4
   - At least 2 articles with status=REVIEW for Test 5 (exception queue)
   - At least 1 article with status=FETCHED for Test 7 (AI config pipeline trigger)
   - Track all seeded row IDs for cleanup at end

**Test 1: Auth Gate (blocks all other tests)**
- Pre-check: `curl -s -o /dev/null -w "%{http_code}" {APP_URL}/admin/articles` — expect 307 redirect
- User action: Open `{APP_URL}/admin/articles` in browser — confirm redirect to /admin/login
- User action: Enter wrong password — confirm error message "Falsches Passwort" shown
- User action: Enter correct ADMIN_PASSWORD — confirm redirect to /admin/articles with session set
- Pass criteria: All three sub-steps confirmed

**Test 2: Auth Gap Exploit (curl without session)**
- Claude action: `curl -s -o /dev/null -w "%{http_code}" -X GET {APP_URL}/admin/articles` without cookie — expect redirect (302/307)
- Claude action: Attempt to discover a Server Action endpoint ID from page source or .next build output. If discoverable, send POST without session cookie and verify it returns redirect, not 200.
- If action ID not extractable: document as "middleware-level auth confirmed; Server Action direct POST test skipped — action IDs not extractable"
- Pass criteria: Unauthenticated requests are rejected at middleware level; Server Action POST also rejected if testable
- **If exploitable:** STOP — fix by adding requireAuth() to all mutation Server Actions, retest, then continue

**Test 3: Manual Article Creation (CMS-01)**
- Pre-check: Claude queries DB for current article count
- User action: Navigate to `{APP_URL}/admin/articles/new`
- User action: Fill Titel, Inhalt, select at least one Bezirk, submit
- Claude verification: Query DB for latest article — confirm source=MANUAL, status=PUBLISHED, isAutoGenerated=false
- User action: Confirm new article appears in /admin/articles list
- Pass criteria: Article created with correct attributes AND visible in list

**Test 4: Article List Filter Performance (CMS-02)**
- Pre-check: Claude queries DB for article count — must be 200+ (seed if not)
- User action: Open `{APP_URL}/admin/articles`
- User action: Apply Bezirk filter (e.g., "Graz") — confirm results narrow and response feels fast
- User action: Apply status filter (e.g., "PUBLISHED") — confirm correct filtering
- User action: Apply source filter — confirm correct filtering
- Pass criteria: Each filter narrows results correctly; UI responds within ~10 seconds (subjective, must feel responsive for editorial tool)

**Test 5: Exception Queue Approve/Reject (CMS-03)**
- Pre-check: Claude queries DB for REVIEW-status articles — seed if none exist
- User action: Navigate to `{APP_URL}/admin/exceptions`
- User action: Confirm side-by-side layout (rewritten article left, raw source right) is readable without horizontal scroll
- User action: Click "Genehmigen" on one article
- Claude verification: Query DB — confirm article status=PUBLISHED, publishedAt set
- User action: Confirm article removed from exception queue
- User action: Click "Ablehnen" on another article
- Claude verification: Query DB — confirm article status=REJECTED
- Pass criteria: Approve sets PUBLISHED, reject sets REJECTED, both remove from queue, layout is readable

**Test 6: Source Add/Edit/Disable (CMS-04)**
- User action: Navigate to `{APP_URL}/admin/sources/new`
- User action: Add new RSS source (URL: any test URL, type: RSS), submit
- Claude verification: Query DB — confirm Source row exists with healthStatus=OK
- User action: Edit the source — change pollInterval or healthFailureThreshold, save
- Claude verification: Query DB — confirm changes persisted
- User action: Disable the source
- Claude verification: Query DB — confirm enabled=false
- Pass criteria: Create, edit, and disable all work correctly

**Test 7: Global AI Config Settings (AICONF-01/AICONF-03)**
- Pre-check: Claude queries DB for current AiConfig.tone value
- User action: Navigate to `{APP_URL}/admin/ai-config`
- User action: Change Ton to "Formell", save
- Claude verification: Query DB — confirm AiConfig.tone = 'FORMAL'
- User action: Refresh page — confirm "Formell" still selected (persistence confirmed)
- If ANTHROPIC_API_KEY available:
  - Pre-check: Ensure at least 1 FETCHED article exists (seed if not)
  - Claude action: Trigger pipeline via `bun run src/scripts/ai-run.ts` (or equivalent)
  - Claude verification: Check pipeline log output for formal tone instruction in prompt
  - Pass criteria: Tone persists in DB AND pipeline uses updated setting
- If ANTHROPIC_API_KEY unavailable:
  - Record as PARTIAL-PASS: "DB persistence confirmed; pipeline trigger untested — ANTHROPIC_API_KEY not available"

**Test 8: Per-Source AI Override (AICONF-02)**
- User action: In `{APP_URL}/admin/ai-config`, find a source's override section
- User action: Set an articleLength override (e.g., LONG), save
- Claude verification: Query DB — confirm AiSourceConfig row exists with correct sourceId and articleLength
- User action: Remove the override
- Claude verification: Query DB — confirm AiSourceConfig row deleted
- Pass criteria: Override creates row, removal deletes it, source reverts to global defaults

**Cleanup:**
- Claude deletes all seeded test data by tracked IDs, following FK-safe order: articleBezirk -> article -> source (only test-created sources)
- Verify no test data remains

Record each test as PASS, FAIL, or PARTIAL-PASS in running results log.
  </action>
  <verify>User confirms all 8 tests executed and records PASS/FAIL/PARTIAL-PASS for each</verify>
  <done>All 8 tests recorded as PASS or PARTIAL-PASS (no unresolved FAIL), any inline fixes applied and retested, seeded test data cleaned up</done>
</task>

<task type="auto">
  <name>Task 2: Create sign-off artifacts and update verification report</name>
  <files>
    .planning/phases/05-editorial-cms/05-VERIFICATION.md
    .planning/phases/14-phase-5-human-acceptance-gate/14-SIGN-OFF.md
  </files>
  <action>
After all 8 tests have been completed and confirmed:

1. **Create 14-SIGN-OFF.md** in `.planning/phases/14-phase-5-human-acceptance-gate/`:
   - Header: "Phase 5 Editorial CMS — Human Acceptance Sign-Off"
   - Date and verifier attribution: "Philipp + Claude (guided walkthrough)"
   - Test results table with 8 rows: Test name, Requirement ID(s), Result (PASS/FAIL/PARTIAL-PASS), Notes
   - Fix log section: document any inline fixes (what was broken, what was fixed, retest result). If no fixes needed, write "No inline fixes required."
   - Environment section: APP_URL used, DB tool used, pipeline trigger method (if applicable)
   - Conclusion: "All Phase 5 human verification flows confirmed. Phase 5 Editorial CMS is fully verified."

2. **Update 05-VERIFICATION.md** frontmatter:
   - Change `status: human_needed` to `status: verified`
   - Update `score:` line to reflect final score (23/23 if all pass, adjust if any PARTIAL-PASS)
   - Add `human_verified: 2026-XX-XX` with actual date
   - Add `human_verifier: "Philipp + Claude (guided walkthrough)"`
   - For each of the 7 `human_verification` entries in the YAML frontmatter, add a `result:` field with PASS/FAIL/PARTIAL-PASS and a brief note

3. **Commit** both files with message: `docs(14): Phase 5 CMS human acceptance sign-off — all flows verified`
  </action>
  <verify>
    <automated>grep -c "status: verified" .planning/phases/05-editorial-cms/05-VERIFICATION.md | grep -q "1" && test -f .planning/phases/14-phase-5-human-acceptance-gate/14-SIGN-OFF.md && echo "PASS"</automated>
  </verify>
  <done>05-VERIFICATION.md status is "verified", 14-SIGN-OFF.md exists with all 8 test results, both files committed to git</done>
</task>

</tasks>

<verification>
1. 05-VERIFICATION.md has `status: verified` in frontmatter
2. 14-SIGN-OFF.md exists with results for all 8 tests
3. All 7 requirement IDs (CMS-01 through CMS-04, AICONF-01 through AICONF-03) have at least one test covering them
4. Any inline fixes are documented in the fix log
5. Test data cleanup confirmed — no seeded test rows remain
</verification>

<success_criteria>
- All 8 tests recorded as PASS or PARTIAL-PASS (no unresolved FAIL)
- Auth gap confirmed closed (Test 2 PASS)
- 05-VERIFICATION.md updated to status: verified
- 14-SIGN-OFF.md created with full test results and fix log
- Both artifacts committed to git
- All seeded test data cleaned up
</success_criteria>

<output>
After completion, create `.planning/phases/14-phase-5-human-acceptance-gate/14-01-SUMMARY.md`
</output>
