---
phase: 05-editorial-cms
plan: 08
type: execute
wave: 4
depends_on:
  - 05-06
  - 05-07
files_modified: []
autonomous: false
requirements:
  - CMS-01
  - CMS-02
  - CMS-03
  - CMS-04
  - AICONF-01
  - AICONF-02
  - AICONF-03

must_haves:
  truths:
    - "All five Phase 5 success criteria are verifiable in the running app"
    - "Editor can complete the full manual article creation flow end to end"
    - "Editor can approve or reject an exception queue item"
    - "Editor can add a source and change its health failure threshold"
    - "Editor can change global AI settings and confirm they are saved"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Editorial CMS. Confirm all five Phase 5 success criteria are met in the running application.

Purpose: Functional acceptance — these flows cannot be verified by automated test alone.
Output: Confirmed working admin interface across all four sections.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-editorial-cms/05-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Editorial CMS at /admin with four sections:
    - Articles: filterable list with inline pin/feature/soft-delete, manual article creation
    - Exception Queue: REVIEW articles with side-by-side rewritten/raw view, approve/reject
    - Sources: health dashboard per source, add/edit/disable sources
    - AI Config: global AI settings, per-source overrides, pipeline config tunables
    Authentication: login form at /admin/login with ADMIN_PASSWORD env var
  </what-built>
  <how-to-verify>
    Run `npm run dev` (or `bun dev`) and verify these flows:

    1. AUTHENTICATION
       - Visit http://localhost:3000/admin/articles without logging in
       - Confirm redirect to http://localhost:3000/admin/login
       - Enter wrong password → confirm error message shown
       - Enter correct ADMIN_PASSWORD → confirm redirect to /admin/articles

    2. ARTICLES LIST (CMS-02)
       - Confirm article list loads with title, status badge, source type, publishedAt date
       - Apply a filter (e.g. Status = PUBLISHED) → confirm list updates
       - Click Pinnen on any article → confirm isPinned reflected (button label should toggle)
       - Click Loeschen on any article → confirm article disappears from list (status=REJECTED)

    3. NEW ARTICLE (CMS-01)
       - Click "Neuer Artikel" button
       - Fill in Titel, Inhalt, select at least one Bezirk, add SEO-Titel
       - Click Speichern → confirm redirect back to /admin/articles
       - Confirm new article appears in list with status=PUBLISHED, source=MANUAL

    4. EXCEPTION QUEUE (CMS-03)
       - Navigate to /admin/exceptions
       - If no REVIEW articles: confirm empty state message shown
       - If REVIEW articles exist: confirm side-by-side layout (rewritten vs raw source text)
       - Click Genehmigen on one item → confirm it disappears from exception queue
       - Verify approved article appears in /admin/articles with status=PUBLISHED

    5. SOURCES (CMS-04)
       - Navigate to /admin/sources
       - Confirm each source shows: health badge (OK/DEGRADED/DOWN), last success time, run stats, failed count
       - Click "Neue Quelle hinzufuegen" → fill in a test URL and type RSS → save
       - Confirm new source appears in list with healthStatus=OK
       - Edit the new source: change poll interval to 30 minutes, set healthFailureThreshold=5
       - Confirm changes are saved (re-open or re-render shows updated values)
       - Disable the source (enabled toggle off) → confirm badge or style indicates disabled

    6. AI CONFIG (AICONF-01/02/03)
       - Navigate to /admin/ai-config
       - Confirm global settings show current values (NEUTRAL tone, MEDIUM length)
       - Change Ton to "Formell" and click Speichern
       - Refresh page → confirm Ton is still "Formell" (change persisted in DB)
       - In per-source overrides: find a source, click "Override hinzufuegen"
       - Set articleLength to LONG for that source, save
       - Confirm override row appears with LONG value
       - Click "Override entfernen" → confirm override is gone, source falls back to global
       - In Pipeline-Konfiguration: change Dead-Man-Schwelle to 12, save
       - Refresh → confirm 12 is shown

    Phase 5 success criteria (ROADMAP):
    1. Authenticated editor can write, publish, pin, feature, edit, and soft-delete articles
    2. Editor can open exception queue, compare source + rewritten text, approve/reject in one click
    3. Article list is filterable — can locate any article in under 10 seconds
    4. Editor can add/configure/disable RSS sources without touching code
    5. Editor can set global AI settings and per-source overrides — changes take effect on next run
  </how-to-verify>
  <resume-signal>
    Type "approved" when all six flows work correctly.
    If issues found, describe the specific failure (e.g., "Step 3: save button returns 500") and Claude will fix before re-verification.
  </resume-signal>
</task>

</tasks>

<verification>
Human-verified: all six flows complete without errors. Phase 5 success criteria confirmed.
</verification>

<success_criteria>
- Authentication gate works (redirect to login, correct password sets cookie)
- Article list filterable; pin/feature/soft-delete work inline
- Manual article creation produces MANUAL/PUBLISHED article with correct Bezirke
- Exception queue shows side-by-side; approve/reject change status correctly
- Source health dashboard shows all four data points; add/edit/disable work
- AI Config global settings persisted in DB; per-source overrides created/deleted correctly
- PipelineConfig maxRetryCount and deadManThresholdHours editable and persisted
</success_criteria>

<output>
After completion, create `.planning/phases/05-editorial-cms/05-08-SUMMARY.md`
</output>
