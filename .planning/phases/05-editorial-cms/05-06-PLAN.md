---
phase: 05-editorial-cms
plan: 06
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
files_modified:
  - src/app/(admin)/admin/articles/page.tsx
  - src/app/(admin)/admin/articles/ArticleFilters.tsx
  - src/app/(admin)/admin/articles/ArticleRow.tsx
  - src/app/(admin)/admin/articles/new/page.tsx
  - src/app/(admin)/admin/exceptions/page.tsx
  - src/app/(admin)/admin/exceptions/ExceptionCard.tsx
autonomous: true
requirements:
  - CMS-01
  - CMS-02
  - CMS-03

must_haves:
  truths:
    - "Editor can open /admin/articles and see a list of articles filtered by Bezirk, source, status, and date range"
    - "Editor can click Pinnen, Featuren, or Loeschen inline on any article row"
    - "Editor can click 'Neuer Artikel' to open a form, fill in content, and save — creating a MANUAL/PUBLISHED article"
    - "Editor can open /admin/exceptions and see all REVIEW-status articles"
    - "Editor can see the rewritten article and the raw source text side by side on the exception detail"
    - "Editor can click Genehmigen or Ablehnen to approve or reject an exception queue item"
  artifacts:
    - path: "src/app/(admin)/admin/articles/page.tsx"
      provides: "Article list page with filters and inline actions"
    - path: "src/app/(admin)/admin/articles/new/page.tsx"
      provides: "Manual article creation form"
    - path: "src/app/(admin)/admin/exceptions/page.tsx"
      provides: "Exception queue page with side-by-side view"
  key_links:
    - from: "src/app/(admin)/admin/articles/page.tsx"
      to: "src/lib/admin/articles-actions.ts"
      via: "listArticlesAdmin() called in Server Component"
      pattern: "listArticlesAdmin"
    - from: "src/app/(admin)/admin/articles/ArticleRow.tsx"
      to: "src/lib/admin/articles-actions.ts"
      via: "form actions: togglePin, toggleFeature, softDelete"
      pattern: "togglePin|toggleFeature|softDelete"
    - from: "src/app/(admin)/admin/articles/new/page.tsx"
      to: "src/lib/admin/articles-actions.ts"
      via: "form action: createManualArticle"
      pattern: "createManualArticle"
    - from: "src/app/(admin)/admin/exceptions/page.tsx"
      to: "src/lib/admin/exceptions-actions.ts"
      via: "listExceptionQueue() + approveArticle/rejectArticle form actions"
      pattern: "listExceptionQueue|approveArticle|rejectArticle"
---

<objective>
Build the Articles list page and Exception Queue page as Next.js Server Components calling the Server Actions from Plan 03.

Purpose: Delivers CMS-01 (manual article creation), CMS-02 (curation: pin/feature/soft-delete), CMS-03 (exception queue review) in the UI layer.
Output: Two working admin pages at /admin/articles and /admin/exceptions.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-editorial-cms/05-CONTEXT.md
@.planning/phases/05-editorial-cms/05-RESEARCH.md
@.planning/phases/05-editorial-cms/05-02-SUMMARY.md
@.planning/phases/05-editorial-cms/05-03-SUMMARY.md

<interfaces>
<!-- Next.js 15 App Router page patterns -->

Server Component page with searchParams (for filters):
```typescript
// src/app/(admin)/admin/articles/page.tsx
import type { ArticleStatus, ArticleSource } from '@prisma/client'

interface SearchParams {
  bezirkId?: string
  source?: string
  status?: string
  from?: string
  to?: string
  page?: string
}

export default async function ArticlesPage({
  searchParams,
}: {
  searchParams: Promise<SearchParams>  // searchParams is async in Next.js 15
}) {
  const params = await searchParams
  // ... call listArticlesAdmin with parsed params
}
```

Note: In Next.js 15, `searchParams` in page.tsx is a Promise — must be awaited.
Source: verified against node_modules/next Next.js 15 breaking changes.

Form actions (mutations) use Server Action references directly in forms:
```tsx
import { togglePin } from '@/lib/admin/articles-actions'
// ...
<form action={togglePin}>
  <input type="hidden" name="id" value={article.id} />
  <button type="submit">Pin</button>
</form>
```

Bezirk multi-select for new article form:
- Load all Bezirke from DB (use existing listBezirke from content/bezirke.ts)
- Render as checkboxes or a multi-select — Claude's discretion
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Articles list page with filters and inline actions</name>
  <files>
    src/app/(admin)/admin/articles/page.tsx,
    src/app/(admin)/admin/articles/ArticleFilters.tsx,
    src/app/(admin)/admin/articles/ArticleRow.tsx
  </files>
  <action>
    Before writing, read `node_modules/next/dist/docs/` for the relevant section on searchParams and Server Components. Read the existing `src/app/page.tsx` and `src/app/layout.tsx` for project conventions.

    Create `src/app/(admin)/admin/articles/page.tsx` — Server Component:
    - `await searchParams` to get filter values (bezirkId, source, status, from, to, page)
    - Parse and pass to `listArticlesAdmin()` from `@/lib/admin/articles-actions`
    - Render: page title "Artikel", a filter toolbar (ArticleFilters client component), "Neuer Artikel" button (links to /admin/articles/new), a table/list of ArticleRow items, simple pagination (prev/next page links with offset)
    - Empty state: "Keine Artikel gefunden." text

    Create `src/app/(admin)/admin/articles/ArticleFilters.tsx` — 'use client' component:
    - Filter controls: Bezirk select (load from URL params), Source select (OTS_AT/RSS/MANUAL), Status select, date range inputs (from/to)
    - On change, updates URL searchParams (use `useRouter` + `useSearchParams` from next/navigation)
    - Keep it simple: 4 inputs + a "Filter zurücksetzen" button that clears all params

    Create `src/app/(admin)/admin/articles/ArticleRow.tsx` — renders one article row:
    - Shows: title (truncated to 80 chars), status badge (colored), source type, publishedAt date, Bezirk tags
    - Inline action forms (each a `<form>` with hidden id input + submit button):
      - Pinnen/Entpinnen: calls togglePin Server Action
      - Featuren/Entfeaturen: calls toggleFeature Server Action
      - Loeschen: calls softDelete Server Action (show a confirm: `onclick="return confirm('Artikel loeschen?')"`)
    - Link to edit form (use /admin/articles/{id}/edit — stub the route to be created if editor navigates there; full edit form is Claude's discretion: can be same page with query param or separate route)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "articles/" | head -20 || echo "articles pages: no type errors"</automated>
  </verify>
  <done>
    `npx tsc --noEmit` reports no errors in articles page files. Articles page imports listArticlesAdmin correctly. ArticleRow has form actions for togglePin, toggleFeature, softDelete. ArticleFilters is a Client Component with 'use client'.
  </done>
</task>

<task type="auto">
  <name>Task 2: Manual article creation form + Exception Queue page</name>
  <files>
    src/app/(admin)/admin/articles/new/page.tsx,
    src/app/(admin)/admin/exceptions/page.tsx,
    src/app/(admin)/admin/exceptions/ExceptionCard.tsx
  </files>
  <action>
    1. Create `src/app/(admin)/admin/articles/new/page.tsx` — Server Component rendering a form:
    - Form fields:
      - Titel (text input, required)
      - Inhalt (textarea, required, ~10 rows)
      - Bezirke (multi-select or checkbox list of all Bezirke — fetch via `listBezirke()` from `@/lib/content/bezirke.ts`)
      - SEO-Titel (text input, optional)
      - Meta-Beschreibung (textarea, ~3 rows, optional)
      - Angepinnt toggle (checkbox)
      - Hervorgehoben toggle (checkbox)
    - Form action: `createManualArticle` from `@/lib/admin/articles-actions`
    - On success (redirect to /admin/articles handled by Server Action)
    - Cancel link back to /admin/articles

    2. Create `src/app/(admin)/admin/exceptions/page.tsx` — Server Component:
    - Calls `listExceptionQueue()` from `@/lib/admin/exceptions-actions`
    - Renders list of ExceptionCard components
    - Empty state: "Keine Artikel in der Ausnahme-Queue." with a green checkmark emoji text

    3. Create `src/app/(admin)/admin/exceptions/ExceptionCard.tsx` — Server Component (no interactivity needed — actions are form submits):
    - Two-column layout (side by side): left = rewritten article (content field), right = raw source (rawPayload rendered as JSON.stringify with 2-space indent inside a `<pre>` tag, or parsed if it matches OTS/RSS shape)
    - Shows: article title, createdAt date, source type badge
    - Approve button: `<form action={approveArticle}><input type="hidden" name="id" .../><button>Genehmigen</button></form>`
    - Reject button: `<form action={rejectArticle}><input type="hidden" name="id" .../><button>Ablehnen</button></form>`
    - Style: Genehmigen = green button, Ablehnen = red button. Tailwind classes only.
    - rawPayload is `Json?` type — use `JSON.stringify(article.rawPayload, null, 2)` as fallback; no TypeScript type assertion errors.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "exceptions/|articles/new" | head -20 || echo "new article + exceptions pages: no type errors"</automated>
  </verify>
  <done>
    Manual article creation form renders all required fields and uses createManualArticle Server Action. Exception queue page calls listExceptionQueue and renders ExceptionCard for each item. Side-by-side layout present. Approve/reject form actions wired. No TypeScript errors.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no TypeScript errors in any of the six new files
2. `npx next build` — build succeeds (all pages compile)
3. `npx vitest run` — full suite still green (UI pages have no unit tests; existing tests unaffected)
</verification>

<success_criteria>
- Articles list page renders with filter toolbar (bezirkId, source, status, date range)
- ArticleRow has three inline action forms (pin, feature, soft-delete)
- New article form has all 7 required fields + uses createManualArticle action
- Exception queue page shows side-by-side (content vs rawPayload) layout
- Approve and Reject buttons wired to correct Server Actions
- All pages are typed correctly (no `any` escapes for rawPayload — use JSON.stringify)
</success_criteria>

<output>
After completion, create `.planning/phases/05-editorial-cms/05-06-SUMMARY.md`
</output>
