---
phase: 29-article-detail
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(public)/artikel/[publicId]/[slug]/page.tsx
  - src/components/reader/TopMeldungenRow.tsx
autonomous: false
requirements: [ART-01]

must_haves:
  truths:
    - "Article page body uses Plus Jakarta Sans (font-body) with warm cream (#FCF9EF) canvas"
    - "Article headlines render in Newsreader (font-headline) with text-text color, no zinc/legacy colors"
    - "Article detail page uses same tokens, corner radius, and icon variant as homepage components"
    - "No hardcoded hex colors remain in article page or its sub-components"
    - "Spacing uses organic scale (spacing-gutter for horizontal, spacing-section for vertical gaps)"
  artifacts:
    - path: "src/app/(public)/artikel/[publicId]/[slug]/page.tsx"
      provides: "Restyled article detail page"
      contains: "text-text"
    - path: "src/components/reader/TopMeldungenRow.tsx"
      provides: "Related articles row with semantic tokens"
      contains: "text-text"
  key_links:
    - from: "src/app/(public)/artikel/[publicId]/[slug]/page.tsx"
      to: "src/app/globals.css"
      via: "Tailwind semantic tokens"
      pattern: "text-text|text-secondary|bg-background|bg-surface"
---

<objective>
Restyle the article detail page to match the Wurzelwelt design system established in Phase 26.

Purpose: The article page still uses legacy color values (text-zinc-800, text-zinc-400, hardcoded #2a2a2a) and non-organic spacing. This plan replaces all legacy styling with Wurzelwelt semantic tokens so the article experience is visually consistent with the homepage.

Output: Article detail page and related-articles row fully using Wurzelwelt palette, typography tokens, and organic spacing.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/app/globals.css
@src/app/(public)/artikel/[publicId]/[slug]/page.tsx
@src/components/reader/TopMeldungenRow.tsx
@src/components/reader/ShareButton.tsx

<interfaces>
<!-- Design tokens from globals.css that must be used -->
From src/app/globals.css:
```css
--color-primary: #1B2D18;
--color-secondary: #4A5D23;
--color-accent: #9F411E;
--color-background: #FCF9EF;
--color-text: #071806;
--color-surface: #F6F4EA;
--color-surface-elevated: #FFFFFF;
--color-primary-container: #4A5D23;
--font-headline: var(--font-newsreader), serif;
--font-body: var(--font-jakarta), sans-serif;
--font-label: var(--font-jakarta), sans-serif;
--radius-sm: 0.75rem;
--spacing-gutter: 1.7rem;
--spacing-section: 4rem;
```

Tailwind utility mapping:
- `text-text` = #071806 (main body text)
- `text-secondary` = #4A5D23 (meta text, labels)
- `text-primary` = #1B2D18 (emphasis)
- `bg-background` = #FCF9EF (page canvas)
- `bg-surface` = #F6F4EA (tonal section)
- `font-headline` = Newsreader serif
- `font-body` / `font-label` = Plus Jakarta Sans
- `rounded-sm` = 0.75rem (Wurzelwelt soft corners)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace legacy colors and spacing on article detail page and TopMeldungenRow</name>
  <files>src/app/(public)/artikel/[publicId]/[slug]/page.tsx, src/components/reader/TopMeldungenRow.tsx</files>
  <action>
In `src/app/(public)/artikel/[publicId]/[slug]/page.tsx`, make these specific replacements:

1. Hero image figcaption: change `text-zinc-400` to `text-secondary/60`
2. Content column: change `px-4 py-6` to `px-[var(--spacing-gutter)] py-[var(--spacing-section)]`
3. Breadcrumb nav: already uses `text-secondary` -- keep as-is
4. Article h1: change `text-zinc-800` to `text-text`
5. Article prose: change `prose-p:text-[#2a2a2a]` to `prose-p:text-text`
6. Ensure `prose-headings:font-headline` stays (correct)
7. Ensure `prose-a:text-primary prose-a:underline` stays (correct)
8. Source attribution div: already uses `text-secondary/70` -- keep as-is

In `src/components/reader/TopMeldungenRow.tsx`:
1. Article headline h3: change `text-zinc-900` to `text-text`

IMPORTANT per CLAUDE.md: Read the Next.js docs in node_modules/next/dist/docs/ before making changes if any API usage is involved. This plan is styling-only so no Next.js API changes needed, but do NOT change any imports, data fetching, or component structure -- only Tailwind classes.

Do NOT change:
- `bg-background` (already correct)
- `max-h-[50vh]` (functional constraint, not a legacy style)
- ShareButton (already uses semantic tokens)
- Any component logic, imports, or data flow
- `img-matte` class (custom utility, correct)
- `scrollbar-none` class (custom utility, correct)
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && grep -n "zinc\|text-\[#" src/app/\(public\)/artikel/\[publicId\]/\[slug\]/page.tsx src/components/reader/TopMeldungenRow.tsx | grep -v node_modules; echo "EXIT:$?"</automated>
  </verify>
  <done>Zero matches for zinc or hardcoded hex text colors in both files. All text colors use semantic tokens (text-text, text-secondary, text-primary). Spacing uses organic variables.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of article detail page</name>
  <action>
Human verifies the restyled article detail page matches Wurzelwelt design system.

What was built: Article detail page restyled with Wurzelwelt design tokens -- warm cream canvas, Newsreader headlines in forest green, Plus Jakarta Sans body text, organic spacing, no legacy zinc/hex colors.

How to verify:
1. Run `npm run dev` (if not already running)
2. Open any article page (e.g., http://localhost:3000 then click an article)
3. Verify: Page background is warm cream (#FCF9EF)
4. Verify: Headline is in serif Newsreader font, dark forest green color
5. Verify: Body text is in Plus Jakarta Sans, readable dark color
6. Verify: Image caption is muted green (not gray)
7. Verify: Horizontal padding matches homepage gutter spacing
8. Verify: "Weitere Artikel" related cards use same dark text, not gray
9. Verify: Overall feel matches homepage (same tokens, same warmth)
  </action>
  <done>User approves visual consistency of article detail page with Wurzelwelt design system</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
After Task 1, run:
```bash
grep -rn "zinc\|text-\[#" src/app/\(public\)/artikel/\[publicId\]/\[slug\]/page.tsx src/components/reader/TopMeldungenRow.tsx
```
Expected: no matches (zero legacy color references).

After checkpoint approval, the article detail page is visually consistent with homepage components.
</verification>

<success_criteria>
- No hardcoded hex colors or zinc-* utilities remain in article page or TopMeldungenRow
- Article page uses bg-background, text-text, text-secondary, font-headline, font-body semantic tokens
- Spacing uses organic scale variables (spacing-gutter, spacing-section)
- Visual consistency with homepage confirmed by human review
</success_criteria>

<output>
After completion, create `.planning/phases/29-article-detail/29-01-SUMMARY.md`
</output>
