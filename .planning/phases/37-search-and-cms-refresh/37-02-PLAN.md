---
phase: 37-search-and-cms-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/reader/SearchPageLayout.tsx
  - src/lib/admin/articles-actions.test.ts
autonomous: true
requirements:
  - SRCH-01
  - CMS-02

must_haves:
  truths:
    - "The search page renders entirely in Archivist tokens -- no legacy warm-cream or forest-green palette remnants are visible"
    - "The theme tag field persists gruene_woche assignment and null clearing through the server action"
  artifacts:
    - path: "src/components/reader/SearchPageLayout.tsx"
      provides: "Archivist-styled search page"
      contains: "bg-parchment"
    - path: "src/lib/admin/articles-actions.test.ts"
      provides: "Theme field persistence test"
      contains: "gruene_woche"
  key_links:
    - from: "src/components/reader/SearchPageLayout.tsx"
      to: "ArticleCard component"
      via: "import and render for result cards"
      pattern: "ArticleCard"
---

<objective>
Audit the search page for any remaining legacy token remnants and add a unit test for the CMS-02 theme tag persistence. The search page is already substantially migrated (per research) -- this plan seals any gaps and provides test coverage for the theme field server action.

Purpose: SRCH-01 requires zero legacy palette remnants on the search page. CMS-02 requires verified theme tag persistence (DB schema and server action exist, but no targeted test).
Output: Search page confirmed clean, theme persistence test added.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-search-and-cms-refresh/37-RESEARCH.md
@src/components/reader/SearchPageLayout.tsx
@src/lib/admin/articles-actions.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add theme tag persistence test for CMS-02</name>
  <files>src/lib/admin/articles-actions.test.ts</files>
  <behavior>
    - Test 1: updateArticleDb sets theme to "gruene_woche" when provided
    - Test 2: updateArticleDb clears theme to null when empty string provided
    - Test 3: updateArticleDb preserves existing theme when theme field not included in update
  </behavior>
  <action>
Add test cases to the existing `src/lib/admin/articles-actions.test.ts` file. Follow the existing test patterns in that file (likely uses vitest + prisma mocking or pgLite).

Write tests FIRST (RED), then verify they pass against the existing implementation (GREEN). The server action already handles theme -- these tests confirm the behavior is correct.

Test cases:
1. Call updateArticleDb with `{ theme: "gruene_woche" }` -- assert the article's theme field is set to "gruene_woche"
2. Call updateArticleDb with `{ theme: "" }` -- assert the article's theme field is set to null (empty string normalizes to null per Phase 35 decision)
3. Call updateArticleDb without theme in the input -- assert the article's theme field is unchanged
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && npx vitest run src/lib/admin/articles-actions.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Three theme-related test cases pass. CMS-02 persistence behavior is verified: assign gruene_woche, clear to null, and preserve on omission all work correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and seal search page Archivist token compliance</name>
  <files>src/components/reader/SearchPageLayout.tsx</files>
  <action>
Audit SearchPageLayout.tsx for any remaining legacy tokens. Per research, the page is already substantially migrated. Check for:

1. Any occurrence of legacy tokens: `text-text`, `bg-background`, `from-primary`, `to-primary-container`, `ring-primary`, `text-secondary`, `text-accent`, `bg-accent`, `text-white` (on dark backgrounds)
2. Any `rounded-full` on interactive elements (should be `rounded-sm` or `rounded-xs`)
3. Any hardcoded color classes that are not Archivist tokens (e.g., `bg-green-*`, `text-blue-*`) -- these should not appear on the search page
4. Verify `rounded-xs` resolves correctly in Tailwind v4 (it maps to the 0.125rem TOKN-03 token)

If any legacy tokens are found, replace them using the same token map:
- `text-text` -> `text-ink`
- `bg-background` -> `bg-parchment`
- `ring-primary` -> `ring-ink`
- `text-secondary` -> `text-slate`
- `text-white` (on dark bg) -> `text-parchment`

If the page is already fully compliant (highly likely), document this finding -- the task is a verification seal, not necessarily a code change.

Run `npx vitest run` to confirm no regressions.
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && grep -n "from-primary\|to-primary-container\|text-text\b\|bg-background\|ring-primary\|text-secondary\|text-accent\|bg-accent" src/components/reader/SearchPageLayout.tsx 2>/dev/null || echo "CLEAN — no legacy tokens in SearchPageLayout.tsx"</automated>
  </verify>
  <done>SearchPageLayout.tsx contains zero legacy MD3 tokens. All surfaces use Archivist Ink/Parchment/Slate tokens. Filter chips, result cards, typography, and background all comply with SRCH-01.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/admin/articles-actions.test.ts` -- theme tests pass
2. `grep -rn "from-primary\|to-primary-container\|text-text\b\|bg-background\|ring-primary\|text-secondary\|text-accent" src/components/reader/SearchPageLayout.tsx` returns empty
3. `npx vitest run` -- full suite green
</verification>

<success_criteria>
- SRCH-01: Search page renders with zero legacy token remnants -- confirmed by grep audit and visual inspection readiness
- CMS-02: Theme tag persistence verified by 3 passing unit tests (assign, clear, preserve)
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/37-search-and-cms-refresh/37-02-SUMMARY.md`
</output>
