---
phase: 33-color-token-foundation
plan: 03
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - src/components/reader/BottomNavClient.tsx
  - src/components/reader/RegionalNavBar.tsx
  - src/components/reader/WurzelNavBar.tsx
  - src/components/reader/WurzelAppBar.tsx
  - src/components/reader/Footer.tsx
autonomous: true
requirements:
  - TOKN-03
  - TOKN-06

must_haves:
  truths:
    - "All nav component shadows use shadow-[var(--shadow-nav)] instead of inline rgba(0,0,0) arbitrary values"
    - "No rounded-full, rounded-xl, rounded-lg, or rounded-2xl exists in any reader component across the entire codebase"
    - "No rgba(0,0,0) or rgb(0 0 0) shadow values exist in any reader component"
    - "The entire reader component directory passes a clean token audit — no old token names, no zinc-*, no raw hex (except Bundesland branding)"
  artifacts:
    - path: "src/components/reader/BottomNavClient.tsx"
      provides: "Nav with ink-tinted shadow token"
    - path: "src/components/reader/WurzelNavBar.tsx"
      provides: "Nav with straight top edge and ink-tinted shadow"
  key_links:
    - from: "src/components/reader/BottomNavClient.tsx"
      to: "src/app/globals.css"
      via: "shadow-[var(--shadow-nav)] referencing @theme --shadow-nav token"
      pattern: "shadow-nav"
    - from: "src/components/reader/WurzelNavBar.tsx"
      to: "src/app/globals.css"
      via: "shadow-[var(--shadow-nav)] referencing @theme --shadow-nav token"
      pattern: "shadow-nav"
---

<objective>
Migrate 5 shell/nav reader components to Archivist tokens and run a final comprehensive verification sweep across all reader components.

Purpose: These nav and footer components have the shadow-[rgba(0,0,0)] anti-pattern (TOKN-06) and decorative rounded-full/2xl that must be flattened (TOKN-03). The verification sweep confirms no token migration gaps remain anywhere.
Output: 5 migrated shell components + confirmed clean audit of entire reader directory.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-color-token-foundation/33-CONTEXT.md
@.planning/phases/33-color-token-foundation/33-RESEARCH.md
@.planning/phases/33-color-token-foundation/33-01-SUMMARY.md

@src/app/globals.css
@src/components/reader/BottomNavClient.tsx
@src/components/reader/RegionalNavBar.tsx
@src/components/reader/WurzelNavBar.tsx
@src/components/reader/WurzelAppBar.tsx
@src/components/reader/Footer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate nav components — shadow tokens and radius flattening</name>
  <files>src/components/reader/BottomNavClient.tsx, src/components/reader/RegionalNavBar.tsx, src/components/reader/WurzelNavBar.tsx, src/components/reader/WurzelAppBar.tsx, src/components/reader/Footer.tsx</files>
  <action>
**BottomNavClient.tsx (TOKN-06 + TOKN-03):**
1. Replace `shadow-[0_-2px_8px_rgba(0,0,0,0.06)]` with `shadow-[var(--shadow-nav)]`
2. Remove rounded-full from nav pill/indicator — use rounded-xs instead
3. Replace zinc-* with ink/slate tokens
4. Replace old token names with Archivist equivalents
5. Do NOT remove the top border on the nav — Phase 34 (SHEL-01) will redesign this component. Only fix shadows, colors, and radii.

**RegionalNavBar.tsx (TOKN-06 + TOKN-03):**
1. Replace `shadow-[0_-2px_8px_rgba(0,0,0,0.06)]` (or similar) with `shadow-[var(--shadow-nav)]`
2. Remove rounded-full from nav pills
3. Replace zinc-* with ink/slate tokens
4. Replace old token names

**WurzelNavBar.tsx (TOKN-06 + TOKN-03):**
1. Replace `shadow-[0_-2px_8px_rgba(0,0,0,0.06)]` with `shadow-[var(--shadow-nav)]`
2. Replace rounded-t-2xl with rounded-none on the nav container (straight top edge — Phase 34 redesigns this)
3. Remove rounded-full from any decorative elements
4. Replace zinc-* with ink/slate tokens
5. Replace old token names

**WurzelAppBar.tsx (TOKN-03):**
1. If there is a rounded-full on an avatar element, replace with `style={{ borderRadius: '50%' }}` (functional circle, not decorative per research recommendation)
2. Replace zinc-* with ink/slate tokens
3. Replace old token names
4. Flatten any rounded-xl/lg to rounded-sm

**Footer.tsx:**
1. Replace zinc-* references with ink/slate tokens
2. Replace old token names
3. Flatten any oversized radii

DO NOT touch:
- Any inline style containing #2D5A27, #154212, #2d7a1f — these are Bundesland branding colors
- Border classes on nav that are structural (top-border active indicators) — Phase 34 scope
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && grep -c "rgba(0,0,0\|rgb(0 0 0" src/components/reader/BottomNavClient.tsx src/components/reader/RegionalNavBar.tsx src/components/reader/WurzelNavBar.tsx | grep ":0$" | wc -l | grep "3"</automated>
  </verify>
  <done>All 3 nav components use shadow-[var(--shadow-nav)] instead of rgba(0,0,0) inline shadows. All 5 files use Archivist tokens. No rounded-full/xl/lg/2xl (except inline borderRadius for functional circles). WurzelNavBar has straight top edge (rounded-none).</done>
</task>

<task type="auto">
  <name>Task 2: Full codebase verification sweep and build confirmation</name>
  <files>src/components/reader/ArticleCard.tsx, src/components/reader/TopMeldungenRow.tsx, src/components/reader/ListItem.tsx, src/components/reader/ArticleFeed.tsx, src/components/reader/HeroArticle.tsx, src/components/reader/EditorialCard.tsx, src/components/reader/SearchPageLayout.tsx, src/components/reader/BezirkModal.tsx, src/components/reader/CookieBanner.tsx, src/components/reader/BottomNavClient.tsx, src/components/reader/RegionalNavBar.tsx, src/components/reader/WurzelNavBar.tsx, src/components/reader/WurzelAppBar.tsx, src/components/reader/Footer.tsx</files>
  <action>
Run the following audit checks across ALL reader components (src/components/reader/*.tsx). Fix any remaining violations found.

**Audit 1 — Old token names (TOKN-01):**
grep for: text-primary, bg-primary (not bg-primary-container which is removed), text-secondary, bg-secondary, bg-background, text-accent, bg-accent, bg-primary-container
Expected: 0 matches in reader components

**Audit 2 — Borders for separation (TOKN-02):**
grep for: border-b, border-t, border-l, border-r (but NOT border-none or border-0 or border-transparent)
Expected: 0 matches used for VISUAL SEPARATION. Note: structural borders (e.g., top-border active indicator on nav) are acceptable — Phase 34 handles those. Use judgment.

**Audit 3 — Oversized radii (TOKN-03):**
grep for: rounded-full, rounded-xl, rounded-lg, rounded-2xl
Expected: 0 matches in reader components (inline style borderRadius exempted)

**Audit 4 — Raw shadows (TOKN-06):**
grep for: rgba(0,0,0, rgb(0 0 0, shadow-[0 (arbitrary shadow with hardcoded values)
Expected: 0 matches — all shadows should use @theme tokens

**Audit 5 — zinc colors:**
grep for: zinc-
Expected: 0 matches in reader components

**Audit 6 — Build check:**
Run `npx next build` to confirm no compilation errors. Warnings about unused old token utilities are acceptable IF they come from non-reader files (admin/layout). Errors in reader components must be fixed.

**Audit 7 — Test suite:**
Run `npx vitest run` to confirm no test regressions.

Any violations found: fix them in the relevant file. Document what was fixed.

EXCLUDE from audits: Files in src/app/(admin)/, src/app/admin/, and inline styles with Bundesland hex (#2D5A27, #154212, #2d7a1f).
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && echo "=== OLD TOKENS ===" && grep -r "text-primary\|bg-primary\b\|text-secondary\|bg-secondary\|bg-background\|text-accent" src/components/reader/ --include="*.tsx" | grep -v "//.*text-primary" | wc -l && echo "=== OVERSIZED RADII ===" && grep -r "rounded-full\|rounded-xl\|rounded-lg\|rounded-2xl" src/components/reader/ --include="*.tsx" | wc -l && echo "=== RAW SHADOWS ===" && grep -r "rgba(0,0,0\|rgb(0 0 0" src/components/reader/ --include="*.tsx" | wc -l && echo "=== ZINC ===" && grep -r "zinc-" src/components/reader/ --include="*.tsx" | wc -l</automated>
  </verify>
  <done>All 4 audit categories return 0 violations across all reader components. Build succeeds. Test suite passes. The entire reader component directory uses exclusively Archivist tokens with no legacy remnants.</done>
</task>

</tasks>

<verification>
- All reader components pass the 5-category audit (old tokens, borders, radii, shadows, zinc)
- `npx next build` succeeds without errors in reader components
- `npx vitest run` passes
- grep -r "BEZIRK_COLORS\|BEZIRK_BADGE_COLORS" src/components/reader/ returns 0
- grep -r "rounded-full\|rounded-xl\|rounded-lg\|rounded-2xl" src/components/reader/ returns 0
- grep -r "rgba(0,0,0" src/components/reader/ returns 0
</verification>

<success_criteria>
Every reader-facing component in src/components/reader/ uses the Archivist token system exclusively. No visible borders for section separation. All radii are 0.125rem or 0.25rem. All shadows use ink-tinted color-mix tokens. The project builds and tests pass. Phase 33 requirements TOKN-01 through TOKN-06 are fully satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/33-color-token-foundation/33-03-SUMMARY.md`
</output>
