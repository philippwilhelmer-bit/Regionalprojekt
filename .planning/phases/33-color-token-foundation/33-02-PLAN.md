---
phase: 33-color-token-foundation
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - src/components/reader/ArticleCard.tsx
  - src/components/reader/TopMeldungenRow.tsx
  - src/components/reader/ListItem.tsx
  - src/components/reader/ArticleFeed.tsx
  - src/components/reader/HeroArticle.tsx
  - src/components/reader/EditorialCard.tsx
  - src/components/reader/SearchPageLayout.tsx
  - src/components/reader/BezirkModal.tsx
  - src/components/reader/CookieBanner.tsx
autonomous: true
requirements:
  - TOKN-02
  - TOKN-03

must_haves:
  truths:
    - "No reader-facing component renders a visible CSS border for section separation"
    - "All cards use unified Archivist ink gradient — no per-Bezirk color maps remain"
    - "All zinc-* color classes in these 9 files are replaced with ink/slate tokens"
    - "All rounded-full, rounded-xl, rounded-lg, rounded-2xl in these files are replaced with rounded-xs or rounded-sm"
  artifacts:
    - path: "src/components/reader/ArticleCard.tsx"
      provides: "Article cards with unified ink gradient"
    - path: "src/components/reader/ListItem.tsx"
      provides: "List items with tonal bg shift instead of border-b"
    - path: "src/components/reader/ArticleFeed.tsx"
      provides: "Article feed with tonal separation, spinner uses inline borderRadius"
  key_links:
    - from: "src/components/reader/ArticleCard.tsx"
      to: "src/app/globals.css"
      via: "Tailwind utility classes referencing @theme tokens"
      pattern: "bg-ink|text-parchment|from-ink"
    - from: "src/components/reader/ListItem.tsx"
      to: "src/app/globals.css"
      via: "bg-parchment tonal shift replacing border-b"
      pattern: "bg-parchment"
---

<objective>
Migrate 9 core reader components from old token names and raw hex values to the Archivist token system. Remove per-Bezirk color maps, replace borders with tonal shifts, and flatten all oversized border radii.

Purpose: These are the primary content display components — cards, lists, feeds, search, modals. After this plan, the reading experience uses the Archivist palette.
Output: 9 reader component files fully migrated to Archivist tokens.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-color-token-foundation/33-CONTEXT.md
@.planning/phases/33-color-token-foundation/33-RESEARCH.md
@.planning/phases/33-color-token-foundation/33-01-SUMMARY.md

@src/app/globals.css
@src/components/reader/ArticleCard.tsx
@src/components/reader/TopMeldungenRow.tsx
@src/components/reader/ListItem.tsx
@src/components/reader/ArticleFeed.tsx
@src/components/reader/HeroArticle.tsx
@src/components/reader/EditorialCard.tsx
@src/components/reader/SearchPageLayout.tsx
@src/components/reader/BezirkModal.tsx
@src/components/reader/CookieBanner.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ArticleCard.tsx and TopMeldungenRow.tsx — remove Bezirk color maps</name>
  <files>src/components/reader/ArticleCard.tsx, src/components/reader/TopMeldungenRow.tsx</files>
  <action>
**ArticleCard.tsx:**
1. DELETE the entire BEZIRK_COLORS map (per-Bezirk gradient hex values)
2. DELETE the entire BEZIRK_BADGE_COLORS map
3. Replace the per-Bezirk gradient lookup with a unified Archivist ink gradient: `bg-gradient-to-br from-ink to-ink-soft`
4. Replace badge styling that used BEZIRK_BADGE_COLORS with: `text-parchment bg-ink-soft` and `rounded-xs`
5. Replace ALL zinc-* color classes: zinc-900 -> ink, zinc-500 -> ink-muted, zinc-400 -> ink-dim, zinc-300 -> slate-muted
6. Replace ALL old token references: text-primary -> text-ink, bg-background -> bg-parchment, bg-surface -> bg-surface, text-secondary -> text-ink-muted, bg-surface-elevated -> bg-surface-elevated, text-accent -> text-aged-wood
7. Replace ALL rounded-full/rounded-xl/rounded-lg with rounded-xs (badges, buttons) or rounded-sm (card container, image)
8. Remove any border-b/border-t used for section separation — use tonal bg shifts instead

**TopMeldungenRow.tsx:**
1. Same treatment as ArticleCard — delete per-Bezirk hex maps if duplicated here
2. Replace gradient colors with `from-ink to-ink-soft`
3. Replace zinc-* with ink/slate tokens
4. Replace old token names with Archivist equivalents
5. Flatten oversized radii to rounded-xs or rounded-sm

DO NOT touch any inline style containing #2D5A27 or #154212 — these are Bundesland branding colors, not design tokens.
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && grep -c "BEZIRK_COLORS\|BEZIRK_BADGE_COLORS" src/components/reader/ArticleCard.tsx src/components/reader/TopMeldungenRow.tsx | grep ":0$" | wc -l | grep "2"</automated>
  </verify>
  <done>Both files have zero BEZIRK_COLORS/BEZIRK_BADGE_COLORS references. All gradients use unified ink tokens. No zinc-* classes. No old token names. No oversized radii.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ListItem, ArticleFeed, HeroArticle, EditorialCard — borders and hex swap</name>
  <files>src/components/reader/ListItem.tsx, src/components/reader/ArticleFeed.tsx, src/components/reader/HeroArticle.tsx, src/components/reader/EditorialCard.tsx</files>
  <action>
**ListItem.tsx (TOKN-02 — No-Line Rule):**
1. Remove `border-b border-surface last:border-b-0` (or whatever border classes exist)
2. Apply tonal background shift: item gets `bg-parchment`, parent container should have `bg-parchment-dim` — the contrast creates visual separation without a line
3. Replace zinc-* with ink/slate tokens
4. Replace old token names with Archivist equivalents
5. Flatten oversized radii

**ArticleFeed.tsx (TOKN-02 + spinner special case):**
1. Remove any border classes used for section separation
2. Replace zinc-* with ink/slate tokens
3. Replace old token names
4. SPINNER SPECIAL CASE: The loading spinner uses `rounded-full` functionally (to create a circle). Replace the Tailwind `rounded-full` class with `style={{ borderRadius: '50%' }}` — this exempts it from the decorative radius rule while keeping the spinner circular
5. Flatten other oversized radii

**HeroArticle.tsx:**
1. Replace zinc-* with ink/slate tokens
2. Replace old token names (text-primary, bg-primary-container, etc.) with Archivist equivalents
3. Replace rounded-full on badge spans with rounded-xs
4. Flatten other oversized radii

**EditorialCard.tsx:**
1. Replace zinc-* with ink/slate tokens
2. Replace old token names
3. Flatten oversized radii
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && grep -c "border-b\|border-surface" src/components/reader/ListItem.tsx | grep "^0$" && echo "LISTITEM_BORDERLESS" && grep "borderRadius.*50%" src/components/reader/ArticleFeed.tsx && echo "SPINNER_INLINE_RADIUS"</automated>
  </verify>
  <done>ListItem has no border-b — uses tonal bg shift. ArticleFeed spinner uses inline borderRadius: 50%. All 4 files use ink/slate/parchment tokens exclusively. No zinc-*, no old token names, no oversized radii.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate SearchPageLayout, BezirkModal, CookieBanner — hex swap and radius</name>
  <files>src/components/reader/SearchPageLayout.tsx, src/components/reader/BezirkModal.tsx, src/components/reader/CookieBanner.tsx</files>
  <action>
**SearchPageLayout.tsx:**
1. Replace zinc-* with ink/slate tokens
2. Replace old token names (bg-surface-elevated -> bg-surface-elevated, text-primary -> text-ink, etc.)
3. Replace ALL rounded-full instances with rounded-xs (pills, chips, buttons)
4. Replace rounded-xl/rounded-lg with rounded-sm (containers)

**BezirkModal.tsx:**
1. Replace zinc-* with ink/slate tokens
2. Replace old token names
3. Replace rounded-full on CTA button with rounded-xs
4. Replace any rounded-xl/lg with rounded-sm on the modal container

**CookieBanner.tsx:**
1. Replace zinc-* with ink/slate tokens
2. Replace old token names
3. Replace rounded-full on primary button with rounded-xs
4. Replace any rounded-xl/lg with rounded-sm

For all 3 files: Do NOT add any borders. If visual separation is needed, use tonal background shifts (bg-parchment vs bg-parchment-dim).
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && grep -c "rounded-full\|rounded-xl\|rounded-lg\|rounded-2xl" src/components/reader/SearchPageLayout.tsx src/components/reader/BezirkModal.tsx src/components/reader/CookieBanner.tsx | grep ":0$" | wc -l | grep "3"</automated>
  </verify>
  <done>All 3 files have zero rounded-full/xl/lg/2xl instances. All use Archivist ink/slate/parchment tokens. No zinc-* classes. No old token names.</done>
</task>

</tasks>

<verification>
- grep -r "BEZIRK_COLORS\|BEZIRK_BADGE_COLORS" src/components/reader/ returns 0 matches
- grep -r "border-b\b" src/components/reader/ListItem.tsx returns 0 matches
- grep -r "rounded-full\|rounded-xl\|rounded-lg\|rounded-2xl" across all 9 files returns 0 matches (except inline borderRadius on spinner)
- grep -r "zinc-" across all 9 files returns 0 matches
- grep -r "text-primary\|bg-primary\b\|text-secondary\|bg-background\|text-accent\|bg-secondary" across all 9 files returns 0 matches
- npx vitest run passes
</verification>

<success_criteria>
All 9 core reader components use exclusively Archivist tokens (ink, parchment, slate, aged-wood families). No per-Bezirk color maps exist. No visible borders for section separation (tonal shifts only). No oversized border radii (rounded-full/xl/lg/2xl all replaced with rounded-xs or rounded-sm). Spinner uses inline borderRadius for functional circular shape.
</success_criteria>

<output>
After completion, create `.planning/phases/33-color-token-foundation/33-02-SUMMARY.md`
</output>
