---
phase: 06-reader-frontend
plan: "03"
type: execute
wave: 1
depends_on:
  - "06-01"
files_modified:
  - src/app/layout.tsx
  - src/app/(public)/layout.tsx
  - src/components/reader/Header.tsx
  - src/components/reader/BottomNav.tsx
  - src/components/reader/Footer.tsx
  - src/components/reader/CookieBanner.tsx
  - src/components/reader/AdUnit.tsx
autonomous: true
requirements:
  - READ-04
  - AD-01

must_haves:
  truths:
    - "All reader pages share a top header, bottom nav, and footer via the (public) layout"
    - "Root layout updated to de language, Geist fonts removed (alpine-themed sans-serif)"
    - "CookieBanner appears on first visit; consent stored in localStorage; does not re-prompt"
    - "AdUnit component renders the correct <ins> element for each zone using NEXT_PUBLIC env vars"
    - "AdSense script loads with next/script afterInteractive strategy in root layout"
    - "Cookie rejection → AdSense served in NPA mode (data-npa attribute set)"
  artifacts:
    - path: "src/app/(public)/layout.tsx"
      provides: "Public route group layout wrapping all reader pages"
      exports: ["default PublicLayout"]
    - path: "src/components/reader/Header.tsx"
      provides: "Top header: site name + Bezirk indicator; 'use client' for modal trigger"
      exports: ["Header"]
    - path: "src/components/reader/BottomNav.tsx"
      provides: "Fixed bottom navigation bar with Nachrichten tab"
      exports: ["BottomNav"]
    - path: "src/components/reader/Footer.tsx"
      provides: "Footer with Impressum link and RSS feed links per Bezirk"
      exports: ["Footer"]
    - path: "src/components/reader/CookieBanner.tsx"
      provides: "GDPR consent banner; reads/writes 'cookie_consent' in localStorage"
      exports: ["CookieBanner"]
    - path: "src/components/reader/AdUnit.tsx"
      provides: "<AdUnit zone='hero'|'between-articles'|'article-detail'> component"
      exports: ["AdUnit"]
  key_links:
    - from: "src/app/(public)/layout.tsx"
      to: "src/components/reader/Header.tsx"
      via: "import Header"
      pattern: "Header"
    - from: "src/app/layout.tsx"
      to: "next/script"
      via: "AdSense pagead script with afterInteractive"
      pattern: "afterInteractive"
---

<objective>
Build the public layout shell — root layout update, (public) route group layout, header, bottom nav, footer, cookie consent banner, and AdUnit component. No page content yet; all reader pages drop into this shell in subsequent plans.

Purpose: Plans 04, 05, and 06 all depend on the shared layout. Building the shell first prevents duplication and ensures consistent chrome across all reader routes.
Output: Seven files providing the complete visual and structural frame for the reader site.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-reader-frontend/06-CONTEXT.md
@.planning/phases/06-reader-frontend/06-RESEARCH.md

<interfaces>
<!-- Root layout — update lang and strip scaffold; keep body structure -->
From src/app/layout.tsx (current, to be updated):
```typescript
// Current: lang="en", Geist fonts, "Create Next App" metadata
// Target: lang="de", remove Geist imports, use system sans-serif via Tailwind
// Keep: <html> + <body> wrapper; add <Script> for AdSense
```

<!-- AdSense env vars (from CONTEXT.md) -->
NEXT_PUBLIC_ADSENSE_PUB_ID — AdSense publisher ID (e.g. ca-pub-XXXXXXXX)
NEXT_PUBLIC_ADSENSE_HERO_SLOT — slot ID for hero zone
NEXT_PUBLIC_ADSENSE_INLINE_SLOT — slot ID for between-articles zone
NEXT_PUBLIC_ADSENSE_ARTICLE_SLOT — slot ID for article detail zone

<!-- Bezirk color palette — Claude's discretion (13 distinct colors) -->
Each Bezirk gets a distinct Tailwind color class for gradient placeholders and badges.
Use the following mapping (slugs from bezirke.ts seed):
graz: bg-blue-600, graz-umgebung: bg-sky-500, liezen: bg-emerald-600,
bruck-muerzzuschlag: bg-teal-600, leoben: bg-cyan-700, murau: bg-violet-600,
murtal: bg-purple-600, voitsberg: bg-indigo-500, deutschlandsberg: bg-amber-600,
weiz: bg-orange-600, hartberg-fuerstenfeld: bg-rose-600,
suedoststeiermark: bg-red-600, leibnitz: bg-lime-700

<!-- CONTEXT.md navigation decisions (locked) -->
- Top header: site name + "· [Bezirk] ▾" indicator
- Bottom nav: single "Nachrichten" tab
- Footer: Impressum link + per-Bezirk RSS links + all-Steiermark RSS link
- Cookie banner: "Akzeptieren" / "Nur notwendige Cookies"; links to /impressum Datenschutz section
- Rejection → NPA mode on AdSense (not hidden)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update root layout and create (public) route group layout</name>
  <files>src/app/layout.tsx, src/app/(public)/layout.tsx</files>
  <action>
    **src/app/layout.tsx** — rewrite (do not keep Geist fonts):
    - Set `lang="de"` on `<html>`
    - Remove Geist imports entirely; use Tailwind's sans-serif stack via className
    - Update metadata: `title: 'Ennstal Aktuell'`, `description: 'Aktuelle Nachrichten aus der Steiermark'`
    - Add `<Script>` from 'next/script' with `strategy="afterInteractive"` for AdSense:
      ```tsx
      <Script
        src={`https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=${process.env.NEXT_PUBLIC_ADSENSE_PUB_ID}`}
        strategy="afterInteractive"
        crossOrigin="anonymous"
      />
      ```
    - Keep the existing `<html>` + `<body className="min-h-full flex flex-col bg-zinc-50 text-zinc-900 antialiased">` wrapper structure.

    **src/app/(public)/layout.tsx** — create new file:
    - Server Component (no 'use client')
    - Imports Header, BottomNav, Footer, CookieBanner
    - Structure:
      ```tsx
      export default function PublicLayout({ children }) {
        return (
          <>
            <Header />
            <main className="flex-1 pb-20">{children}</main>
            <Footer />
            <BottomNav />
            <CookieBanner />
          </>
        )
      }
      ```
    - The `pb-20` on `<main>` accounts for the fixed bottom nav height so content is not obscured.
    - Move `src/app/page.tsx` (the Next.js scaffold) into `src/app/(public)/page.tsx` — it will be replaced by the homepage in Plan 04, but it must exist in the correct location now. The root `src/app/page.tsx` should be deleted.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Root layout has lang="de", AdSense Script tag, no Geist imports. (public)/layout.tsx exists and imports the four shell components. npx tsc --noEmit has no new errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build Header, BottomNav, Footer, CookieBanner, AdUnit components</name>
  <files>
    src/components/reader/Header.tsx,
    src/components/reader/BottomNav.tsx,
    src/components/reader/Footer.tsx,
    src/components/reader/CookieBanner.tsx,
    src/components/reader/AdUnit.tsx
  </files>
  <action>
    Create src/components/reader/ directory.

    **Header.tsx** — 'use client' (reads localStorage to display selected Bezirk name):
    - Shows: `<site name> · <BezirkIndicator> ▾`
    - Site name from hardcoded "Ennstal Aktuell" (bundesland.config.ts read is server-side; Header is client for modal trigger)
    - BezirkIndicator: reads localStorage key `bezirk_selection` (JSON array of slugs). Displays first selected name or "Steiermark" if none selected. Tapping the indicator dispatches a custom event `openBezirkModal` — BezirkModal (Plan 04) listens for this event.
    - Tailwind: `sticky top-0 z-40 bg-white border-b border-zinc-200 px-4 h-14 flex items-center justify-between`
    - Left: site name `font-bold text-zinc-900`
    - Right: Bezirk indicator button `text-sm text-zinc-600 flex items-center gap-1`

    **BottomNav.tsx** — Server Component (no state needed):
    - Fixed bottom bar, single tab "Nachrichten" with a newspaper icon (use inline SVG or Unicode ◎)
    - Tailwind: `fixed bottom-0 inset-x-0 z-40 bg-white border-t border-zinc-200 h-16 flex items-center justify-center`
    - Single link `href="/"` with label "Nachrichten" below icon

    **Footer.tsx** — Server Component:
    - Renders Impressum link and RSS feed links
    - Impressum: `<a href="/impressum">Impressum & Datenschutz</a>`
    - RSS links: all-Steiermark `/rss/steiermark.xml` + list all 13 Bezirk slugs as RSS links `/rss/[slug].xml`
    - Hard-code the 13 Bezirk slugs (graz, graz-umgebung, liezen, bruck-muerzzuschlag, leoben, murau, murtal, voitsberg, deutschlandsberg, weiz, hartberg-fuerstenfeld, suedoststeiermark, leibnitz) — avoids DB call in footer
    - Tailwind: `bg-zinc-100 border-t border-zinc-200 px-4 py-6 text-sm text-zinc-500`

    **CookieBanner.tsx** — 'use client':
    - On mount, read localStorage key `cookie_consent`. If present (value 'accepted' or 'rejected'), do not show banner.
    - If absent, render bottom banner: `"Wir verwenden Cookies für personalisierte Werbung."` + link to `/impressum#datenschutz` + two buttons: "Akzeptieren" (sets `cookie_consent=accepted`) / "Nur notwendige Cookies" (sets `cookie_consent=rejected`).
    - On rejection, set `window.__adsenseNpa = true` — AdUnit reads this to set NPA mode.
    - Tailwind: `fixed bottom-16 inset-x-0 z-50 bg-white border-t border-zinc-200 px-4 py-3 flex flex-col sm:flex-row gap-3 items-start sm:items-center shadow-lg`
    - Bottom is 16 (above bottom nav which is h-16).

    **AdUnit.tsx** — 'use client':
    - Props: `zone: 'hero' | 'between-articles' | 'article-detail'`
    - Maps zone to slot env var:
      ```typescript
      const slotMap = {
        hero: process.env.NEXT_PUBLIC_ADSENSE_HERO_SLOT,
        'between-articles': process.env.NEXT_PUBLIC_ADSENSE_INLINE_SLOT,
        'article-detail': process.env.NEXT_PUBLIC_ADSENSE_ARTICLE_SLOT,
      }
      ```
    - On mount, pushes `window.adsbygoogle` command. If `window.__adsenseNpa` is true, sets `data-adsbygoogle-status` to trigger NPA mode (pass `{ google_npa: true }` in push options).
    - Renders: `<ins className="adsbygoogle" data-ad-client={pubId} data-ad-slot={slot} data-ad-format="auto" data-full-width-responsive="true" />`
    - If NEXT_PUBLIC_ADSENSE_PUB_ID is not set (local dev), renders `<div className="bg-zinc-100 rounded h-20 flex items-center justify-center text-xs text-zinc-400">Ad [{zone}]</div>` placeholder instead.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All five component files exist. npx tsc --noEmit has no errors. Each component file has correct 'use client' directive where needed. AdUnit renders placeholder in dev when env var absent.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx next build` succeeds (root layout + (public) layout are valid Next.js 15 layouts)
- (public)/layout.tsx, Header, BottomNav, Footer, CookieBanner, AdUnit all exist
- Root layout has lang="de" and AdSense Script tag
</verification>

<success_criteria>
Layout shell complete: all reader pages will inherit header, bottom nav, footer, and cookie banner automatically. AdUnit renders placeholders in dev. TypeScript compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reader-frontend/06-03-SUMMARY.md`
</output>
