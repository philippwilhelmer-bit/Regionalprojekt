---
phase: 11-fix-state-wide-article-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/scripts/backfill-state-wide.ts
autonomous: true
requirements:
  - AI-02

must_haves:
  truths:
    - "A backfill script exists that identifies articles with no ArticleBezirk rows and status in PUBLISHED/WRITTEN/REVIEW and sets isStateWide=true on them"
    - "The script supports --dry-run to preview candidates without writing"
    - "The script is manually invoked only — it is gated by (import.meta as any).main"
  artifacts:
    - path: "src/scripts/backfill-state-wide.ts"
      provides: "One-time operator CLI tool for repairing existing state-wide articles that were never flagged"
      contains: "backfill"
  key_links:
    - from: "src/scripts/backfill-state-wide.ts"
      to: "prisma.article.findMany + updateMany"
      via: "bezirke: { none: {} }, status: { in: ['PUBLISHED','WRITTEN','REVIEW'] }"
      pattern: "none.*\\{\\}"
---

<objective>
Create the one-time backfill script `src/scripts/backfill-state-wide.ts` that identifies existing articles with no ArticleBezirk rows and a terminal status (PUBLISHED, WRITTEN, REVIEW), then sets isStateWide=true on them.

Purpose: The pipeline bug has been running since launch — any articles that were already processed and left with no Bezirk associations are silently invisible to all readers. The backfill repairs those records. Must not auto-run on deploy — requires operator review via --dry-run first.
Output: src/scripts/backfill-state-wide.ts following the ai-run.ts CLI pattern.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-fix-state-wide-article-pipeline/11-CONTEXT.md
@.planning/phases/11-fix-state-wide-article-pipeline/11-01-SUMMARY.md

<interfaces>
<!-- CLI pattern to follow. From src/scripts/ai-run.ts -->
```typescript
import { prisma } from '../lib/prisma'

async function main(): Promise<void> {
  // ... logic ...
}

if ((import.meta as any).main) {
  main().catch((err) => {
    console.error('[script] Fatal:', err)
    process.exit(1)
  })
}
```

Backfill query pattern (from CONTEXT.md):
```typescript
const candidates = await prisma.article.findMany({
  where: {
    bezirke: { none: {} },
    status: { in: ['PUBLISHED', 'WRITTEN', 'REVIEW'] },
  },
  select: { id: true, title: true, status: true },
})
```

Bulk update:
```typescript
const result = await prisma.article.updateMany({
  where: { id: { in: candidates.map((a) => a.id) } },
  data: { isStateWide: true },
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backfill-state-wide.ts CLI script</name>
  <files>src/scripts/backfill-state-wide.ts</files>
  <action>
    Create `src/scripts/backfill-state-wide.ts` following the ai-run.ts pattern exactly.

    The script must:
    1. Import `prisma` singleton from `'../lib/prisma'`
    2. In `main()`:
       a. Read `process.argv.includes('--dry-run')` to set a dryRun flag
       b. Query candidates: `prisma.article.findMany({ where: { bezirke: { none: {} }, status: { in: ['PUBLISHED', 'WRITTEN', 'REVIEW'] } }, select: { id: true, title: true, status: true } })`
       c. Log: `[backfill] Found ${candidates.length} candidate(s)` — list each as `  id=${c.id} status=${c.status} title="${c.title}"`
       d. If dryRun: log `[backfill] Dry run — no changes written. Re-run without --dry-run to apply.` and return
       e. If no candidates: log `[backfill] Nothing to update.` and return
       f. Call `prisma.article.updateMany({ where: { id: { in: candidates.map(a => a.id) } }, data: { isStateWide: true } })`
       g. Log: `[backfill] Updated ${result.count} article(s) → isStateWide=true`
    3. Gate with `if ((import.meta as any).main) { main().catch(...) }` — same pattern as ai-run.ts

    Invocation (for operator reference in a comment at top of file):
    ```
    // Usage: bun run src/scripts/backfill-state-wide.ts [--dry-run]
    // Preview: bun run src/scripts/backfill-state-wide.ts --dry-run
    // Apply:   bun run src/scripts/backfill-state-wide.ts
    ```

    IMPORTANT: The query `bezirke: { none: {} }` correctly excludes mid-pipeline articles (FETCHED/TAGGED) via the status filter — those are not included in ['PUBLISHED','WRITTEN','REVIEW']. Do not add an explicit exclusion for FETCHED/TAGGED — the status filter already handles it.

    No test file is required for this script — it is a one-time operator tool with no business logic, analogous to ai-run.ts which also has no dedicated test.
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - src/scripts/backfill-state-wide.ts exists
    - `npx tsc --noEmit` produces no errors related to this file
    - Script is gated by (import.meta as any).main — not auto-executed on import
    - --dry-run flag prints candidates and exits without DB writes
    - Full test suite still passes: `npx vitest run`
  </done>
</task>

</tasks>

<verification>
1. `src/scripts/backfill-state-wide.ts` exists and TypeScript compiles without errors
2. Script is NOT auto-executed on import — main() only runs when file is executed directly
3. --dry-run flag exits without writing to DB
4. Query filter `bezirke: { none: {} }` + `status: { in: [...] }` targets only terminal-status articles
5. Full test suite still passes (no regressions from the new file)
</verification>

<success_criteria>
- Backfill script exists and compiles clean
- Operator can run --dry-run to preview affected articles before committing
- Script follows established ai-run.ts CLI pattern
- `npx vitest run` still green (192+ tests)
</success_criteria>

<output>
After completion, create `.planning/phases/11-fix-state-wide-article-pipeline/11-02-SUMMARY.md`
</output>
