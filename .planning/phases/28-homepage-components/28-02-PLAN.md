---
phase: 28-homepage-components
plan: "02"
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - src/components/reader/HomepageLayout.tsx
autonomous: false
requirements: [COMP-05, COMP-07]

must_haves:
  truths:
    - "The reader's selected Bezirk section appears at the top of content with a visually larger featured RegionalEditorialCard"
    - "Alternating homepage sections use tonal background shifts between background and surface with no border lines"
    - "MascotGreeting speech-bubble card appears on the homepage above the main content"
    - "The inline RegionalSelector is removed from the homepage body"
  artifacts:
    - path: "src/components/reader/HomepageLayout.tsx"
      provides: "Rewired homepage with MascotGreeting, tonal sections, RegionalEditorialCard in Mein Bezirk, no inline RegionalSelector"
      exports: ["HomepageLayout"]
  key_links:
    - from: "src/components/reader/HomepageLayout.tsx"
      to: "src/components/reader/MascotGreeting.tsx"
      via: "import and render"
      pattern: "MascotGreeting"
    - from: "src/components/reader/HomepageLayout.tsx"
      to: "src/components/reader/RegionalEditorialCard.tsx"
      via: "import and render in bezirk sections"
      pattern: "RegionalEditorialCard"
    - from: "src/components/reader/HomepageLayout.tsx"
      to: "src/components/reader/ListItem.tsx"
      via: "import for compact article rows in Mein Bezirk"
      pattern: "ListItem"
---

<objective>
Rewire HomepageLayout to deliver the full Wurzelwelt editorial homepage: MascotGreeting at top, tonal section alternation, Mein Bezirk with RegionalEditorialCard featured cards, and removal of inline RegionalSelector.

Purpose: This is the integration plan that wires Plan 01's components into the live homepage, completing all Phase 28 requirements.
Output: Updated HomepageLayout.tsx delivering the Wurzelwelt homepage experience.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-homepage-components/28-CONTEXT.md
@.planning/phases/28-homepage-components/28-01-SUMMARY.md

<interfaces>
<!-- Components created in Plan 01 -->

From src/components/reader/MascotGreeting.tsx:
```typescript
export function MascotGreeting(): JSX.Element;
// Self-contained — no props needed
```

From src/components/reader/RegionalEditorialCard.tsx:
```typescript
import type { ArticleWithBezirke } from "@/lib/content/articles";
export function RegionalEditorialCard({ article }: { article: ArticleWithBezirke }): JSX.Element;
```

From src/components/reader/ListItem.tsx (already exists):
```typescript
export function ListItem({ article }: { article: ArticleWithBezirke }): JSX.Element;
```

From src/components/reader/HeroArticle.tsx (restyled in Plan 01):
```typescript
export function HeroArticle({ article }: { article: ArticleWithBezirke }): JSX.Element;
```

Existing HomepageLayout props (unchanged):
```typescript
interface HomepageLayoutProps {
  hero: ArticleWithBezirke | null;
  pinnedArticles: ArticleWithBezirke[];
  allArticles: ArticleWithBezirke[];
  bezirke?: BezirkItem[];
}
```

Tonal backgrounds:
- background: #FCF9EF (bg-background)
- surface: #F6F4EA (bg-surface)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire HomepageLayout with Wurzelwelt components and tonal sections</name>
  <files>src/components/reader/HomepageLayout.tsx</files>
  <action>
Rewrite HomepageLayout.tsx to deliver the Wurzelwelt homepage. Keep the existing props interface and data logic (selectedSlugs, groupArticlesByBezirk, localStorage). Change the rendering structure:

**Imports to add:**
- `import { MascotGreeting } from "./MascotGreeting";`
- `import { RegionalEditorialCard } from "./RegionalEditorialCard";`

**Imports to remove:**
- Remove `RegionalSelector` import — the inline selector is removed from homepage (WurzelAppBar button handles bezirk selection)
- Remove `ArticleCard` import — replaced by RegionalEditorialCard for featured cards
- Remove `BezirkSection` import if present

**Layout structure (top to bottom):**

1. **HeroArticle** — unchanged, render if hero exists
2. **MascotGreeting** — render immediately after hero, wrapped in `<div className="bg-surface py-[var(--spacing-section)]">` for tonal contrast
3. **TopMeldungenRow** — keep if filteredPinned.length > 0, wrapped in `<div className="bg-background py-[var(--spacing-section)]">`
4. **AdUnit zone="hero"** — keep, in bg-surface section
5. **Mein Bezirk sections** (when hasBezirkSelection):
   - Section heading "Dein Bezirk" in font-headline text-xl font-semibold text-primary, with px-[var(--spacing-gutter)]
   - Each bezirk section: one `RegionalEditorialCard` as featured card + up to 3 `ListItem` compact rows below
   - Layout per bezirk: section heading (bezirk name in font-headline text-lg font-semibold text-primary), then RegionalEditorialCard for first article, then ListItems in bg-surface-elevated rounded-sm shadow-sm px-3 container
   - Max 3 list items per bezirk (not 4) per CONTEXT.md decision
   - Remove the Styrian flag accent div entirely from bezirk section headers — Wurzelwelt brand, no regional flag
   - Ad unit every 2nd section (keep existing pattern)
6. **Flat view** (when no bezirk selected):
   - Heading "Alle Nachrichten" in same style
   - First article as RegionalEditorialCard, next 3 as ListItems in compact container
   - Remainder articles after that in another ListItem container
7. **Empty state** — keep existing empty state logic

**Tonal section alternation (COMP-07):**
- Wrap each major section in alternating `bg-background` / `bg-surface` divs
- Pattern: hero (no bg needed — image fills), MascotGreeting (bg-surface), TopMeldungenRow (bg-background), Ad (bg-surface), first bezirk section (bg-background), second bezirk section (bg-surface), etc.
- Each section wrapper uses `py-[var(--spacing-section)]` for consistent vertical rhythm
- No border lines between sections — only tonal shift provides separation

**Keep unchanged:**
- selectedSlugs state + localStorage logic
- groupArticlesByBezirk call
- The openBezirkModal custom event listener pattern is NOT in HomepageLayout (it's in WurzelAppBar/BezirkModal) — no changes needed there
- The `bezirke` prop is still passed from page.tsx but no longer rendered inline — it stays in the interface for potential future use by other components

**Important:** Remove the `RegionalSelector` rendering block entirely (lines ~136-141 in current file). The WurzelAppBar bezirk button + BezirkModal is now the sole access point. Keep the `bezirke` prop in the interface but do not render RegionalSelector.
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && export PATH="$HOME/.nvm/versions/node/v25.8.0/bin:$PATH" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>HomepageLayout renders MascotGreeting, uses RegionalEditorialCard for featured cards, limits Mein Bezirk to 3 list items, alternates tonal backgrounds between sections, and has no inline RegionalSelector. TypeScript compiles clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Wurzelwelt homepage</name>
  <files>src/components/reader/HomepageLayout.tsx</files>
  <action>
Human verifies the complete Wurzelwelt homepage visually.

What was built: MascotGreeting speech bubble with Wurzelmann, restyled Topmeldung hero, RegionalEditorialCard featured cards, prioritized Mein Bezirk sections, tonal section alternation, no inline Bezirk selector.

Steps to verify:
1. Visit the homepage on localhost or Vercel deployment
2. Verify MascotGreeting speech bubble appears with "Wurzelmann sagt:" label and time-appropriate greeting
3. Verify Wurzelmann avatar (raw PNG, not circular) appears near the speech bubble
4. Verify the Topmeldung hero has rounded corners and gradient pill badge
5. Verify article sections alternate between warm cream and light sage backgrounds with no visible border lines
6. Select a Bezirk via the AppBar button — verify the featured card uses full-width aspect-video image with Newsreader headline
7. Verify max 3 compact ListItems below each featured card
8. Verify no inline RegionalSelector widget appears on the homepage body
  </action>
  <verify>User confirms visual correctness by typing "approved"</verify>
  <done>User has approved the Wurzelwelt homepage visual design and component layout</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Homepage renders without runtime errors
- MascotGreeting visible on homepage
- Tonal alternation visible (bg-background / bg-surface shifts)
- RegionalEditorialCard used for featured articles in Mein Bezirk
- No inline RegionalSelector on homepage
</verification>

<success_criteria>
The Wurzelwelt homepage delivers the full editorial experience: mascot greeting at top, featured hero with gradient overlay, regional editorial cards with serif headlines, prioritized Mein Bezirk sections with 1 featured + 3 compact items, and tonal section separation with no borders. User confirms visual correctness via checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/28-homepage-components/28-02-SUMMARY.md`
</output>
