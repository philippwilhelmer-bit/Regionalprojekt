---
phase: 43-ai-pipeline-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai/steps/merged.ts
  - src/lib/ai/steps/merged.test.ts
autonomous: true
requirements:
  - AIPL-01
  - AIPL-02
  - AIPL-03
  - AIPL-04
  - AIPL-05
must_haves:
  truths:
    - "A single Anthropic tool_use call returns all 8 fields ({bezirkSlugs, isStateWide, mentionsPrivateIndividual, headline, lead, body, seoTitle, metaDescription}) typed end-to-end with no `as any` casts"
    - "The system prompt is sent as a two-block array; the first block carries `cache_control: {type: 'ephemeral'}`, the second does not"
    - "`max_tokens: 1024` is set on the call and `stop_reason === 'max_tokens'` throws a descriptive error"
    - "`runMergedCall` returns split token counts: `inputTokens`, `cachedInputTokens`, `cacheCreationTokens`, `outputTokens`"
    - "If the model returns `isStateWide: true` with non-empty `bezirkSlugs`, the defensive guard clears `bezirkSlugs` to `[]`"
  artifacts:
    - path: "src/lib/ai/steps/merged.ts"
      provides: "runMergedCall, MergedResult, buildBezirkList, buildStaticPrefix, buildDynamicSuffix"
      exports: ["runMergedCall", "MergedResult", "buildBezirkList", "buildStaticPrefix", "buildDynamicSuffix"]
      min_lines: 200
    - path: "src/lib/ai/steps/merged.test.ts"
      provides: "Vitest unit coverage for AIPL-01..AIPL-05 against mocked Anthropic client"
      min_lines: 200
  key_links:
    - from: "src/lib/ai/steps/merged.ts"
      to: "@anthropic-ai/sdk"
      via: "client.messages.create({tools, tool_choice, system: [cached, dynamic]})"
      pattern: "tool_choice: \\{ type: 'tool', name: TOOL_NAME"
    - from: "src/lib/ai/steps/merged.ts"
      to: "src/lib/admin/ai-config-dal.ts"
      via: "getResolvedAiConfig(db, sourceId?)"
      pattern: "getResolvedAiConfig\\("
    - from: "src/lib/ai/steps/merged.ts"
      to: "response.usage"
      via: "cache_creation_input_tokens / cache_read_input_tokens"
      pattern: "cache_(creation|read)_input_tokens"
---

<objective>
Create `src/lib/ai/steps/merged.ts` implementing `runMergedCall` — the single typed `tool_use` Anthropic call that returns the 8-field merged structured output, with `cache_control: {type: 'ephemeral'}` on the static system prefix and explicit `max_tokens: 1024` + truncation guard.

Purpose: This is the cornerstone of Phase 43 — every downstream wave wires into the typed `MergedResult` returned here. The function is self-contained (no pipeline integration in this plan) so the cache-aware token split and the defensive `isStateWide` guard can be unit-tested in isolation against a mocked Anthropic client.

Output: One new implementation file, one new test file, fully green against the existing Vitest setup. Zero schema changes (`prisma db push` must remain a no-op for the whole phase).
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-ai-pipeline-quick-wins/43-CONTEXT.md
@.planning/phases/43-ai-pipeline-quick-wins/43-RESEARCH.md
@.planning/phases/43-ai-pipeline-quick-wins/43-VALIDATION.md
@.planning/drafts/43-01-merged-prompt-DRAFT.md
@.planning/drafts/43-01-test-fixtures-DRAFT.md
@CLAUDE.md
@AGENTS.md

# Reference source for reuse / pattern parity
@src/lib/ai/steps/step1-tag.ts
@src/lib/admin/ai-config-dal.ts

<interfaces>
<!-- Key types and contracts this plan creates and downstream plans consume. -->
<!-- Wave 2 (43-03 pipeline integration) imports `runMergedCall` and `MergedResult` from this file. -->

From `src/lib/ai/steps/merged.ts` (NEW — created by this plan):
```typescript
export interface MergedResult {
  // Tagging fields
  bezirkSlugs: string[]
  isStateWide: boolean
  mentionsPrivateIndividual: boolean
  // Writing fields
  headline: string
  lead: string
  body: string
  seoTitle: string
  metaDescription: string
  // Token accounting (cache-aware)
  inputTokens: number
  cachedInputTokens: number
  cacheCreationTokens: number
  outputTokens: number
}

export type BezirkInput = { slug: string; name: string; gemeindeSynonyms: string[] }

export function buildBezirkList(bezirke: BezirkInput[]): string
export function buildStaticPrefix(bezirkList: string): string
export function buildDynamicSuffix(config: ResolvedAiConfig): string

export async function runMergedCall(
  client: Anthropic,
  articleText: string,
  bezirke: BezirkInput[],
  db: PrismaClient,
  sourceId?: number,
): Promise<MergedResult>
```

Anthropic SDK shapes referenced (from `@anthropic-ai/sdk@^0.80.0`, already installed):
- `Anthropic.Messages.MessageCreateParamsNonStreaming` — `system` may be `string | TextBlockParam[]`; each `TextBlockParam` may carry `cache_control: {type: 'ephemeral'}`
- `response.usage` — includes `input_tokens`, `output_tokens`, optional `cache_creation_input_tokens`, `cache_read_input_tokens`
- `response.content[]` — `tool_use` block has `{type: 'tool_use', id, name, input}` where `input` is the typed JSON payload

From `src/lib/admin/ai-config-dal.ts` (already exists):
```typescript
export interface ResolvedAiConfig {
  tone: 'NEUTRAL' | 'FORMAL' | 'CONVERSATIONAL'
  articleLength: 'SHORT' | 'MEDIUM' | 'LONG'
  styleNotes: string | null
  modelOverride: string | null
}
export async function getResolvedAiConfig(db: PrismaClient, sourceId?: number): Promise<ResolvedAiConfig>
```

From `src/lib/ai/steps/step1-tag.ts:51-77` (reuse anchor — DO NOT import, replicate the format):
- `buildBezirkContext(bezirke)` produces one line per Bezirk: `${slug} (${name}): ${synonym1, synonym2, ...}` (slug only when synonyms empty). `buildBezirkList` in `merged.ts` replicates this verbatim.
- IMPORTANT: the `'steiermark-weit'` magic slug MUST NOT appear in the output of `buildBezirkList`. `isStateWide: boolean` replaces it at the schema boundary. The `prisma/seed-data/bezirke.ts` list does not contain a `'steiermark-weit'` entry; do not add one in this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write merged.test.ts unit suite (Wave 0 tests-first)</name>
  <files>src/lib/ai/steps/merged.test.ts</files>
  <behavior>
    Implement the 11 unit cases from `.planning/drafts/43-01-test-fixtures-DRAFT.md` (Part 1, lines 17-256). The full scaffold — `TEST_BEZIRKE`, `VALID_TOOL_INPUT`, `makeToolUseBlock`, `makeMockClient`, `makeMockDb` — is reproduced verbatim in the draft. Use it as-is; do not invent alternative helpers.

    The 11 cases (all under `describe('runMergedCall()')`):
    1. "parses all 8 fields from tool_use block" — asserts each field from VALID_TOOL_INPUT lands on `MergedResult` (AIPL-01)
    2. "uses tools + tool_choice (no output_config cast)" — asserts `args.tools[0].name === 'publish_article'`, `args.tool_choice === {type:'tool', name:'publish_article'}`, `args.output_config` is undefined (AIPL-02)
    3. "passes system as array with cache_control on prefix only" — asserts `system[0].cache_control === {type:'ephemeral'}` and `system[1].cache_control === undefined`; `system[0].text` contains 'VERFÜGBARE BEZIRKE'; `system[1].text` contains 'TON:' (AIPL-04)
    4. "Bezirk list is in static prefix (cacheable), not dynamic suffix" — asserts `staticBlock.text` contains 'graz (Graz (Stadt))' and `dynamicBlock.text` does NOT contain 'graz (' (AIPL-04)
    5. "uses max_tokens 1024" — asserts `args.max_tokens === 1024` (AIPL-03)
    6. "throws on stop_reason=max_tokens" — `await expect(...).rejects.toThrow(/truncated/i)` (AIPL-03)
    7. "throws when response has no tool_use block" — `await expect(...).rejects.toThrow(/tool_use/)` (AIPL-01)
    8. "defensive guard: isStateWide=true with non-empty bezirkSlugs returns empty bezirkSlugs" — asserts `result.isStateWide === true && result.bezirkSlugs.length === 0`
    9. "returns split token counts (input vs cached vs cacheCreation)" — asserts `inputTokens=80, cacheCreationTokens=1500, cachedInputTokens=1500, outputTokens=400` (AIPL-05)
    10. "treats missing cache_*_tokens fields as 0" — asserts `cachedInputTokens === 0 && cacheCreationTokens === 0` when usage omits them (AIPL-05)
    11. "per-source override: FORMAL tone reaches dynamic suffix, not cached prefix" — asserts `dynamicBlock.text` contains 'formell, seriös' and `staticBlock.text` does NOT
    12. (bonus from draft) "styleNotes appear in dynamic suffix when provided" — asserts `dynamicBlock.text` contains 'ZUSÄTZLICHE ANWEISUNGEN: Nutze immer kurze Sätze.'
  </behavior>
  <action>
Copy the test scaffold from `.planning/drafts/43-01-test-fixtures-DRAFT.md` lines 17-256 into `src/lib/ai/steps/merged.test.ts` verbatim. Do not modify the helper factories or test bodies — they are authoritative.

Important details (already present in the draft, do not re-derive):
- `makeMockDb` returns a `PrismaClient`-shaped duck-typed object exposing `aiConfig.findFirst` and `aiSourceConfig.findFirst`. Both have to be present because `getResolvedAiConfig(db, sourceId?)` reads from both. Default tone `NEUTRAL`, length `MEDIUM`, styleNotes `null`, modelOverride `null`.
- `makeToolUseBlock` returns a `{type: 'tool_use', id, name, input}` block matching the SDK's content-block shape.
- `makeMockClient` defaults to a `tool_use` content block with `stop_reason: 'tool_use'`, `input_tokens: 1500`, `output_tokens: 400`. Overrides allow `stop_reason`, `usage`, `content`.

This task RED-phase: the imports `import { runMergedCall, type MergedResult } from './merged'` will fail because `merged.ts` does not exist yet. That failure IS the RED gate. Commit the failing test, then proceed to Task 2.

Do NOT use `as any` anywhere. The `as unknown as Anthropic.Messages.MessageCreateParamsNonStreaming` cast on `spy.mock.calls[0][0]` is intentional (the SDK union is too wide for direct narrowing); keep it.

Do NOT add a "watch" mode; do NOT use `vi.useFakeTimers`. The tests are synchronous-await against a mocked client; they run in ~50ms.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai/steps/merged.test.ts 2>&1 | tail -30</automated>
    <!-- Expected RED state: 11 tests fail with "Failed to resolve import './merged'" because merged.ts does not exist. -->
    <!-- The failure IS the verification — Task 2 turns it green. -->
  </verify>
  <done>
    `src/lib/ai/steps/merged.test.ts` exists with 11 `it()` blocks matching the behavior list above. `npx vitest run src/lib/ai/steps/merged.test.ts` reports an import-resolution failure on `./merged` (RED). No other test file in `src/lib/ai/` regresses.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement runMergedCall in merged.ts (GREEN phase)</name>
  <files>src/lib/ai/steps/merged.ts</files>
  <behavior>
    Implement the full `runMergedCall` + supporting builders from `.planning/drafts/43-01-merged-prompt-DRAFT.md` lines 155-326. Each of the 11 unit tests from Task 1 must turn green. No additional assertions need to pass — but no test must regress either.

    Behavioral checklist (every item maps to a test from Task 1):
    - All 8 fields of the schema parse from `toolUse.input` and land on `MergedResult` (test 1)
    - The call uses `tools: [{name: 'publish_article', input_schema: MERGED_OUTPUT_SCHEMA}]` and `tool_choice: {type:'tool', name:'publish_article'}` — no `output_config`, no `as any` (test 2)
    - `system` is a two-element array: `[{type:'text', text: staticPrefix, cache_control:{type:'ephemeral'}}, {type:'text', text: dynamicSuffix}]` (test 3, 4)
    - `max_tokens: 1024` (test 5)
    - `stop_reason === 'max_tokens'` throws `new Error('Merged AI call truncated: ...')` matching `/truncated/i` (test 6)
    - Missing `tool_use` block throws `new Error('No tool_use block named "publish_article" in response')` matching `/tool_use/` (test 7)
    - Defensive guard: `const cleanBezirkSlugs = parsed.isStateWide ? [] : parsed.bezirkSlugs` (test 8)
    - Token accounting: `inputTokens = response.usage.input_tokens`, `cachedInputTokens = response.usage.cache_read_input_tokens ?? 0`, `cacheCreationTokens = response.usage.cache_creation_input_tokens ?? 0`, `outputTokens = response.usage.output_tokens` (tests 9, 10)
    - `getResolvedAiConfig(db, sourceId)` is awaited; `buildDynamicSuffix(config)` produces `TON: <tone>\nLÄNGE: <length>[\n\nZUSÄTZLICHE ANWEISUNGEN: <styleNotes>]` (tests 11, 12)
  </behavior>
  <action>
Copy the implementation from `.planning/drafts/43-01-merged-prompt-DRAFT.md` lines 155-326 into `src/lib/ai/steps/merged.ts` verbatim. The draft is authoritative — schema, prompt text, builder signatures, and the runMergedCall body are already finalized in CONTEXT.md and RESEARCH.md.

Imports to use exactly as shown in the draft:
- `import type Anthropic from '@anthropic-ai/sdk'`
- `import type { PrismaClient } from '@prisma/client'`
- `import { getResolvedAiConfig, type ResolvedAiConfig } from '../../admin/ai-config-dal'`

DO NOT extract a sub-helper that imports from `step1-tag.ts`. `buildBezirkList` MUST live in `merged.ts`; replicate the line format from `step1-tag.ts:51-77` (`${slug} (${name}): ${synonym1, synonym2, ...}`). This isolation is deliberate so `step1-tag.ts` can be deleted in v3.3 without touching `merged.ts`.

DO NOT use `as any`, `as Function`, or `output_config`. The whole point of AIPL-02 is the typed path. If TypeScript complains, the only acceptable narrowing is `toolUse.input as { bezirkSlugs: string[]; isStateWide: boolean; ... }` (typed cast on the parsed JSON, not on the SDK call args).

The `TOOL_NAME` constant is `'publish_article'`. The prompt text is German per the locked decisions in CONTEXT.md; copy the exact wording from the draft (lines 195-223 of `43-01-merged-prompt-DRAFT.md`).

The model default is `'claude-haiku-4-5-20251001'`, overridden by `resolvedConfig.modelOverride` when set.

Defensive guard placement: AFTER parsing `toolUse.input`, BEFORE building the `MergedResult` return value. Pattern: `const cleanBezirkSlugs = parsed.isStateWide ? [] : parsed.bezirkSlugs`. Use `cleanBezirkSlugs` (not `parsed.bezirkSlugs`) in the return.

Cache token field reads: `response.usage.cache_read_input_tokens ?? 0` and `response.usage.cache_creation_input_tokens ?? 0`. The `?? 0` is required because these fields are optional on Anthropic's usage type. Test 10 verifies this fallback.

  </action>
  <verify>
    <automated>npx vitest run src/lib/ai/steps/merged.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
    `src/lib/ai/steps/merged.ts` exists and exports `runMergedCall`, `MergedResult` (interface), `buildBezirkList`, `buildStaticPrefix`, `buildDynamicSuffix`. All 11+ tests in `merged.test.ts` pass. `npx vitest run src/lib/ai/` reports zero failing tests (existing `step1-tag.test.ts`, `step2-write.test.ts`, `step2-write-source-override.test.ts`, `circuit-breaker.test.ts`, `pipeline.test.ts` all still green). `grep -n "as any\|as Function\|output_config" src/lib/ai/steps/merged.ts` returns no matches. `npx tsc --noEmit` succeeds.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run src/lib/ai/steps/merged.test.ts` — 11+ cases green, zero red.
2. `npx vitest run src/lib/ai/` — entire AI suite green (no regression in step1/step2/pipeline/circuit-breaker tests).
3. `npx tsc --noEmit` — clean.
4. `grep -n "as any\|as Function\|output_config" src/lib/ai/steps/merged.ts` returns empty (AIPL-02 invariant).
5. `grep -c "cache_control" src/lib/ai/steps/merged.ts` returns at least 1 (AIPL-04 invariant).
6. `grep -n "max_tokens: 1024\|max_tokens: \\d" src/lib/ai/steps/merged.ts` shows exactly `max_tokens: 1024` (AIPL-03 invariant).
7. `grep -n "cache_creation_input_tokens\|cache_read_input_tokens" src/lib/ai/steps/merged.ts` returns at least 2 matches (AIPL-05 invariant).
8. `npx prisma db push --skip-generate --accept-data-loss` reports "Already in sync" — schema-free constraint upheld.
</verification>

<success_criteria>
- `runMergedCall(client, articleText, bezirke, db, sourceId?)` returns a fully-typed `MergedResult` from one `client.messages.create` call.
- All 11 unit tests pass; nothing else in `src/lib/ai/` regresses.
- The `system` array carries `cache_control: {type: 'ephemeral'}` on `system[0]` only.
- `max_tokens: 1024` with explicit `stop_reason === 'max_tokens'` throw.
- Cache-aware token split returned: `inputTokens`, `cachedInputTokens`, `cacheCreationTokens`, `outputTokens`.
- Defensive `isStateWide → []` guard active.
- Zero `as any` / `output_config` casts in the new file.
- Zero schema changes — `prisma db push` is a no-op.
</success_criteria>

<output>
After completion, create `.planning/phases/43-ai-pipeline-quick-wins/43-01-SUMMARY.md` documenting:
- The two committed files (`merged.ts`, `merged.test.ts`) with line counts.
- A short interface block (re-stating `MergedResult` and `runMergedCall` signatures) for downstream plans.
- Any deviations from the draft (expected: none — the draft is authoritative).
- The `npx vitest run src/lib/ai/` pass count after this plan lands.
</output>
