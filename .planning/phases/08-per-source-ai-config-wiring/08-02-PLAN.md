---
phase: 08-per-source-ai-config-wiring
plan: 02
type: tdd
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/lib/ingestion/ingest.ts
  - src/lib/ai/steps/step2-write.ts
  - src/lib/ai/pipeline.ts
  - src/lib/ai/steps/step2-write-source-override.test.ts
autonomous: true
requirements:
  - AICONF-02

must_haves:
  truths:
    - "An article created by ingest.ts has its sourceId set to the ingesting Source's id"
    - "runStep2Write() accepts an optional sourceId 5th parameter and calls getResolvedAiConfig(db, sourceId) instead of getAiConfig(db)"
    - "pipeline.ts passes article.sourceId to runStep2Write"
    - "The TODO comment in step2-write.ts is removed"
    - "With a per-source AiSourceConfig override, messages.create is called with the override model — confirmed by test"
    - "With sourceId = null/undefined, messages.create is called with the default global model — confirmed by test"
    - "All existing tests pass"
  artifacts:
    - path: "src/lib/ingestion/ingest.ts"
      provides: "sourceId: src.id on article.create()"
      contains: "sourceId"
    - path: "src/lib/ai/steps/step2-write.ts"
      provides: "optional sourceId 5th parameter, getResolvedAiConfig call"
      contains: "getResolvedAiConfig"
    - path: "src/lib/ai/pipeline.ts"
      provides: "article.sourceId passed to runStep2Write"
      contains: "article.sourceId"
    - path: "src/lib/ai/steps/step2-write-source-override.test.ts"
      provides: "Integration tests for AICONF-02 override and fallback paths"
      contains: "uses per-source AiSourceConfig override"
  key_links:
    - from: "src/lib/ingestion/ingest.ts"
      to: "src/lib/ai/pipeline.ts"
      via: "Article.sourceId set on create, read by processArticles()"
      pattern: "sourceId.*src\\.id"
    - from: "src/lib/ai/pipeline.ts"
      to: "src/lib/ai/steps/step2-write.ts"
      via: "article.sourceId passed as 5th argument"
      pattern: "runStep2Write.*article\\.sourceId"
    - from: "src/lib/ai/steps/step2-write.ts"
      to: "src/lib/admin/ai-config-dal.ts"
      via: "getResolvedAiConfig(db, sourceId ?? undefined)"
      pattern: "getResolvedAiConfig"
---

<objective>
Wire `Article.sourceId` through the full ingestion and pipeline path so that per-source AI config overrides actually affect article generation. Implement the two override tests (RED → GREEN) to prove the wiring is correct.

Purpose: Closes AICONF-02 — an editor's per-source tone/style settings now affect article generation.
Output: Three modified source files, one fleshed-out test file, all tests green.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/08-per-source-ai-config-wiring/08-CONTEXT.md
@.planning/phases/08-per-source-ai-config-wiring/08-RESEARCH.md
@.planning/phases/08-per-source-ai-config-wiring/08-01-SUMMARY.md
</context>

<interfaces>
<!-- src/lib/ai/steps/step2-write.ts — CURRENT signature to extend -->
<!--
export async function runStep2Write(
  client: Anthropic,
  articleText: string,
  bezirkNames: string[],
  db?: PrismaClient          // existing optional 4th param
): Promise<Step2Result>

// Inside: replace these 5 lines:
  const aiConfig = await getAiConfig(db)
  const resolvedConfig: ResolvedAiConfig = {
    tone: aiConfig.tone,
    articleLength: aiConfig.articleLength,
    styleNotes: aiConfig.styleNotes,
    modelOverride: aiConfig.modelOverride,
  }
// With single call:
  const resolvedConfig = await getResolvedAiConfig(db, sourceId ?? undefined)
-->

<!-- src/lib/admin/ai-config-dal.ts — getResolvedAiConfig signature -->
<!--
export async function getResolvedAiConfig(
  dbOrSourceId: PrismaClient | number,
  sourceId?: number
): Promise<ResolvedAiConfig>
// Call with injected DB: getResolvedAiConfig(db, sourceId ?? undefined)
// When sourceId is undefined → returns global AiConfig (null sourceId fallthrough confirmed)
-->

<!-- src/lib/ai/pipeline.ts — CURRENT runStep2Write call site (line ~148) -->
<!--
const step2 = await runStep2Write(anthropicClient, articleText, matchedBezirkNames, db)
// Becomes:
const step2 = await runStep2Write(anthropicClient, articleText, matchedBezirkNames, db, article.sourceId ?? undefined)
-->

<!-- src/lib/ingestion/ingest.ts — CURRENT article.create() call (lines 115-126) -->
<!--
await db.article.create({
  data: {
    externalId: item.externalId,
    source: src.type,
    // ADD:  sourceId: src.id,
    status: 'FETCHED',
    title: item.title,
    content: item.body,
    contentHash,
    rawPayload: item.rawPayload ?? undefined,
    publishedAt: item.publishedAt ?? undefined,
  },
})
-->

<!-- src/test/setup-db.ts — test DB setup (createTestDb picks up new migration automatically) -->
<!--
import { createTestDb, cleanDb } from '../../../test/setup-db'
// createTestDb() applies all migrations via sorted readdirSync scan — the new
// 20260323_phase8_article_source_id migration is picked up automatically.
-->
</interfaces>

<feature>
  <name>Per-source AI config override in runStep2Write</name>
  <files>src/lib/ai/steps/step2-write-source-override.test.ts, src/lib/ai/steps/step2-write.ts, src/lib/ai/pipeline.ts, src/lib/ingestion/ingest.ts</files>
  <behavior>
    RED phase — implement two test cases in step2-write-source-override.test.ts (currently it.todo):
    - Case 1 (override): Create a Source row + AiSourceConfig with modelOverride='claude-opus-4-5', call runStep2Write(..., db, src.id), assert messages.create called with { model: 'claude-opus-4-5' }
    - Case 2 (fallback): No AiSourceConfig for source, call runStep2Write(..., db, undefined), assert messages.create called with { model: default_model } (e.g. 'claude-haiku-4-5-20251001')

    Test structure:
    - Import: describe, it, expect, vi, beforeEach, afterEach from vitest
    - Import: runStep2Write from ./step2-write
    - Import: createTestDb, cleanDb from ../../../test/setup-db
    - Import: type Anthropic from @anthropic-ai/sdk
    - Use real pgLite DB (createTestDb()) — NOT a mock DB
    - Fake Anthropic client: { messages: { create: vi.fn().mockResolvedValue(cannedResponse) } } as unknown as Anthropic
    - Canned response must match Step2 JSON schema shape: { headline, lead, body, seoTitle, metaDescription } in a text content block with usage object
    - Create Source row inline with db.source.create({ data: { type: 'OTS_AT', url: 'https://ots.at' } })
    - Create AiSourceConfig with db.aiSourceConfig.create({ data: { sourceId: src.id, modelOverride: 'claude-opus-4-5' } })
    - For the global fallback test, also call db.aiConfig.upsert or create to set a known default model (or rely on getAiConfig find-or-create default)
    - cleanDb() in afterEach (not beforeEach) to preserve DB between steps within a test

    GREEN phase — implement the three source file changes:
    1. ingest.ts: add `sourceId: src.id` to article.create() data object
    2. step2-write.ts: add sourceId? as 5th param, replace getAiConfig+manual construction with getResolvedAiConfig(db, sourceId ?? undefined), remove TODO comment, add import for getResolvedAiConfig (already partially imported — check existing imports)
    3. pipeline.ts: pass article.sourceId ?? undefined as 5th arg to runStep2Write
  </behavior>
  <implementation>
    Follow RED → GREEN → (no refactor needed — changes are minimal).

    RED: implement the two test bodies in step2-write-source-override.test.ts. Run `npx vitest run src/lib/ai/steps/step2-write-source-override.test.ts` — both tests MUST fail (step2-write.ts still uses old signature). Commit: `test(08-02): add failing override tests for AICONF-02`

    GREEN: apply the three source file changes (ingest.ts, step2-write.ts, pipeline.ts). Run `npx vitest run src/lib/ai/steps/step2-write-source-override.test.ts` — both tests MUST pass. Run `npx vitest run` — full suite green. Commit: `feat(08-02): wire Article.sourceId through pipeline for per-source AI config (AICONF-02)`

    Import note for step2-write.ts: the file currently imports `getAiConfig` from `../../admin/ai-config-dal`. After the change, import `getResolvedAiConfig` instead (or alongside if getAiConfig is still used elsewhere — it is NOT used elsewhere in this file, so replace the import). Remove the `ResolvedAiConfig` manual construction and the `getAiConfig` import.

    Existing call sites of runStep2Write (pipeline.ts only) — the 5th param is optional so the change is additive. Existing step2-write.test.ts uses a mock DB that returns a canned aiConfig — after the change it must return a canned ResolvedAiConfig from getResolvedAiConfig. Check whether existing mock DB in step2-write.test.ts needs updating. If the mock provides `aiConfig.findFirst` but getResolvedAiConfig calls both `aiSourceConfig.findFirst` and `aiConfig.findFirst`, the existing mock may need an `aiSourceConfig` stub added. Inspect step2-write.test.ts carefully before implementing.
  </implementation>
</feature>

<verification>
After full RED → GREEN cycle:
- `npx vitest run src/lib/ai/steps/step2-write-source-override.test.ts` — 2/2 tests pass
- `npx vitest run` — full suite green (all existing tests still pass)
- `npx tsc --noEmit` — exits 0
- `grep "TODO" src/lib/ai/steps/step2-write.ts` — no TODO comment found
- `grep "getResolvedAiConfig" src/lib/ai/steps/step2-write.ts` — present
- `grep "sourceId" src/lib/ingestion/ingest.ts` — present in article.create()
- `grep "article.sourceId" src/lib/ai/pipeline.ts` — present in runStep2Write call
</verification>

<success_criteria>
- Two override tests (AICONF-02) pass: override config used when sourceId present, global config used when sourceId null
- ingest.ts sets sourceId: src.id on article.create()
- step2-write.ts calls getResolvedAiConfig(db, sourceId ?? undefined) — TODO comment removed
- pipeline.ts passes article.sourceId ?? undefined to runStep2Write
- Full vitest suite green
- tsc --noEmit exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-per-source-ai-config-wiring/08-02-SUMMARY.md`
</output>
