---
phase: 08-per-source-ai-config-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260323_phase8_article_source_id/migration.sql
  - src/lib/ai/steps/step2-write-source-override.test.ts
autonomous: true
requirements:
  - AICONF-02

must_haves:
  truths:
    - "prisma generate succeeds after schema change — no TypeScript errors on Article.sourceId"
    - "The new migration SQL file sorts after 20260323_phase6_publicid alphabetically"
    - "The test stub file exists with two it.todo() cases — one for override path, one for null/global fallback path"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Article.sourceId Int? FK to Source, Source.articles reverse relation, @@index([sourceId])"
      contains: "sourceId"
    - path: "prisma/migrations/20260323_phase8_article_source_id/migration.sql"
      provides: "ALTER TABLE ADD COLUMN sourceId with FK and index DDL"
      contains: "ALTER TABLE"
    - path: "src/lib/ai/steps/step2-write-source-override.test.ts"
      provides: "Wave 0 test stubs for AICONF-02"
      exports: ["describe('runStep2Write() — per-source override')"]
  key_links:
    - from: "prisma/schema.prisma"
      to: "prisma/migrations/20260323_phase8_article_source_id/migration.sql"
      via: "createTestDb() sorted migration scan"
      pattern: "sourceId.*INTEGER.*REFERENCES.*Source"
    - from: "src/lib/ai/steps/step2-write-source-override.test.ts"
      to: "src/lib/ai/steps/step2-write.ts"
      via: "import runStep2Write"
      pattern: "from.*step2-write"
---

<objective>
Add `Article.sourceId` nullable FK to the Prisma schema, write the hand-crafted migration SQL, run `prisma generate`, and create the Wave 0 test stub file that Plan 02 will make pass.

Purpose: Establishes the DB contract that all pipeline wiring in Plan 02 depends on. The test stub ensures Plan 02 follows RED → GREEN order.
Output: Updated schema.prisma, migration SQL directory, generated Prisma client types, and a stub test file with two it.todo() cases.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/08-per-source-ai-config-wiring/08-CONTEXT.md
@.planning/phases/08-per-source-ai-config-wiring/08-RESEARCH.md
</context>

<interfaces>
<!-- Existing Article model fields in prisma/schema.prisma (excerpt) — add sourceId alongside these -->
<!--
model Article {
  id              Int           @id @default(autoincrement())
  externalId      String?
  source          ArticleSource            ← existing enum field — DO NOT rename; new FK uses different field name
  sourceId        Int?                     ← ADD THIS
  sourceFk        Source?   @relation(...)  ← ADD THIS (name must differ from 'source' enum field)
  status          ArticleStatus @default(FETCHED)
  ...
  @@index([status])
  @@index([publicId])
  @@index([sourceId])                      ← ADD THIS
}

model Source {
  id              Int           @id @default(autoincrement())
  ...
  articles        Article[]               ← ADD THIS reverse relation
  runs            IngestionRun[]
  aiSourceConfig  AiSourceConfig?
}
-->

<!-- Existing migration pattern (from 20260323_phase6_publicid/migration.sql): -->
<!--
ALTER TABLE "Article" ADD COLUMN "publicId" TEXT;
CREATE UNIQUE INDEX "Article_publicId_key" ON "Article"("publicId") WHERE "publicId" IS NOT NULL;
-->

<!-- Phase 5 FK DDL pattern (from 20260322_phase5/migration.sql): -->
<!--
-- FK with ON DELETE CASCADE (existing pattern reference — Phase 8 uses SET NULL instead):
REFERENCES "Source"("id") ON DELETE CASCADE
-->

<!-- Fake Anthropic client shape used in step2-write.test.ts (reference for stub): -->
<!--
const defaultResponse = {
  content: [{ type: 'text' as const, text: JSON.stringify({ headline, lead, body, seoTitle, metaDescription }) }],
  usage: { input_tokens: 200, output_tokens: 150 },
}
const mockClient = { messages: { create: vi.fn().mockResolvedValue(defaultResponse) } } as unknown as Anthropic
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Article.sourceId FK to Prisma schema + migration SQL + prisma generate</name>
  <files>
    prisma/schema.prisma,
    prisma/migrations/20260323_phase8_article_source_id/migration.sql
  </files>
  <action>
    1. Open `prisma/schema.prisma`.

    2. In the `Article` model, add after the existing `source ArticleSource` field:
       ```
       sourceId  Int?
       sourceFk  Source?   @relation(fields: [sourceId], references: [id], onDelete: SetNull)
       ```
       IMPORTANT: The field name for the relation must be `sourceFk` (not `source`) — the Article model already has `source ArticleSource` and Prisma forbids duplicate field names.

    3. In the `Article` model, add `@@index([sourceId])` alongside the existing index lines.

    4. In the `Source` model, add `articles  Article[]` as a reverse relation alongside the existing `runs` and `aiSourceConfig` relations.

    5. Create directory `prisma/migrations/20260323_phase8_article_source_id/` and write `migration.sql`:
       ```sql
       -- Phase 8: Add Article.sourceId nullable FK to Source
       -- onDelete: SET NULL — articles survive Source deletion, fall through to global AI config
       ALTER TABLE "Article" ADD COLUMN "sourceId" INTEGER REFERENCES "Source"("id") ON DELETE SET NULL;
       CREATE INDEX "Article_sourceId_idx" ON "Article"("sourceId");
       ```

    6. Run `npx prisma generate` to regenerate the Prisma client types with the new `sourceId` field.

    7. Run `npx tsc --noEmit` to confirm no TypeScript errors from the schema change.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    `tsc --noEmit` exits 0. `prisma/schema.prisma` contains `sourceId Int?` and `sourceFk Source?` in Article model and `articles Article[]` in Source model. Migration SQL file exists at `prisma/migrations/20260323_phase8_article_source_id/migration.sql`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 — write step2-write-source-override.test.ts stub</name>
  <files>src/lib/ai/steps/step2-write-source-override.test.ts</files>
  <behavior>
    - Test 1: sourceId with AiSourceConfig override → `messages.create` called with the override model
    - Test 2: sourceId = null/undefined → `messages.create` called with default global model
  </behavior>
  <action>
    Create `src/lib/ai/steps/step2-write-source-override.test.ts` with two it.todo() stubs. Import structure must compile without TypeScript errors. Use the same import path pattern as `step2-write.test.ts`.

    ```typescript
    import { describe, it } from 'vitest'
    // Implementation target: src/lib/ai/steps/step2-write.ts

    describe('runStep2Write() — per-source override', () => {
      it.todo('uses per-source AiSourceConfig override when sourceId is set')
      it.todo('falls through to global AiConfig when sourceId is null/undefined')
    })
    ```

    Do NOT import `runStep2Write` yet in this stub — keep it importless so it compiles even if step2-write.ts has TypeScript errors mid-task. Plan 02 will flesh out the implementations.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ai/steps/step2-write-source-override.test.ts</automated>
  </verify>
  <done>
    Vitest reports the file found, 0 tests run (both are todo), 0 failures. File compiles cleanly under tsc --noEmit.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `npx tsc --noEmit` exits 0
- `npx vitest run src/lib/ai/steps/step2-write-source-override.test.ts` runs with 0 failures (todos shown)
- `ls prisma/migrations/20260323_phase8_article_source_id/migration.sql` exists
- `grep -c "sourceId" prisma/schema.prisma` returns >= 2 (field + index line)
</verification>

<success_criteria>
- Article.sourceId Int? and sourceFk Source? added to Prisma schema with onDelete: SetNull
- Source.articles Article[] reverse relation added
- @@index([sourceId]) added in Article model
- Hand-crafted migration SQL exists at correct timestamp-prefixed path
- prisma generate succeeds
- tsc --noEmit exits 0
- Test stub file exists with two it.todo() cases, vitest finds it, 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/08-per-source-ai-config-wiring/08-01-SUMMARY.md`
</output>
