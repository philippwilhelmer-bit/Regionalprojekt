---
phase: 26-design-system-brand-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
autonomous: true
requirements: [DS-01, DS-02, DS-03]

must_haves:
  truths:
    - "The Tailwind theme contains only semantic color tokens (primary, secondary, accent, background, text, surface) with no legacy color names"
    - "Body text and UI labels render in Plus Jakarta Sans"
    - "All icons render in the Rounded variant of Material Symbols"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Wurzelwelt color tokens, font tokens, radius baseline"
      contains: "@theme"
    - path: "src/app/layout.tsx"
      provides: "Plus Jakarta Sans font loading, Material Symbols Rounded CDN"
      contains: "Plus_Jakarta_Sans"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS variable bridge (--font-jakarta on html, @theme references it)"
      pattern: "--font-jakarta"
---

<objective>
Replace the v1.1 design token foundation with the Wurzelwelt design system: new color palette, Plus Jakarta Sans font, and Material Symbols Rounded icons.

Purpose: All subsequent plans (border migration, component restyling, brand rename) depend on these tokens existing. This is the single source of truth for the Wurzelwelt visual language.
Output: Updated globals.css with new @theme tokens, updated layout.tsx with new font loading and icon CDN.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-design-system-brand-foundation/26-CONTEXT.md

<interfaces>
<!-- Current state of files being modified -->

From src/app/globals.css (current v1.1 tokens to replace):
```css
@theme {
  --color-styrian-green: #2D5A27;
  --color-cream: #fbfaee;
  --color-primary: #2D5A27;
  --color-background: #FBFAEE;
  --color-alpine-red: #8b0000;
  --color-sage: #4a5d4e;
  --color-cream-dark: #F5F4E8;
  --font-headline: var(--font-newsreader), serif;
  --font-body: var(--font-inter), sans-serif;
  --font-label: var(--font-work-sans), sans-serif;
  --radius-sm: 2px;
}
```

From src/app/layout.tsx (current font imports to replace):
```typescript
import { Newsreader, Inter, Work_Sans } from "next/font/google";
// Inter → Plus_Jakarta_Sans
// Work_Sans → removed (Plus Jakarta Sans covers both body + labels)
// Material Symbols Outlined → Rounded in CDN URL
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Tailwind theme tokens with Wurzelwelt color palette and font references</name>
  <files>src/app/globals.css</files>
  <action>
Replace the entire @theme block in globals.css with the Wurzelwelt design system tokens:

**Colors (semantic names only, clean break from v1.1):**
- --color-primary: #1B2D18 (forest green)
- --color-secondary: #4A5D23 (moss green)
- --color-accent: #9F411E (terracotta)
- --color-background: #FCF9EF (warm cream)
- --color-text: #071806 (near-black green)
- --color-surface: #F6F4EA (slightly darker cream for alternating sections)
- --color-surface-elevated: #FFFFFF (white, for cards/modals above surface)
- --color-primary-container: #4A5D23 (for CTA gradient endpoint)

Remove ALL legacy tokens: styrian-green, cream, cream-dark, alpine-red, sage, primary (old hex).

**Fonts:**
- --font-headline: var(--font-newsreader), serif (unchanged)
- --font-body: var(--font-jakarta), sans-serif (was --font-inter)
- --font-label: var(--font-jakarta), sans-serif (was --font-work-sans, now same as body)

**Radius:**
- --radius-sm: 0.75rem (was 2px — the new baseline for interactive elements)

**Spacing (custom properties for organic scale):**
- --spacing-gutter: 1.7rem (mobile gutters)
- --spacing-section: 4rem (section gaps)

Also update the body rule to use the new font-body token. Keep .img-matte and .scrollbar-none utilities unchanged.
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && grep -c "styrian-green\|cream-dark\|alpine-red\|--color-sage\|--font-inter\|--font-work-sans" src/app/globals.css | grep "^0$" && grep -q "#1B2D18" src/app/globals.css && grep -q "#FCF9EF" src/app/globals.css && grep -q "0.75rem" src/app/globals.css && echo "PASS"</automated>
  </verify>
  <done>globals.css contains only Wurzelwelt semantic tokens, zero legacy v1.1 color names, 0.75rem radius baseline, organic spacing custom properties</done>
</task>

<task type="auto">
  <name>Task 2: Swap font imports to Plus Jakarta Sans and icon CDN to Rounded variant</name>
  <files>src/app/layout.tsx</files>
  <action>
1. Replace font imports:
   - Remove `Inter` and `Work_Sans` imports from "next/font/google"
   - Add `Plus_Jakarta_Sans` import from "next/font/google"
   - Keep `Newsreader` import unchanged

2. Configure Plus Jakarta Sans:
   ```typescript
   const plusJakartaSans = Plus_Jakarta_Sans({
     weight: ['400', '500', '600', '700'],
     subsets: ['latin'],
     display: 'swap',
     variable: '--font-jakarta',
   });
   ```

3. Remove the `inter` and `workSans` const declarations.

4. Update html className: replace `${inter.variable} ${workSans.variable}` with `${plusJakartaSans.variable}`

5. Replace Material Symbols CDN link:
   - Change "Material+Symbols+Outlined" to "Material+Symbols+Rounded" in the href URL
   - Keep all other parameters (opsz, wght, FILL, GRAD) identical

6. Update body className: replace `bg-cream` with `bg-background` (the new semantic token name)

7. Update metadata description to "Nachrichten aus der Steiermark" (keep config.siteName for title).
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && grep -q "Plus_Jakarta_Sans" src/app/layout.tsx && grep -q "Material+Symbols+Rounded" src/app/layout.tsx && grep -q "font-jakarta" src/app/layout.tsx && ! grep -q "Inter\b" src/app/layout.tsx && ! grep -q "Work_Sans" src/app/layout.tsx && ! grep -q "bg-cream" src/app/layout.tsx && echo "PASS"</automated>
  </verify>
  <done>layout.tsx loads Plus Jakarta Sans (400-700) as --font-jakarta, Newsreader unchanged, Material Symbols Rounded in CDN link, body uses bg-background</done>
</task>

</tasks>

<verification>
- `grep -r "styrian-green\|alpine-red\|--color-sage\|cream-dark" src/app/globals.css` returns nothing
- `grep "Plus_Jakarta_Sans" src/app/layout.tsx` finds the import
- `grep "Rounded" src/app/layout.tsx` finds the CDN link
- `npx next build` completes without errors (font and token compilation)
</verification>

<success_criteria>
1. globals.css @theme block contains ONLY Wurzelwelt semantic tokens (primary, secondary, accent, background, text, surface, surface-elevated, primary-container)
2. No legacy color names exist in globals.css
3. layout.tsx imports Plus_Jakarta_Sans (not Inter or Work_Sans) and sets --font-jakarta variable
4. Material Symbols CDN URL references "Rounded" not "Outlined"
5. Build succeeds (note: components referencing old token names will show warnings or fail in Plan 02 scope)
</success_criteria>

<output>
After completion, create `.planning/phases/26-design-system-brand-foundation/26-01-SUMMARY.md`
</output>
