---
phase: 31-icon-token-consistency-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/reader/WurzelNavBar.tsx
  - src/components/reader/WurzelAppBar.tsx
  - src/components/reader/EilmeldungBanner.tsx
  - src/components/reader/ShareButton.tsx
  - src/components/reader/BottomNavClient.tsx
  - src/components/reader/SearchPageLayout.tsx
  - src/components/reader/RegionalAppBar.tsx
  - src/components/reader/Header.tsx
  - src/components/reader/RegionalNavBar.tsx
  - src/components/reader/MascotGreeting.tsx
  - src/components/reader/RegionalEditorialCard.tsx
  - src/components/reader/HomepageLayout.tsx
  - src/app/layout.tsx
  - src/app/__tests__/public-layout-metadata.test.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - DS-03

must_haves:
  truths:
    - "All icon spans use material-symbols-rounded class matching the CDN-loaded Rounded variant"
    - "No text-zinc-* tokens remain in Phase 28 components (MascotGreeting, RegionalEditorialCard, HomepageLayout) or layout.tsx body"
    - "Test mocks in public-layout-metadata.test.ts reference WurzelAppBar/WurzelNavBar (not stale RegionalAppBar/RegionalNavBar)"
    - "REQUIREMENTS.md traceability table shows DS-04 through DS-07 as Complete"
  artifacts:
    - path: "src/components/reader/WurzelNavBar.tsx"
      provides: "Icon class fix"
      contains: "material-symbols-rounded"
    - path: "src/components/reader/WurzelAppBar.tsx"
      provides: "Icon class fix"
      contains: "material-symbols-rounded"
    - path: "src/components/reader/MascotGreeting.tsx"
      provides: "Zinc token removal"
      contains: "text-text"
    - path: "src/app/__tests__/public-layout-metadata.test.ts"
      provides: "Updated test mock"
      contains: "WurzelAppBar"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability update"
      contains: "DS-04.*Complete"
  key_links:
    - from: "src/app/layout.tsx"
      to: "CDN stylesheet"
      via: "link rel=stylesheet for Material+Symbols+Rounded"
      pattern: "Material.*Symbols.*Rounded"
    - from: "all icon spans"
      to: "CDN font"
      via: "material-symbols-rounded class"
      pattern: "material-symbols-rounded"
---

<objective>
Fix icon class mismatch, replace residual zinc tokens with semantic design tokens, correct stale test mocks, and update REQUIREMENTS.md traceability.

Purpose: Close DS-03 requirement gap — the CDN loads Material Symbols Rounded but all component spans use the `material-symbols-outlined` class, causing icon rendering to fail. Additionally, Phase 28 components and layout.tsx still use legacy `text-zinc-*` Tailwind classes instead of the Wurzelwelt semantic `text-text` token.
Output: All icon classes aligned with CDN, zinc tokens replaced with semantic tokens, test mocks updated, traceability table corrected.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace material-symbols-outlined with material-symbols-rounded and remove zinc tokens</name>
  <files>
    src/components/reader/WurzelNavBar.tsx
    src/components/reader/WurzelAppBar.tsx
    src/components/reader/EilmeldungBanner.tsx
    src/components/reader/ShareButton.tsx
    src/components/reader/BottomNavClient.tsx
    src/components/reader/SearchPageLayout.tsx
    src/components/reader/RegionalAppBar.tsx
    src/components/reader/Header.tsx
    src/components/reader/RegionalNavBar.tsx
    src/components/reader/MascotGreeting.tsx
    src/components/reader/RegionalEditorialCard.tsx
    src/components/reader/HomepageLayout.tsx
    src/app/layout.tsx
  </files>
  <action>
    **Icon class fix:** In all 9 files that contain `material-symbols-outlined`, replace every occurrence with `material-symbols-rounded`. The files are: WurzelNavBar.tsx, WurzelAppBar.tsx, EilmeldungBanner.tsx, ShareButton.tsx, BottomNavClient.tsx, SearchPageLayout.tsx, RegionalAppBar.tsx, Header.tsx, RegionalNavBar.tsx.

    **Zinc token replacement in Phase 28 components AND layout.tsx body:** Replace all `text-zinc-*` classes with Wurzelwelt semantic equivalents. Apply these mappings:

    - `text-zinc-900` -> `text-text` (full darkness, maps to #071806)
    - `text-zinc-800` -> `text-text` (close enough to full)
    - `text-zinc-700` -> `text-text/70` (70% opacity)
    - `text-zinc-600` -> `text-text/60` (60% opacity)
    - `text-zinc-500` -> `text-text/50` (50% opacity)
    - `text-zinc-400` -> `text-text/40` (40% opacity)
    - `text-zinc-300` -> `text-text/30` (30% opacity)
    - `bg-zinc-100` -> `bg-surface` (if any exist)
    - `hover:text-zinc-*` variants follow same pattern (e.g., `hover:text-zinc-700` -> `hover:text-text/70`)

    Files to fix for zinc tokens (Phase 28 components per success criteria):
    - `src/components/reader/MascotGreeting.tsx`: text-zinc-900 -> text-text, text-zinc-600 -> text-text/60
    - `src/components/reader/RegionalEditorialCard.tsx`: text-zinc-900 -> text-text, text-zinc-400 -> text-text/40
    - `src/components/reader/HomepageLayout.tsx`: text-zinc-500 -> text-text/50
    - `src/app/layout.tsx` body class: text-zinc-900 -> text-text

    IMPORTANT: Only fix zinc tokens in Phase 28 components and layout.tsx as specified by the success criteria. Other components with zinc tokens are out of scope for this phase.
  </action>
  <verify>
    <automated>grep -rn "material-symbols-outlined" src/ && echo "FAIL: outlined still present" || echo "PASS: no outlined classes"; grep -n "text-zinc-" src/components/reader/MascotGreeting.tsx src/components/reader/RegionalEditorialCard.tsx src/components/reader/HomepageLayout.tsx src/app/layout.tsx && echo "FAIL: zinc tokens remain" || echo "PASS: no zinc in scoped files"</automated>
  </verify>
  <done>
    - Zero occurrences of `material-symbols-outlined` in src/
    - Zero occurrences of `text-zinc-*` in MascotGreeting.tsx, RegionalEditorialCard.tsx, HomepageLayout.tsx, and layout.tsx body
    - All icon spans use `material-symbols-rounded` matching the CDN font
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix stale test mock and update REQUIREMENTS.md traceability</name>
  <files>
    src/app/__tests__/public-layout-metadata.test.ts
    .planning/REQUIREMENTS.md
  </files>
  <action>
    **Test mock fix:** In `src/app/__tests__/public-layout-metadata.test.ts`, the layout imports `WurzelAppBar` and `WurzelNavBar` but the test mocks the old `RegionalAppBar` and `RegionalNavBar`. Update the two stale mock blocks:

    Replace:
    ```
    vi.mock('@/components/reader/RegionalAppBar', () => ({
      RegionalAppBar: () => null,
    }))
    vi.mock('@/components/reader/RegionalNavBar', () => ({
      RegionalNavBar: () => null,
    }))
    ```

    With:
    ```
    vi.mock('@/components/reader/WurzelAppBar', () => ({
      WurzelAppBar: () => null,
    }))
    vi.mock('@/components/reader/WurzelNavBar', () => ({
      WurzelNavBar: () => null,
    }))
    ```

    **REQUIREMENTS.md traceability update:** In the traceability table at the bottom of `.planning/REQUIREMENTS.md`, change the Status column for DS-04, DS-05, DS-06, and DS-07 from their current values to `Complete`. These were completed in Phase 26 but the table was not updated. Also change DS-03 status to `Complete` since this plan closes it.
  </action>
  <verify>
    <automated>cd /Users/philipp/Claudebot/Regionalprojekt && npx vitest run src/app/__tests__/public-layout-metadata.test.ts 2>&1 | tail -20; grep -E "DS-0[3-7]" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - Test passes with no "Cannot find module" or mock mismatch errors
    - REQUIREMENTS.md shows DS-03 through DS-07 all as "Complete"
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "material-symbols-outlined" src/` returns zero matches
2. `grep -n "text-zinc-" src/components/reader/MascotGreeting.tsx src/components/reader/RegionalEditorialCard.tsx src/components/reader/HomepageLayout.tsx src/app/layout.tsx` returns zero matches
3. `npx vitest run src/app/__tests__/public-layout-metadata.test.ts` passes
4. `grep -E "DS-0[3-7].*Complete" .planning/REQUIREMENTS.md` shows 5 lines
</verification>

<success_criteria>
- All components use `.material-symbols-rounded` class matching the CDN stylesheet
- No `text-zinc-*` tokens remain in Phase 28 components or layout.tsx body
- Test mocks reference current WurzelAppBar/WurzelNavBar components
- REQUIREMENTS.md traceability table shows DS-03 through DS-07 as "Complete"
</success_criteria>

<output>
After completion, create `.planning/phases/31-icon-token-consistency-fix/31-01-SUMMARY.md`
</output>
