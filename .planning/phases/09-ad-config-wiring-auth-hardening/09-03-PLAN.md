---
phase: 09-ad-config-wiring-auth-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(public)/impressum/page.tsx
  - src/app/(public)/artikel/[publicId]/[slug]/page.tsx
autonomous: true
requirements:
  - AD-02
must_haves:
  truths:
    - "Impressum page renders publisherName, address, and email from bundesland.config.ts instead of placeholder text"
    - "Article detail page JSON-LD NewsArticle schema publisher.name reads from config.branding.impressum.publisherName"
    - "Remaining Impressum placeholders ([TELEFON], [UNTERNEHMENSGEGENSTAND], [BLATTLINIE], [DATENSCHUTZ_EMAIL]) are left as static strings for operator to fill"
  artifacts:
    - path: "src/app/(public)/impressum/page.tsx"
      provides: "Impressum page with 3 config-driven fields wired"
      contains: "config.branding.impressum.publisherName"
    - path: "src/app/(public)/artikel/[publicId]/[slug]/page.tsx"
      provides: "Article detail page with JSON-LD publisher.name from config"
      contains: "config.branding.impressum.publisherName"
  key_links:
    - from: "src/app/(public)/impressum/page.tsx"
      to: "bundesland.config.ts"
      via: "import config from '@/../bundesland.config'"
      pattern: "config\\.branding\\.impressum"
    - from: "src/app/(public)/artikel/[publicId]/[slug]/page.tsx"
      to: "bundesland.config.ts"
      via: "import config from '@/../bundesland.config'"
      pattern: "config\\.branding\\.impressum\\.publisherName"
---

<objective>
Wire three Impressum placeholder strings and the JSON-LD publisher name to values from bundesland.config.ts.

Purpose: Closes Phase 9 success criterion 4 — Impressum publisher details come from the Bundesland config file, not placeholder text. Enables reuse of this codebase for a different Bundesland by changing only bundesland.config.ts.
Output: impressum/page.tsx and article detail page.tsx with direct config imports replacing hardcoded strings.
</objective>

<execution_context>
@/Users/philipp/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md

<interfaces>
<!-- Config values to wire in. -->

From bundesland.config.ts:
```typescript
branding: {
  impressum: {
    publisherName: 'Ennstal Aktuell Medien GmbH',
    address: 'Mustergasse 1, 8940 Liezen, Steiermark',
    email: 'redaktion@ennstal-aktuell.at',
  },
}
```

Import pattern (established in project — same as other public Server Components):
```typescript
import config from '@/../bundesland.config'
```

Strings to REPLACE in impressum/page.tsx:
- `[MEDIENINHABER_NAME]` (or any hardcoded publisher name) → `{config.branding.impressum.publisherName}`
- Hardcoded address block → `{config.branding.impressum.address}, Österreich`
- `[EMAIL]` (or hardcoded email) → `{config.branding.impressum.email}` (also in mailto href)

Strings to LEAVE as static text (operator fills before launch):
- `[TELEFON]`, `[UNTERNEHMENSGEGENSTAND]`, `[BLATTLINIE]`, `[DATENSCHUTZ_EMAIL]`

String to REPLACE in article detail page JSON-LD:
- Hardcoded `"Ennstal Aktuell"` in publisher.name → `config.branding.impressum.publisherName`

"Österreich" stays hardcoded — platform is Austria-only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Impressum + JSON-LD publisher name to config</name>
  <files>src/app/(public)/impressum/page.tsx, src/app/(public)/artikel/[publicId]/[slug]/page.tsx</files>
  <action>
**impressum/page.tsx:**

1. Add `import config from '@/../bundesland.config'` at the top of the file (after existing imports).
2. Read the current file to identify the exact placeholder strings for publisher name, address, and email.
3. Replace:
   - Publisher name placeholder → `{config.branding.impressum.publisherName}`
   - Address block → `{config.branding.impressum.address}, Österreich`
   - Email placeholder → `{config.branding.impressum.email}` (also update mailto href to `href={\`mailto:${config.branding.impressum.email}\`}`)
4. Leave `[TELEFON]`, `[UNTERNEHMENSGEGENSTAND]`, `[BLATTLINIE]`, `[DATENSCHUTZ_EMAIL]` as-is — these are static placeholders for the operator.

**artikel/[publicId]/[slug]/page.tsx:**

1. Add `import config from '@/../bundesland.config'` if not already present (article detail page may already import config for other purposes — check before adding duplicate).
2. Find the JSON-LD object in the file. It will contain a `publisher` object with a hardcoded name (likely `"Ennstal Aktuell"` or similar).
3. Replace the hardcoded string with `config.branding.impressum.publisherName`.

**Verification steps (run after edits):**
- `npx tsc --noEmit` — no type errors
- `npx next build` — build succeeds (confirm no import resolution errors)

Commit: `feat(09-03): wire Impressum and JSON-LD publisher name from bundesland.config`
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
impressum/page.tsx has no hardcoded publisher name, address, or email — all three read from config.branding.impressum.
Article detail page JSON-LD publisher.name reads from config.branding.impressum.publisherName.
npx tsc --noEmit passes with no errors.
Manual check: next build + load /impressum confirms "Ennstal Aktuell Medien GmbH", "Mustergasse 1, 8940 Liezen, Steiermark", "redaktion@ennstal-aktuell.at" render correctly.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no TypeScript errors
2. `npx next build` — build succeeds
3. Manual: `next start`, load `/impressum` — verify publisherName, address, email match bundesland.config.ts values
4. Manual: load any article detail page, inspect HTML source for JSON-LD script tag — `publisher.name` matches config value
5. Grep: `grep -n "Ennstal Aktuell" src/app/\(public\)/artikel` — should only appear in config-sourced expressions, not as a hardcoded string literal
</verification>

<success_criteria>
- No hardcoded publisher name, address, or email strings in impressum/page.tsx
- JSON-LD publisher.name on article detail page reads from config
- npx tsc --noEmit and npx next build both pass
- Changing bundesland.config.ts branding.impressum values would update both pages without any component code changes
</success_criteria>

<output>
After completion, create `.planning/phases/09-ad-config-wiring-auth-hardening/09-03-SUMMARY.md`
</output>
